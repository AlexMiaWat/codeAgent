# 📊 Руководство по мониторингу Code Agent Server

## Обзор

Этот документ описывает, как запустить и мониторить работу Code Agent Server через логи.

## 🚀 Запуск сервера

### Способ 1: Прямой запуск

```bash
export OPENROUTER_API_KEY=your_api_key_here
python -m src.server
```

### Способ 2: Фоновый запуск

```bash
export OPENROUTER_API_KEY=your_api_key_here
python -m src.server &
```

### Способ 3: С использованием nohup

```bash
export OPENROUTER_API_KEY=your_api_key_here
nohup python -m src.server > server.log 2>&1 &
```

## 📝 Мониторинг через логи

### 1. Основной лог файл

Все события сервера записываются в:
```
logs/code_agent.log
```

### 2. Просмотр логов в реальном времени

#### Linux/Mac:
```bash
tail -f logs/code_agent.log
```

#### Windows:
```bash
# Используйте PowerShell
Get-Content logs\code_agent.log -Wait -Tail 30

# Или используйте наш скрипт
scripts\watch_logs.bat
```

### 3. Скрипт мониторинга

Используйте наш специальный скрипт для структурированного просмотра статуса:

```bash
python scripts/monitor_server.py
```

**Вывод скрипта:**
```
================================================================================
CODE AGENT SERVER - MONITORING
================================================================================
Session: 20260118_231126
Iteration: 1
Started: 2026-01-18T23:11:26.254894
Status: [RUNNING]

TASK STATISTICS:
   In Progress: 1
   Completed: 0
   Failed: 0
   Total: 1

CURRENT TASK:
   ID: task_1768767086
   Text: Реализация Learning (Этап 14)...
   State: in_progress
   Attempts: 1
   Started: 2026-01-18T23:11:26.274216
================================================================================
```

### 4. Непрерывный мониторинг

#### Linux/Mac:
```bash
watch -n 5 python scripts/monitor_server.py
```

#### Windows PowerShell:
```powershell
while ($true) { 
    cls
    python scripts/monitor_server.py
    Start-Sleep -Seconds 5
}
```

## 📊 Checkpoint система

### Файл checkpoint

Checkpoint файл содержит текущее состояние сервера:
```
D:\Space\life\.codeagent_checkpoint.json
```

### Структура checkpoint:

```json
{
    "version": "1.0",
    "server_state": {
        "last_start_time": "2026-01-18T23:11:26.254894",
        "last_stop_time": null,
        "iteration_count": 1,
        "clean_shutdown": false
    },
    "tasks": [
        {
            "task_id": "task_1768767086",
            "task_text": "Реализация Learning...",
            "state": "in_progress",
            "start_time": "2026-01-18T23:11:26.274216",
            "attempts": 1
        }
    ],
    "current_task": "task_1768767086",
    "session_id": "20260118_231126"
}
```

### Просмотр checkpoint:

```bash
# Красивый вывод JSON
cat D:\Space\life\.codeagent_checkpoint.json | python -m json.tool

# Или через Python
python -c "import json; print(json.dumps(json.load(open('D:/Space/life/.codeagent_checkpoint.json')), indent=2))"
```

## 🎯 Что показывают логи

### Уровни логирования

1. **INFO** - Обычная работа сервера
   - Инициализация
   - Начало/конец задач
   - Итерации

2. **WARNING** - Предупреждения
   - Fallback на альтернативные методы
   - Таймауты (не критичные)

3. **ERROR** - Ошибки
   - Критические ошибки выполнения
   - Проблемы с конфигурацией

### Фазы выполнения задачи

Каждая задача проходит через несколько фаз:

```
📍 Анализ задачи
   ↓
📍 Генерация инструкции
   ↓
📍 Выполнение через Cursor
   ↓
📍 Ожидание результата
   ↓
📍 Завершение
```

### Пример лога задачи:

```
23:11:26 - ================================================================================
ЗАДАЧА: Реализация Learning (Этап 14)
ID: task_1768767086
================================================================================
23:11:26 - --------------------------------------------------------------------------------
📍 Анализ задачи
--------------------------------------------------------------------------------
23:11:26 - Тип задачи определен: development
23:11:26 - Интерфейс: Cursor

23:11:26 - --------------------------------------------------------------------------------
📍 Генерация инструкции
--------------------------------------------------------------------------------
23:11:26 - 📝 Инструкция 1 (тип: development)
23:11:26 -    Переходим к выполнению задачи...

23:11:26 - --------------------------------------------------------------------------------
📍 ЭТАП 1, ИНСТРУКЦИЯ 1 - Выполнение через Cursor
--------------------------------------------------------------------------------
23:11:26 - 💬 Создан новый диалог
23:11:26 - Выполнение инструкции для задачи task_1768767086 через Cursor CLI

23:11:26 - ❌ Ответ от Cursor: ОШИБКА
23:11:26 -    Причина: CLI вернул код 1

23:11:26 - ℹ️  Переключение на файловый интерфейс

23:11:26 - --------------------------------------------------------------------------------
📍 Ожидание результата
--------------------------------------------------------------------------------
23:11:26 - ⏳ Ожидание результата...
23:11:26 -    Файл: docs/results/last_result.md
23:11:26 -    Таймаут: 300с
```

## 🔍 Полезные команды для анализа

### Поиск ошибок:

```bash
# Все ошибки
grep "ERROR" logs/code_agent.log

# Последние 10 ошибок
grep "ERROR" logs/code_agent.log | tail -10
```

### Поиск предупреждений:

```bash
grep "WARNING" logs/code_agent.log
```

### Статистика выполнения:

```bash
# Количество завершенных задач
grep "Задача выполнена" logs/code_agent.log | wc -l

# Количество ошибок
grep "ERROR" logs/code_agent.log | wc -l
```

### Фильтрация по задаче:

```bash
# Все логи для конкретной задачи
grep "task_1768767086" logs/code_agent.log
```

## 📈 Метрики производительности

### Время выполнения задач

Логи содержат временные метки для каждой задачи:
- Время начала: `start_time`
- Время завершения: `end_time`
- Количество попыток: `attempts`

### Статистика сессии

Checkpoint файл содержит:
- Количество итераций
- Количество выполненных задач
- Количество задач с ошибками

## 🛠️ Утилиты мониторинга

### 1. monitor_server.py

**Назначение:** Структурированный вывод статуса сервера

**Использование:**
```bash
python scripts/monitor_server.py
```

**Функции:**
- Показывает текущую сессию
- Статистику задач
- Текущую задачу
- Последние завершенные задачи
- Задачи с ошибками
- Последние строки лога

### 2. watch_logs.bat (Windows)

**Назначение:** Просмотр логов в реальном времени

**Использование:**
```bash
scripts\watch_logs.bat
```

**Функции:**
- Автоматическое обновление каждые 5 секунд
- Показывает последние 30 строк лога
- Временная метка обновления

## 🚨 Что делать при проблемах

### Сервер не запускается

1. Проверьте логи:
   ```bash
   tail -50 logs/code_agent.log
   ```

2. Проверьте переменные окружения:
   ```bash
   echo $OPENROUTER_API_KEY
   ```

3. Проверьте зависимости:
   ```bash
   pip list | grep crewai
   pip list | grep litellm
   ```

### Задачи зависают

1. Проверьте checkpoint:
   ```bash
   cat D:\Space\life\.codeagent_checkpoint.json | python -m json.tool
   ```

2. Проверьте, не превышен ли таймаут:
   - По умолчанию: 300 секунд (5 минут)

3. Проверьте файлы инструкций:
   ```bash
   ls -la D:\Space\life\cursor_commands\
   ```

### Много ошибок в логах

1. Проверьте типы ошибок:
   ```bash
   grep "ERROR" logs/code_agent.log | tail -20
   ```

2. Проверьте конфигурацию:
   ```bash
   cat config/config.yaml
   ```

## 📚 Дополнительные ресурсы

- **Документация checkpoint системы:** `docs/checkpoint_recovery.md`
- **Руководство по восстановлению:** `docs/quick_recovery_guide.md`
- **Конфигурация логирования:** `config/logging.yaml`
- **Примеры использования:** `examples/checkpoint_demo.py`

## 🎓 Лучшие практики

1. **Регулярно проверяйте логи** - используйте `monitor_server.py`
2. **Следите за checkpoint файлом** - он показывает реальное состояние
3. **Используйте фильтры** - grep для поиска конкретных событий
4. **Архивируйте старые логи** - для экономии места
5. **Мониторьте метрики** - время выполнения, количество ошибок

## 💡 Советы

- Используйте `watch` для непрерывного мониторинга
- Настройте уведомления при критических ошибках
- Регулярно проверяйте checkpoint для отслеживания прогресса
- Используйте цветные логи для быстрого визуального анализа

---

**Дата создания:** 2026-01-18  
**Версия:** 1.0  
**Автор:** Code Agent Team
