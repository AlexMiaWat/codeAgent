# Правила организации файлов для Code Agent

## 🚨 ВАЖНЫЕ ИНСТРУКЦИИ ДЛЯ AI АССИСТЕНТА

### ❌ СТРОГИЙ ЗАПРЕТ НА АВТОМАТИЧЕСКИЕ КОММИТЫ
- **НИКОГДА** не делай автоматические коммиты в Git!
- **НИКОГДА** не выполняй `git add` или `git commit` без прямого указания пользователя!
- Все изменения должны быть проверены и одобрены пользователем
- Коммиты делать ТОЛЬКО по явному запросу: "сделай коммит" или "commit changes"
- При любых изменениях в коде спрашивать подтверждение перед коммитом
- Это правило имеет высший приоритет над всеми остальными!

### GIT КОММИТЫ
- **ЗАПРЕЩЕНО** делать автоматические коммиты в Git!
- Все изменения должны быть проверены пользователем
- Коммиты делать ТОЛЬКО по явному запросу пользователя
- При изменениях в коде всегда спрашивать подтверждение
- Не выполнять `git add`/`git commit` без прямого указания пользователя
- Исключение: мелкие правки форматирования по запросу

### РАБОТА С КОДОМ
- Всегда проверять синтаксис после изменений
- Документировать изменения в changelog
- Следовать существующей структуре проекта
- Спрашивать подтверждение перед существенными изменениями

## 🎯 Общие принципы

При создании или перемещении файлов в проекте Code Agent, ВСЕГДА следуй этой структуре:

## 📁 Структура директорий

### `/test` - Тесты
**ВСЕ тесты должны быть в `/test` или поддиректориях:**
- ✅ `test/test_*.py` - основные тесты
- ✅ `test/integration/test_*.py` - интеграционные тесты
- ✅ `test/unit/test_*.py` - юнит-тесты (если создаются)
- ✅ `test/cursor_commands/*.txt` - тестовые команды для Cursor
- ❌ **НИКОГДА** не создавай тесты в корне проекта
- ❌ **НИКОГДА** не создавай тесты в `src/`

**Примеры правильных путей:**
- `test/test_new_feature.py`
- `test/integration/test_server_integration.py`
- `test/unit/test_utils.py`

### `/logs` - Логи
**ВСЕ логи должны быть в `/logs` (не `/log`):**
- ✅ `logs/*.log` - все лог-файлы
- ✅ `logs/codeagent.log` - основной лог
- ✅ `logs/task_*.log` - логи задач
- ✅ `logs/errors.log` - лог ошибок
- ❌ **НИКОГДА** не создавай логи в корне проекта
- ❌ **НИКОГДА** не создавай логи в `src/`
- ❌ **НИКОГДА** не используй директорию `/log` (только `/logs`)

**Примеры правильных путей:**
- `logs/codeagent.log`
- `logs/task_12345_20260118_120000.log`
- `logs/errors.log`

### `/docs` - Документация
**ВСЕ MD файлы (кроме README.md в корне) должны быть в `/docs`:**
- ✅ `docs/core/*.md` - основная документация (API, архитектура)
- ✅ `docs/guides/*.md` - руководства пользователя
- ✅ `docs/integration/*.md` - документация интеграций
- ✅ `docs/features/*.md` - документация функций
- ✅ `docs/solutions/*.md` - решения проблем и отчеты
- ✅ `docs/planning/*.md` - планирование и roadmap
- ✅ `docs/archive/reports/*.md` - архивные отчеты
- ✅ `docs/archive/research/*.md` - архивные исследования
- ✅ `docs/changelog/*.md` - история изменений
- ✅ `docs/testing/*.md` - документация по тестированию
- ✅ `README.md` в корне - единственный MD файл в корне
- ❌ **НИКОГДА** не создавай MD файлы в корне (кроме README.md)
- ❌ **НИКОГДА** не создавай MD файлы в `src/`

**Примеры правильных путей:**
- `docs/guides/NEW_FEATURE_GUIDE.md`
- `docs/integration/API_INTEGRATION.md`
- `docs/archive/reports/IMPLEMENTATION_REPORT.md`
- `docs/core/ARCHITECTURE.md`

### `/config` - Конфигурация
**ВСЕ конфигурационные файлы должны быть в `/config`:**
- ✅ `config/*.yaml` - YAML конфигурации
- ✅ `config/*.yml` - альтернативный формат YAML
- ✅ `config/*.json` - JSON конфигурации (если нужны)
- ❌ **НИКОГДА** не создавай конфиги в корне (кроме стандартных: pyproject.toml, pytest.ini)
- ❌ **НИКОГДА** не создавай конфиги в `src/`

**Примеры правильных путей:**
- `config/config.yaml`
- `config/agents.yaml`
- `config/logging.yaml`
- `config/llm_settings.yaml`

### `/src` - Исходный код
**ВСЕ исходные Python файлы должны быть в `/src` или поддиректориях:**
- ✅ `src/*.py` - основные модули
- ✅ `src/agents/*.py` - агенты CrewAI
- ✅ `src/llm/*.py` - модули работы с LLM
- ✅ `src/tools/*.py` - пользовательские инструменты
- ❌ **НИКОГДА** не создавай исходный код в корне (кроме main.py)
- ❌ **НИКОГДА** не создавай тесты в `src/`

**Примеры правильных путей:**
- `src/new_module.py`
- `src/agents/custom_agent.py`
- `src/llm/model_wrapper.py`

### `/scripts` - Скрипты
**ВСЕ вспомогательные скрипты должны быть в `/scripts`:**
- ✅ `scripts/*.py` - Python скрипты
- ✅ `scripts/*.bat` - Windows batch скрипты
- ✅ `scripts/*.sh` - Shell скрипты
- ❌ **НИКОГДА** не создавай скрипты в корне (кроме стандартных: Makefile)

**Примеры правильных путей:**
- `scripts/setup.py`
- `scripts/monitor_server.py`
- `scripts/agent.bat`

### `/docker` - Docker
**ВСЕ Docker файлы должны быть в `/docker`:**
- ✅ `docker/*.yml` - Docker Compose файлы
- ✅ `docker/Dockerfile.*` - Dockerfile'ы
- ✅ `docker/*.sh` - Docker скрипты
- ❌ **НИКОГДА** не создавай Docker файлы в корне

**Примеры правильных путей:**
- `docker/docker-compose.agent.yml`
- `docker/Dockerfile.agent`
- `docker/test_docker_agent.sh`

### `/examples` - Примеры
**ВСЕ примеры кода должны быть в `/examples`:**
- ✅ `examples/*.py` - примеры использования
- ❌ **НИКОГДА** не создавай примеры в корне

**Примеры правильных путей:**
- `examples/demo_feature.py`
- `examples/usage_example.py`

## 🚫 Запрещенные действия

### НЕ создавай файлы в корне проекта:
- ❌ Тесты (используй `/test`)
- ❌ Логи (используй `/logs`)
- ❌ MD документация (используй `/docs`, кроме README.md)
- ❌ Конфигурация (используй `/config`)
- ❌ Исходный код (используй `/src`, кроме main.py)
- ❌ Скрипты (используй `/scripts`)
- ❌ Docker файлы (используй `/docker`)

### НЕ используй неправильные имена директорий:
- ❌ `/log` (используй `/logs`)
- ❌ `/test/` в корне (используй `/test`)
- ❌ `/doc` (используй `/docs`)

## ✅ Что МОЖНО в корне

В корне проекта разрешены только:
- ✅ `README.md` - основная документация
- ✅ `main.py` - точка входа
- ✅ `pyproject.toml` - конфигурация Python проекта
- ✅ `pytest.ini` - конфигурация pytest
- ✅ `Makefile` - Make команды
- ✅ `requirements*.txt` - зависимости
- ✅ `.gitignore` - правила Git
- ✅ `.cursorrules` - этот файл
- ✅ Стандартные конфигурационные файлы (`.env.example`, и т.д.)

## 📋 Чеклист перед созданием файла

Перед созданием любого файла, проверь:

1. **Тест?** → `/test/` или `/test/integration/`
2. **Лог?** → `/logs/` (не `/log`)
3. **MD файл?** → `/docs/` (кроме README.md)
4. **Конфиг?** → `/config/`
5. **Исходный код?** → `/src/` или поддиректории
6. **Скрипт?** → `/scripts/`
7. **Docker?** → `/docker/`
8. **Пример?** → `/examples/`

## 🎯 Примеры правильного размещения

### Создание нового теста
```
❌ НЕПРАВИЛЬНО: test_new_feature.py (в корне)
✅ ПРАВИЛЬНО: test/test_new_feature.py
✅ ПРАВИЛЬНО: test/integration/test_new_feature.py
```

### Создание нового лога
```
❌ НЕПРАВИЛЬНО: app.log (в корне)
❌ НЕПРАВИЛЬНО: log/app.log (неправильная директория)
✅ ПРАВИЛЬНО: logs/app.log
✅ ПРАВИЛЬНО: logs/task_12345.log
```

### Создание новой документации
```
❌ НЕПРАВИЛЬНО: NEW_FEATURE.md (в корне)
❌ НЕПРАВИЛЬНО: docs/NEW_FEATURE.md (без категории)
✅ ПРАВИЛЬНО: docs/guides/NEW_FEATURE.md
✅ ПРАВИЛЬНО: docs/features/NEW_FEATURE.md
✅ ПРАВИЛЬНО: docs/integration/NEW_FEATURE.md
```

### Создание нового модуля
```
❌ НЕПРАВИЛЬНО: new_module.py (в корне)
❌ НЕПРАВИЛЬНО: src/new_module.py (если это агент)
✅ ПРАВИЛЬНО: src/new_module.py (обычный модуль)
✅ ПРАВИЛЬНО: src/agents/new_agent.py (если это агент)
✅ ПРАВИЛЬНО: src/llm/new_llm.py (если это LLM модуль)
```

## 🔍 Если сомневаешься

Если не уверен, куда поместить файл:
1. Проверь существующую структуру проекта
2. Посмотри на похожие файлы
3. Следуй логике: тесты → `/test`, логи → `/logs`, документация → `/docs`
4. Если это новый тип файла, создай подходящую директорию в соответствующем месте

## 📚 Дополнительная информация

Подробная структура проекта описана в:
- `docs/core/project_structure.md`
