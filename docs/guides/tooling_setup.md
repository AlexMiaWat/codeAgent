# Настройка инструментария для разработки

Этот документ описывает настройку инструментов для эффективной разработки в проекте Code Agent.

## 🎯 Цель

Обеспечить автоматическое форматирование, линтинг, проверку типов и запуск тестов для поддержания высокого качества кода и ускорения разработки.

## 📦 Установленные инструменты

### Обязательные инструменты (уже в зависимостях)

- **Black** — автоматическое форматирование кода
- **Ruff** — сверхбыстрый линтер и форматтер
- **Mypy** — статическая проверка типов
- **Pytest** — фреймворк для тестирования
- **Pre-commit** — управление pre-commit хуками

### Дополнительные инструменты (рекомендуемые)

- **pytest-cov** — покрытие кода
- **pytest-xdist** — параллельный запуск тестов
- **bandit** — проверка безопасности кода (опционально)

## ⚙️ Настройка

### 1. Установка зависимостей

Убедитесь, что установлены все dev-зависимости:

```bash
pip install -e ".[dev]"
```

Или вручную:

```bash
pip install black ruff mypy pytest pre-commit
```

### 2. Pre-commit хуки

В корне проекта уже создан файл `.pre-commit-config.yaml`. Для установки хуков выполните:

```bash
pre-commit install
```

Теперь при каждом коммите будут автоматически запускаться:
- Удаление лишних пробелов и фикс окончаний файлов
- Проверка YAML, JSON, TOML
- Форматирование Black
- Линтинг Ruff с автоматическим исправлением
- Проверка типов Mypy
- Запуск тестов Pytest (только при push)

Чтобы запустить хуки на всех файлах:

```bash
pre-commit run --all-files
```

### 3. Настройка VS Code

Файл `.vscode/settings.json` уже содержит рекомендуемые настройки:

- **Форматирование при сохранении** (Black)
- **Автоматическое исправление при сохранении** (Ruff)
- **Линтинг при сохранении** (Ruff, Mypy)
- **Удаление лишних пробелов и добавление final newline**

Если вы используете другую IDE, настройте аналогичные параметры.

## 🚀 Использование скриптов

### Скрипт для комплексной проверки

В `scripts/run_quality_checks.py` находится скрипт, который запускает все проверки одной командой:

```bash
python scripts/run_quality_checks.py
```

Скрипт выполняет:
1. Проверку форматирования Black
2. Линтинг Ruff с автоматическим исправлением
3. Проверку типов Mypy
4. Запуск всех тестов Pytest

Если какая-либо проверка не проходит, скрипт завершится с ненулевым кодом и даст рекомендации по исправлению.

### Интеграция с CI

Вы можете добавить запуск этого скрипта в ваш CI/CD пайплайн для гарантии качества кода.

## 🧪 Тестирование

### Запуск тестов

```bash
python -m pytest test/ -x --tb=short
```

Или с покрытием:

```bash
python -m pytest test/ --cov=src --cov-report=term-missing
```

### Маркировка тестов

Тесты помечены маркерами:
- `unit` — модульные тесты (быстрые)
- `integration` — интеграционные тесты (требуют внешних зависимостей)
- `cursor` — тесты, требующие Cursor IDE
- `llm` — тесты, требующие доступ к LLM API
- `docker` — тесты, требующие Docker

Запуск только unit-тестов:

```bash
python -m pytest test/ -m unit
```

## 🔧 Ручные команды

### Форматирование

```bash
python -m black .
```

### Линтинг и автоматическое исправление

```bash
python -m ruff check --fix .
```

### Проверка типов

```bash
python -m mypy src test --ignore-missing-imports --show-error-codes
```

## 📝 Рекомендации

### Перед коммитом

1. Запустите скрипт проверки: `python scripts/run_quality_checks.py`
2. Или доверьтесь pre-commit хукам — они автоматически запустятся при `git commit`

### При получении ошибок

- **Black**: запустите `python -m black .` для автоматического форматирования
- **Ruff**: запустите `python -m ruff check --fix .` для автоматического исправления большинства проблем
- **Mypy**: исправьте аннотации типов в соответствии с сообщениями об ошибках
- **Pytest**: исправьте упавшие тесты

## 🐛 Устранение неполадок

### Pre-commit хуки не запускаются

Убедитесь, что хуки установлены:

```bash
pre-commit install
```

Проверьте, что файл `.git/hooks/pre-commit` существует.

### Ошибка "command not found"

Убедитесь, что инструменты установлены в текущем окружении Python:

```bash
pip list | grep -E "black|ruff|mypy|pytest"
```

### VS Code не форматирует код

1. Убедитесь, что расширение "Python" и "Black Formatter" установлены
2. Проверьте настройки VS Code: `Ctrl+Shift+P` → "Preferences: Open Settings (JSON)"
3. Убедитесь, что `"editor.formatOnSave": true` и `"python.formatting.provider": "black"`

## 📚 Дополнительные ресурсы

- [Документация Black](https://black.readthedocs.io/)
- [Документация Ruff](https://docs.astral.sh/ruff/)
- [Документация Mypy](https://mypy.readthedocs.io/)
- [Документация Pytest](https://docs.pytest.org/)
- [Документация Pre-commit](https://pre-commit.com/)

---

**Обновлено:** 2026-02-05  
**Версия:** 1.0  
**Автор:** Code Agent Team