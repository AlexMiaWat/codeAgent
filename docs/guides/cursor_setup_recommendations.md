# Рекомендации по настройке Cursor для Code Agent

**Дата:** 2025-01-26  
**Цель:** Определить оптимальную конфигурацию Cursor IDE и технологий для работы с Code Agent

---

## 🎯 ОБЗОР: Что нужно для идеальной работы Code Agent

Code Agent должен взаимодействовать с Cursor IDE для автоматизации выполнения задач. Для этого необходима правильная настройка Cursor, установка расширений, библиотек и конфигурация.

### ⚠️ ВАЖНО: Различие между проектами

- **Проект Code Agent** (`D:\Space\codeAgent\`) - сам инструмент автоматизации
- **Целевой проект** (`<PROJECT_DIR>`, например `D:\Space\life\`) - проект, который дорабатывает Code Agent

Настройки для этих проектов **разные** и должны быть четко разделены!

---

## 📦 НЕОБХОДИМЫЕ БИБЛИОТЕКИ И ЗАВИСИМОСТИ

### Python библиотеки для Code Agent

#### Обязательные (уже в проекте):
```toml
# pyproject.toml
dependencies = [
    "crewai>=0.1.0",           # ✅ Уже есть
    "crewai-tools>=0.1.0",    # ✅ Уже есть
    "pyyaml>=6.0",            # ✅ Уже есть
    "python-dotenv>=1.0.0",   # ✅ Уже есть
]
```

#### Критически необходимые (отсутствуют):
```toml
# Добавить в pyproject.toml
dependencies = [
    # Для работы с Cursor IDE
    "pyautogui>=0.9.54",      # Управление мышью и клавиатурой
    "pytesseract>=0.3.10",    # OCR для распознавания текста на скриншотах
    "Pillow>=10.0.0",         # Обработка изображений (зависимость для pyautogui)
    "opencv-python>=4.8.0",   # Обработка изображений для анализа интерфейса
    "mss>=9.0.1",             # Быстрые скриншоты экрана
    "pygetwindow>=0.0.9",     # Управление окнами Windows
    
    # Для работы с файлами и репортами
    "watchdog>=3.0.0",        # Мониторинг изменений файлов (для ожидания репортов)
    "aiofiles>=23.2.0",       # Асинхронная работа с файлами
    
    # Для работы с Git
    "GitPython>=3.1.40",       # Работа с Git репозиториями
    
    # Для улучшенного логирования
    "colorlog>=6.8.0",        # Цветное логирование
    "rich>=13.7.0",           # Красивый вывод в консоль
]
```

#### Рекомендуемые (для улучшения):
```toml
# Для альтернативных подходов
dependencies = [
    "easyocr>=1.7.0",         # Альтернатива pytesseract (более точная, но медленнее)
    "keyboard>=0.13.5",       # Альтернатива pyautogui для клавиатуры
    "pynput>=1.7.6",          # Альтернатива для управления вводом
]
```

---

## 🔧 НАСТРОЙКА CURSOR IDE

> ⚠️ **ВАЖНО:** Настройки Cursor различаются для проекта Code Agent и для целевого проекта!

### 1. Расширения Cursor (если доступны)

#### Рекомендуемые расширения:
- **Code Agent Integration** (если будет создано) - для программного взаимодействия
- **GitLens** - для лучшей работы с Git
- **Error Lens** - для визуализации ошибок
- **TODO Highlight** - для работы с todo-листами

### 2. Настройки Cursor для проекта Code Agent

#### Файл: `D:\Space\codeAgent\.cursor\settings.json`

**Назначение:** Настройки Cursor IDE для разработки самого Code Agent.

```json
{
  "cursor.chat.autoSave": true,
  "cursor.chat.saveLocation": "docs/results/",
  "cursor.chat.maxHistory": 100,
  "files.watcherExclude": {
    "**/node_modules/**": true,
    "**/.git/**": true,
    "**/logs/**": true
  },
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000
}
```

**Примечание:** Пути относительны к корню проекта Code Agent (`D:\Space\codeAgent\`).

### 3. Настройки Cursor для целевого проекта (опционально)

#### Файл: `<PROJECT_DIR>\.cursor\settings.json` (например, `D:\Space\life\.cursor\settings.json`)

**Назначение:** Настройки Cursor IDE для работы с целевым проектом, который дорабатывает Code Agent.

```json
{
  "cursor.chat.autoSave": true,
  "cursor.chat.saveLocation": "docs/results/",
  "cursor.chat.maxHistory": 100,
  "files.watcherExclude": {
    "**/node_modules/**": true,
    "**/.git/**": true,
    "**/logs/**": true,
    "**/cursor_commands/**": true,
    "**/cursor_results/**": true
  },
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000
}
```

**Примечание:** 
- Пути относительны к корню целевого проекта (`<PROJECT_DIR>`)
- Эти настройки могут создаваться автоматически при инициализации проекта
- Не критичны для работы, если используется файловый интерфейс или CLI

### 4. Горячие клавиши Cursor

#### Рекомендуемые настройки:
- `Ctrl+Shift+P` → "Cursor: New Chat" - для создания нового диалога
- `Ctrl+Enter` - для отправки сообщения в чат
- `Ctrl+Shift+R` - для перезапуска агента (если доступно)

**Настройка в Cursor:**
1. Открыть настройки (`Ctrl+,`)
2. Перейти в "Keyboard Shortcuts"
3. Настроить горячие клавиши для работы с чатом

---

## 🖥️ НАСТРОЙКА ОПЕРАЦИОННОЙ СИСТЕМЫ

### Windows (текущая ОС)

#### Необходимые компоненты:
1. **Tesseract OCR** (для pytesseract):
   - Скачать: https://github.com/UB-Mannheim/tesseract/wiki
   - Установить в `C:\Program Files\Tesseract-OCR`
   - Добавить в PATH или указать путь в коде:
   ```python
   pytesseract.pytesseract.tesseract_cmd = r'C:\Program Files\Tesseract-OCR\tesseract.exe'
   ```

2. **Разрешения для автоматизации:**
   - Windows может требовать разрешения для управления окнами
   - Настроить в "Настройки" → "Конфиденциальность" → "Автоматизация"

3. **Настройки дисплея:**
   - Рекомендуется масштаб 100% для точного распознавания координат
   - Если используется масштаб >100%, нужно учитывать в коде

### Альтернатива: Linux/Mac

Для Linux/Mac потребуются аналогичные библиотеки, но с другими путями установки.

---

## 📝 КОНФИГУРАЦИЯ CODE AGENT

> ⚠️ **ВАЖНО:** `config/config.yaml` находится в проекте Code Agent (`D:\Space\codeAgent\config\`), но пути в нем указывают на **целевой проект** через переменную `${PROJECT_DIR}`.

**Ключевые моменты конфигурации:**
- `project.base_dir: ${PROJECT_DIR}` - путь к целевому проекту из `.env`
- Все пути в шаблонах инструкций (`docs/results/...`, `docs/reviews/...`) - **относительно целевого проекта**
- `cursor.ocr.tesseract_path` - путь к Tesseract на системе

**Полная конфигурация:** См. `config/config.yaml` в проекте Code Agent.

---

## 🔌 РАСШИРЕНИЯ И ИНТЕГРАЦИИ

> 📖 **Подробная информация о Cursor API и интеграции:**  
> См. раздел [Cursor API и программная интеграция](../integration/cursor_integration.md) в документации по интеграции.

### 1. Cursor API

**Доступные API:**
- ✅ **Background Agents API** - для работы с репозиториями
- ✅ **Admin API** - для управления командой
- ✅ **AI Code Tracking API** - для аналитики
- ✅ **Custom API Keys** - для подключения собственных ключей
- ❌ **Публичный API для управления чатом** - пока отсутствует

**Альтернативные подходы:**
- UI автоматизация (pywinauto, win32gui)
- Cursor CLI (headless режим)
- Файловая система как канал связи
- MCP (Model Context Protocol)

### 2. Расширение для Cursor (если будет создано)

Если будет создано расширение для Cursor, оно должно:
- Предоставлять API для программного взаимодействия
- Слушать файлы-команды от Code Agent
- Автоматически создавать диалоги
- Отправлять результаты обратно в файлы-репорты

### 3. Альтернативные подходы

#### Вариант A: Файловая система
- Code Agent создает файл `cursor_commands/instruction_001.txt`
- Cursor расширение читает файл и выполняет команду
- Результат записывается в `cursor_results/result_001.txt`

#### Вариант B: HTTP API (если Cursor предоставит)
- Code Agent отправляет HTTP запросы в Cursor
- Cursor выполняет команды и возвращает результаты

#### Вариант C: WebSocket (для real-time взаимодействия)
- Двусторонняя связь между Code Agent и Cursor

---

## 🧪 ТЕСТИРОВАНИЕ И ОТЛАДКА

### 1. Тестовый режим

Добавить в `config.yaml`:
```yaml
testing:
  enabled: false
  mock_cursor: true  # Использовать мок вместо реального Cursor
  screenshot_dir: "tests/screenshots"
  save_all_screenshots: true
```

### 2. Инструменты для отладки

- **Snipping Tool** (Windows) - для проверки координат элементов
- **Inspect Element** - для анализа структуры интерфейса Cursor
- **Logging** - детальное логирование всех операций

---

## 📊 МОНИТОРИНГ И МЕТРИКИ

### Рекомендуемые метрики:

1. **Время выполнения задач**
2. **Количество успешных/неуспешных выполнений**
3. **Время ожидания репортов**
4. **Количество ошибок OCR**
5. **Количество повторных попыток**

### Инструменты:

- **Prometheus** + **Grafana** (для production)
- **Простое логирование** (для разработки)

---

## 🚀 РЕКОМЕНДАЦИИ ПО РАЗВЕРТЫВАНИЮ

### 1. Разработка

```bash
# Установка зависимостей
pip install -e ".[dev]"

# Запуск в режиме разработки
python -m src.server --dev
```

### 2. Production

```bash
# Установка только необходимых зависимостей
pip install -e .

# Запуск как сервис
python -m src.server --production
```

### 3. Docker (опционально)

Создать `Dockerfile` для изолированного запуска.

---

## ✅ ЧЕКЛИСТ НАСТРОЙКИ

### Для проекта Code Agent (`D:\Space\codeAgent\`):

- [ ] Установлены все Python зависимости из `pyproject.toml`
- [ ] Создан `.cursor/settings.json` в корне Code Agent проекта
- [ ] Настроен `.env` с `PROJECT_DIR` и API ключами

### Для целевого проекта (`<PROJECT_DIR>`, например `D:\Space\life\`):

- [ ] Созданы директории `docs/results/` и `docs/reviews/`
- [ ] (Опционально) Создан `.cursor/settings.json` в корне целевого проекта
- [ ] Создан `todo.txt` или `todo.md` со списком задач
- [ ] Если используется файловый интерфейс: созданы `cursor_commands/` и `cursor_results/`

### Дополнительно (системные настройки):

- [ ] Установлен Tesseract OCR (если используется OCR-интерфейс)
- [ ] Настроены горячие клавиши в Cursor (опционально)
- [ ] Протестирована работа `pyautogui` (если используется UI-интерфейс)

---

## 🎓 ОБУЧЕНИЕ И ДОКУМЕНТАЦИЯ

### Рекомендуемые материалы:

1. **Документация pyautogui**: https://pyautogui.readthedocs.io/
2. **Документация pytesseract**: https://pypi.org/project/pytesseract/
3. **Документация CrewAI**: https://docs.crewai.com/
4. **Документация Cursor**: https://cursor.sh/docs

---

## 🔒 БЕЗОПАСНОСТЬ

### Рекомендации:

1. **Не хранить API ключи в коде** - использовать `.env`
2. **Ограничить права выполнения кода** - использовать изолированную среду
3. **Логировать все операции** - для аудита
4. **Валидировать все входные данные** - для предотвращения инъекций

---

**Заключение:** Правильная настройка Cursor и установка необходимых библиотек критически важны для работы Code Agent. Следуйте этому руководству для достижения максимальной эффективности.
