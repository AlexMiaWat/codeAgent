# Рекомендации по MCP серверам для Python разработки

## Обзор

Model Context Protocol (MCP) позволяет расширить возможности AI-ассистентов (таких как Kilo Code, Claude Code, Cursor AI) за счёт подключения специализированных серверов. Для эффективной работы с Python проектами рекомендуем подключить следующие MCP серверы.

## 🎯 Критерии выбора

- **Релевантность для Python**: анализ кода, рефакторинг, линтинг, тестирование.
- **Активная разработка**: серверы с регулярными обновлениями.
- **Простота установки**: доступность через pip, Docker или бинарные сборки.
- **Интеграция с существующим стеком**: совместимость с black, ruff, pytest, git.

## 🚀 Рекомендуемые MCP серверы

### 1. Python Refactoring Assistant (`slamer59/mcp-python-refactoring`)

**Назначение**: Анализ Python кода и предоставление рекомендаций по рефакторингу (без автоматического изменения кода). Использует библиотеки rope, radon, vulture, jedi, libcst, mccabe.

**Преимущества**:
- Обнаружение сложных участков кода (цикломатическая сложность).
- Выявление dead code (vulture).
- Предложения по улучшению архитектуры.
- Работает в режиме "советника" — не изменяет код без согласия.

**Установка**:
```bash
# Через pip
pip install mcp-python-refactoring

# Или через uv
uvx mcp-python-refactoring
```

**Конфигурация MCP** (пример для Cursor):
```json
{
  "mcpServers": {
    "python-refactoring": {
      "command": "uvx",
      "args": ["mcp-python-refactoring"],
      "env": {}
    }
  }
}
```

### 2. Refactor MCP (`myuon/refactor-mcp`)

**Назначение**: Мощный инструмент для рефакторинга на основе regex-поиска и замены с поддержкой контекстных паттернов и фильтрации файлов.

**Преимущества**:
- Автоматическая замена кода по шаблонам.
- Поддержка capture groups (`$1`, `$2`).
- Ограничение операций определёнными файлами (glob patterns).
- Режим dry-run для предварительного просмотра изменений.

**Установка**:
```bash
# Через npm (сервер написан на TypeScript)
npx @myuon/refactor-mcp

# Или через Docker
docker run -it ghcr.io/myuon/refactor-mcp:latest
```

**Конфигурация**:
```json
{
  "mcpServers": {
    "refactor-mcp": {
      "command": "npx",
      "args": ["@myuon/refactor-mcp"],
      "env": {}
    }
  }
}
```

### 3. Python Code Analyzer (`seanivore/code-analyzer`)

**Назначение**: Анализ структуры, сложности и зависимостей Python кода с использованием парсинга AST.

**Преимущества**:
- Детальный отчёт о связях между модулями.
- Оценка сложности функций/классов.
- Выявление циклических импортов.
- Интеграция с VS Code.

**Установка**:
```bash
pip install code-analyzer-mcp
```

### 4. MCP Server Analyzer for Python (`anselmoo/mcp-server-analyzer`)

**Назначение**: Статический анализ Python кода с использованием RUFF (линтер) и VULTURE (обнаружение dead code).

**Преимущества**:
- Быстрый линтинг через ruff.
- Обнаружение неиспользуемого кода через vulture.
- Не требует дополнительных файлов конфигурации.
- Можно запускать через uvx, pip, Docker или из исходников.

**Установка**:
```bash
uvx mcp-server-analyzer
```

### 5. Git MCP Server (`modelcontextprotocol/servers/tree/main/src/git`)

**Назначение**: Операции с Git: коммиты, ветки, история, diff.

**Преимущества**:
- Просмотр статуса репозитория.
- Создание коммитов, управление ветками.
- Интеграция с AI-ассистентом для автоматизации Git-операций.

**Установка**:
Официальный сервер из репозитория MCP:
```bash
npm install -g @modelcontextprotocol/server-git
```

### 6. Filesystem MCP Server (`modelcontextprotocol/servers/tree/main/src/filesystem`)

**Назначение**: Навигация по файловой системе, чтение/запись файлов, поиск.

**Преимущества**:
- Позволяет ассистенту исследовать структуру проекта.
- Чтение и редактирование файлов через MCP.
- Уже может быть подключен (проверьте конфигурацию).

### 7. Docker MCP Server (`modelcontextprotocol/servers/tree/main/src/docker`)

**Назначение**: Управление Docker-контейнерами, образами, сетями.

**Преимущества**:
- Запуск/остановка контейнеров.
- Мониторинг состояния Docker.
- Полезно для тестирования в изолированном окружении.

## ⚙️ Интеграция с текущим проектом

### Текущие MCP серверы

В проекте уже подключены:
- **components-server** — управление компонентами
- **puppeteer** — автоматизация браузера
- **memory** — граф знаний
- **microsoft-learn** — документация Microsoft
- **n8n-mcp** — автоматизация workflows
- **context7** — актуальная документация библиотек
- **tavily** — веб-поиск
- **exa** — поиск кода и компаний

### Добавление новых серверов

#### Способ 1: Через Cursor IDE

1. Откройте настройки Cursor: `Ctrl + ,`
2. Найдите раздел "MCP Servers"
3. Добавьте новый сервер, указав команду запуска и аргументы.
4. Перезапустите Cursor.

#### Способ 2: Через конфигурационный файл

Создайте или отредактируйте файл `~/.cursor/mcp.json`:

```json
{
  "mcpServers": {
    "python-refactoring": {
      "command": "uvx",
      "args": ["mcp-python-refactoring"]
    },
    "refactor-mcp": {
      "command": "npx",
      "args": ["@myuon/refactor-mcp"]
    },
    "python-analyzer": {
      "command": "uvx",
      "args": ["mcp-server-analyzer"]
    },
    "git": {
      "command": "npx",
      "args": ["@modelcontextprotocol/server-git"]
    }
  }
}
```

#### Способ 3: Через переменные окружения

Для Kilo Code, запущенного как агент, можно передавать MCP серверы через переменную `MCP_SERVERS`.

### Проверка подключения

После добавления серверов перезапустите клиент MCP (Cursor или другую IDE). Убедитесь, что новые инструменты доступны в чате с ассистентом.

## 🧪 Тестирование работы

Для проверки функциональности новых MCP серверов выполните:

1. **Python Refactoring**: Запросите у ассистента анализ конкретного файла: "Проанализируй сложность кода в `src/server.py`".
2. **Refactor MCP**: Попросите переименовать переменную во всех файлах проекта: "Замени `old_var` на `new_var` во всех `.py` файлах".
3. **Code Analyzer**: Запросите отчёт о зависимостях модулей: "Покажи граф импортов для модуля `src.llm`".
4. **Git MCP**: Узнайте статус репозитория: "Какие изменения ожидают коммита?".

## 📈 Ожидаемые улучшения

После подключения рекомендованных серверов вы получите:

- **Ускорение рефакторинга** — автоматические предложения и массовые замены.
- **Глубокий анализ кода** — выявление скрытых проблем и dead code.
- **Улучшенное управление версиями** — Git операции без переключения контекста.
- **Интеграция с Docker** — управление тестовыми окружениями.

## 🛠️ Устранение неполадок

### Сервер не запускается

- Убедитесь, что команда указана правильно и доступна в PATH.
- Проверьте, установлены ли все зависимости сервера.
- Посмотрите логи ошибок в консоли Cursor (Developer Tools).

### Инструменты не появляются в чате

- Перезапустите Cursor.
- Убедитесь, что MCP серверы активированы в настройках.
- Проверьте, поддерживает ли ваш план Cursor MCP (некоторые планы ограничивают количество серверов).

### Конфликты с существующими серверами

- Избегайте дублирования функциональности.
- При конфликте имён измените ключ в конфигурации.

## 🔗 Полезные ссылки

- [Официальный репозиторий MCP серверов](https://github.com/modelcontextprotocol/servers)
- [Документация по настройке MCP в Cursor](https://docs.cursor.com/context/model-context-protocol)
- [Каталог Awesome MCP Servers](https://mcpservers.org/)

---

**Обновлено:** 2026-02-05  
**Версия:** 1.0  
**Автор:** Code Agent Team