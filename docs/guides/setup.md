# Руководство по настройке Code Agent

## Быстрый старт

### 1. Установка зависимостей

```bash
# Использование pip
pip install crewai crewai-tools pyyaml python-dotenv

# Или с использованием uv (рекомендуется)
uv pip install crewai crewai-tools pyyaml python-dotenv
```

### 2. Настройка переменных окружения

**ВАЖНО: Разделение директорий**

Code Agent работает с двумя разными директориями:

1. **`codeAgent/`** - директория самого агента
   - Здесь находится код агента, конфигурация, документация
   - Здесь создается файл `.env` с переменными окружения
   - Пример: `D:\Space\codeAgent\`

2. **`${PROJECT_DIR}`** - целевой проект для работы агента
   - Это проект, над которым будет работать агент
   - Здесь находятся `docs/`, `todo.md` (или `todo.txt`, `todo.yaml`), `codeAgentProjectStatus.md`
   - Указывается в переменной `PROJECT_DIR` в `.env` файле
   - Пример: `D:\Space\your-project\`

**Создание `.env` файла:**

1. Скопируйте шаблон:
   ```bash
   cp .env.example .env
   ```

2. Отредактируйте `.env` в корне `codeAgent/`:
   ```env
   # Директория целевого проекта для работы агента
   # Это НЕ директория codeAgent, а проект, над которым будет работать агент
   PROJECT_DIR=D:\Space\your-project
   
   # API ключи для LLM (выберите один из провайдеров)
   OPENROUTER_API_KEY=your_openrouter_api_key_here
   # или
   OPENAI_API_KEY=your_openai_api_key_here
   # или
   ANTHROPIC_API_KEY=your_anthropic_api_key_here
   ```

**Пример структуры:**
```
D:\Space\
├── codeAgent\              ← Директория агента (здесь .env)
│   ├── .env               ← Файл с PROJECT_DIR
│   ├── config\
│   ├── src\
│   └── docs\
└── your-project\            ← Целевой проект (PROJECT_DIR)
    ├── docs\              ← Документация проекта
    ├── todo.txt           ← Список задач
    └── codeAgentProjectStatus.md  ← Статусы (создается агентом)
```

### 3. Настройка конфигурации

Отредактируйте `config/config.yaml` при необходимости:

```yaml
project:
  # base_dir использует переменную PROJECT_DIR из .env файла
  # PROJECT_DIR должен быть установлен в .env в корне codeAgent/
  base_dir: ${PROJECT_DIR}  # Берется из .env
  docs_dir: docs
  status_file: codeAgentProjectStatus.md

server:
  check_interval: 60  # Интервал проверки задач в секундах
  task_delay: 5       # Задержка между задачами в секундах
```

### 4. Подготовка целевого проекта

Убедитесь, что в директории целевого проекта (`PROJECT_DIR`, например `D:\Space\your-project\`) есть:

```
D:\Space\your-project\
├── docs/                    # Документация проекта
│   ├── README.md
│   └── ...
└── todo.md                  # Список задач (или todo.txt, todo.yaml)
```

#### Формат todo-листа

**Рекомендуемый формат: Markdown с чекбоксами (`todo.md`)** - формат по умолчанию:

```markdown
# Текущие задачи проекта

## Высокий приоритет
- [ ] Инициализировать проект
- [ ] Настроить окружение разработки
- [ ] Создать базовую структуру
- [ ] Написать документацию
```

**Альтернативные форматы:**

Текстовый формат (`todo.txt`):
```
1. Инициализировать проект
2. Настроить окружение разработки
3. Создать базовую структуру
4. Написать документацию
```

YAML формат (`todo.yaml`):
```yaml
tasks:
  - text: "Инициализировать проект"
    done: false
  - text: "Настроить окружение разработки"
    done: false
```

Формат можно изменить в `config/config.yaml`:
```yaml
project:
  todo_format: md  # или txt, yaml
```

Или с отступами для иерархии:

```
1. Основная задача
  1.1. Подзадача 1
  1.2. Подзадача 2
2. Следующая задача
```

### 5. Запуск агента

```bash
# Из корня проекта codeAgent (рекомендуется)
python main.py

# Или напрямую через модуль
python -m src.server
```

**Особенности запуска:**
- Автоматически проверяет и освобождает порт 3456
- Завершает старый процесс, если порт занят
- Запускает HTTP сервер на порту 3456 для мониторинга
- Поддерживает автоперезапуск при изменении .py файлов

**HTTP API доступен по адресу:** `http://127.0.0.1:3456`

## Подробная настройка

### Структура конфигурационного файла

`config/config.yaml` содержит следующие секции:

#### project

- `base_dir` - Базовая директория проекта (из .env)
- `docs_dir` - Директория документации относительно base_dir
- `status_file` - Имя файла статусов
- `todo_format` - Формат todo-листа (txt, yaml, md)

#### agent

- `role` - Роль агента в CrewAI
- `goal` - Цель агента
- `backstory` - История/контекст агента
- `allow_code_execution` - Разрешить выполнение кода
- `verbose` - Подробный вывод

#### server

- `check_interval` - Интервал проверки задач (секунды)
- `task_delay` - Задержка между задачами (секунды)
- `max_iterations` - Максимальное количество итераций (null для бесконечного цикла)
- `http_enabled` - Включить HTTP сервер (по умолчанию: true)
- `http_port` - Порт для HTTP сервера (по умолчанию: 3456, всегда один и тот же)
- `auto_reload` - Включить автоперезапуск (по умолчанию: true)
- `reload_on_py_changes` - Перезапускать при изменении .py файлов (по умолчанию: true)

#### docs

- `supported_extensions` - Расширения файлов для чтения
- `max_file_size` - Максимальный размер файла (символы)

### Использование различных провайдеров LLM

⚠️ **Важно:** Code Agent использует **слабые языковые модели** для простых задач. См. [Ограничения LLM](../integration/llm_limitations.md)

**Рекомендуемые модели для Code Agent:**

**OpenAI (рекомендуется):**
```env
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-3.5-turbo  # ✅ Слабая модель для простых задач
# НЕ использовать GPT-4 - слишком мощная для Code Agent!
```

**Anthropic:**
```env
ANTHROPIC_API_KEY=sk-ant-...
# Используйте Claude Haiku - легкая модель
```

**Ollama (локальные модели):**
```env
OLLAMA_API_URL=http://localhost:11434
OLLAMA_MODEL=llama2  # ✅ Легкая модель (7B параметров)
# Или mistral - тоже легкая модель
```

**Почему слабые модели?**
- Code Agent выполняет только простые действия (парсинг, выбор шаблонов)
- Не требуется deep thinking или сложный анализ
- Экономия ресурсов (бесплатные/дешевые модели)
- Мощные модели используются в Cursor Agents, не в Code Agent

**Другие провайдеры:**
См. документацию CrewAI: https://docs.crewai.com/

### 🆓 Бесплатные онлайн API-провайдеры LLM

Для разработки и тестирования Code Agent можно использовать полностью бесплатные API-провайдеры, которые не требуют кредитной карты и предоставляют доступ к различным моделям.

#### 1. FreeFlow LLM — агрегированный бесплатный API

**Унифицированный бесплатный API**, который подключается к нескольким провайдерам и моделям полностью бесплатно (без кредитной карты).

**Характеристики:**
- Поддержка моделей: GPT-4o, Llama 3.3 70B, Google Gemini 2.5 Flash и др.
- Ротация по провайдерам при превышении лимитов
- До ~16 000 бесплатных запросов ежедневно (суммарно по провайдерам)
- OpenAI-style интерфейс (совместим с Python)
- Отлично подходит для мощной генерации и кода без оплаты

**Использование:**
```python
# Используется как единый API в Python
# Совместим с OpenAI-style интерфейсом
```

#### 2. OpenRouter — мульти-модельный бесплатный API-шлюз

**Единый API для 100+ моделей** от разных провайдеров (GPT, Claude, Mistral, LLaMA и др.).

**Характеристики:**
- Один API-ключ, одна точка входа
- Free-tier модели: Llama 3.1 8B Instruct, Mistral 7B Instruct, Gemma 2.5 9B
- Удобно для проектов без раздельных интеграций
- ⚠️ Бесплатные лимиты ограничены (дневные/минутные)

**Использование:**
```env
OPENROUTER_API_KEY=your_key_here
OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct:free
```

#### 3. Google AI Studio — официальный бесплатный доступ

**Официальный free-tier доступ** к моделям серии Gemini и Gemma.

**Характеристики:**
- Доступ к Gemma 3 (4B/12B/27B) на free-tier
- Официальные бесплатные квоты от Google
- Количество бесплатных запросов/токенов в день ограничено
- Требуется регистрация в Google AI Studio

**Использование:**
```env
GOOGLE_API_KEY=your_google_ai_key_here
```

#### 4. Puter.js — доступ к сотням моделей без API-ключа

**Библиотека/скрипт** для доступа к более 500+ моделей (OpenAI, Anthropic, Gemini, Grok и др.) без ключей.

**Характеристики:**
- Доступ через браузер и клиентский JS
- Не требует API-ключей и сервера
- ⚠️ Менее «чистый API для Python» - полезно для прототипов и экспериментов

**Ограничения:**
- Предназначен в первую очередь для JavaScript/браузерных приложений
- Не идеален для серверных Python-приложений

#### 5. Дополнительные бесплатные провайдеры

**Groq API:**
- Предоставляет бесплатные ключи с приличными квотами для LLaMA-серий моделей
- Очень быстрая инференс за счет специализированного железа

**NVIDIA NIM:**
- Бесплатные квоты для некоторых моделей
- Хорошая производительность для локальных и облачных сценариев

**Mistral Free Tiers:**
- Некоторые модели Mistral доступны бесплатно через различные агрегаторы

#### 📊 Таблица бесплатных моделей через API

| Модель | Доступ через | Примечания |
|--------|--------------|------------|
| **Llama 3.1 8B Instruct** | OpenRouter, FreeFlow | Бесплатная, пригодна для текста и кода |
| **Mistral 7B Instruct** | OpenRouter, FreeFlow | Оптимальный баланс скорости/качества |
| **Gemma 2.5 / Gemma 3** | Google AI Studio, OpenRouter | Официальные бесплатные квоты |
| **GPT-4o / GPT-OSSX** | FreeFlow (GitHub Models) | Доступно через 3-сторонние API |
| **Qwen (free tiers)** | OpenRouter | Некоторые варианты бесплатны |
| **DeepSeek R1 (preview)** | OpenRouter | Бесплатные тестовые варианты |

#### 🔍 Рекомендации по выбору бесплатного провайдера

**Для разработки Code Agent рекомендуется:**

1. **FreeFlow LLM** — лучший выбор для начала:
   - Максимальный лимит запросов (~16K/день)
   - Поддержка мощных моделей (GPT-4o, Llama 3.3 70B)
   - OpenAI-style интерфейс (легкая интеграция)
   - Ротация провайдеров автоматически

2. **OpenRouter** — если нужен единый API для множества моделей:
   - Один ключ для 100+ моделей
   - Удобно для экспериментов с разными моделями
   - Ограниченные free-tier лимиты

3. **Google AI Studio** — если работаете в экосистеме Google:
   - Официальная поддержка
   - Стабильные квоты
   - Хорошая интеграция с Google-инструментами

**Выводы:**
- ✅ Для Code Agent достаточно бесплатных провайдеров благодаря простоте задач (парсинг, выбор шаблонов)
- ✅ FreeFlow LLM и OpenRouter — наиболее универсальные решения с хорошими лимитами
- ✅ Не требуется кредитная карта — можно начать разработку сразу
- ✅ Возможна ротация между провайдерами при превышении лимитов
- ⚠️ Бесплатные лимиты могут быть ограничены для production-использования
- ⚠️ Некоторые провайдеры требуют регистрации (но бесплатно)

### Форматы todo-листа

#### Текстовый формат (todo.txt)

```
1. Первая задача
2. Вторая задача
  2.1. Подзадача
3. Третья задача
```

#### Markdown с чекбоксами (todo.md)

```markdown
- [ ] Первая задача
- [ ] Вторая задача
  - [ ] Подзадача 1
  - [ ] Подзадача 2
- [x] Выполненная задача
```

#### YAML формат (todo.yaml)

```yaml
tasks:
  - text: Первая задача
    done: false
    children:
      - text: Подзадача
        done: false
  - text: Вторая задача
    done: false
```

## Устранение неполадок

### Ошибка: "Директория проекта не найдена"

Проверьте:
1. Указан ли `PROJECT_DIR` в `.env`
2. Существует ли указанная директория
3. Правильно ли указан путь (абсолютный путь рекомендуется)

### Ошибка: "Файл todo не найден"

Убедитесь, что в директории проекта есть файл:
- `todo.txt`
- `TODO.txt`
- `todo.yaml`
- `TODO.md`
- `todo.md`

### Ошибка API ключа

Проверьте:
1. Указан ли API ключ в `.env`
2. Действителен ли API ключ
3. Правильно ли указан провайдер (OPENAI_API_KEY, ANTHROPIC_API_KEY и т.д.)

### Агент не выполняет задачи

Проверьте:
1. Логи в `logs/code_agent.log`
2. Файл статусов `codeAgentProjectStatus.md`
3. Настройки `allow_code_execution` в конфигурации

## Следующие шаги

После настройки:

1. **Протестируйте агента** на простых задачах
2. **Изучите файл статусов** `codeAgentProjectStatus.md` для отслеживания прогресса
3. **Настройте агента** под свои нужды через `config/config.yaml`
4. **Расширьте функциональность** добавив свои инструменты или агенты

## Дополнительные ресурсы

- [Документация CrewAI](https://docs.crewai.com/)
- [Архитектура Code Agent](../core/architecture.md)
- [API Reference](../core/api.md)
