# Инструкция по ревизии проекта Code Agent

**Цель:** Провести комплексную ревизию проекта, удалить артефакты, очистить документацию от выполненных задач и статусов, сжать содержимое для продолжения работы.

**Дата создания:** 2026-01-26  
**Версия:** 1.0

---

## 📋 Общая стратегия

Ревизия проекта включает:
1. **Ревизию кода** - проверка качества, удаление неиспользуемого кода
2. **Ревизию документации** - удаление устаревших отчетов, статусов, выполненных задач
3. **Очистку файлов** - удаление временных файлов, старых тестов, репортов
4. **Сжатие содержимого** - оптимизация документации, удаление дубликатов

---

## 🔍 ЭТАП 1: Ревизия кода

### 1.1 Проверка неиспользуемого кода

**Действия:**
```bash
# Найти неиспользуемые импорты
ruff check src/ --select F401

# Найти неиспользуемые функции и классы
vulture src/ --min-confidence 80

# Проверить дублирование кода
pylint --disable=all --enable=duplicate-code src/
```

**Что удалять:**
- ❌ Неиспользуемые импорты
- ❌ Закомментированный код (старше 30 дней)
- ❌ Дублирующиеся функции
- ❌ Мертвый код (unreachable code)

**Что оставлять:**
- ✅ Рабочий код, даже если временно не используется
- ✅ Код с TODO комментариями (если актуально)
- ✅ Заглушки для будущей функциональности

### 1.2 Проверка качества кода

**Действия:**
```bash
# Проверка линтером
ruff check src/ test/

# Проверка типов
mypy src/

# Проверка форматирования
black --check src/ test/
```

**Исправления:**
- ✅ Привести код к единому стилю
- ✅ Исправить предупреждения линтера
- ✅ Добавить недостающие type hints
- ✅ Улучшить обработку ошибок

### 1.3 Проверка тестов

**Действия:**
```bash
# Запустить все тесты
pytest test/ -v

# Проверить покрытие
pytest test/ --cov=src --cov-report=term-missing
```

**Что делать:**
- ✅ Удалить сломанные тесты (если не планируется исправление)
- ✅ Объединить дублирующиеся тесты
- ✅ Удалить тесты для удаленного функционала

---

## 📚 ЭТАП 2: Ревизия документации

### 2.1 Удаление устаревших отчетов

**Директории для проверки:**
- `docs/archive/reports/` - архивные отчеты
- `docs/testing/` - отчеты о тестировании
- `docs/solutions/` - отчеты о решениях
- `docs/root/` - отчеты в корне docs

**Критерии удаления:**
- ❌ Отчеты старше 3 месяцев (если не содержат важной информации)
- ❌ Дублирующиеся отчеты (оставить самый полный)
- ❌ Промежуточные отчеты (оставить финальные)
- ❌ Отчеты со статусом "✅ ЗАВЕРШЕНО" без актуальной информации

**Что оставлять:**
- ✅ Финальные отчеты о важных изменениях
- ✅ Отчеты с уникальной информацией
- ✅ Отчеты, на которые есть ссылки в других документах

**Действия:**
```bash
# Найти все отчеты
find docs/ -name "*REPORT*.md" -o -name "*SUMMARY*.md" -o -name "*STATUS*.md"

# Проверить размеры файлов
find docs/ -name "*.md" -exec wc -l {} \; | sort -rn | head -20
```

### 2.2 Удаление статусов и выполненных задач

**Файлы для проверки:**
- `docs/IMPROVEMENT_TODO.md` - список задач
- `docs/planning/*.md` - планы с выполненными задачами
- Все файлы с чеклистами и статусами

**Что удалять:**
- ❌ Секции с выполненными задачами (✅ ВЫПОЛНЕНО)
- ❌ Статусы "ЗАВЕРШЕНО", "ВЫПОЛНЕНО" в заголовках
- ❌ Устаревшие чеклисты
- ❌ Прогресс-бары и статистику выполнения

**Что оставлять:**
- ✅ Актуальные задачи (не выполненные)
- ✅ Важную историческую информацию
- ✅ Ссылки на реализованные функции

**Пример очистки:**
```markdown
# БЫЛО:
### Проблема #1: Отсутствие файла `.env.example` ✅ ВЫПОЛНЕНО
- [x] Проанализировать все места использования переменных окружения
- [x] Создать `.env.example` с шаблоном всех необходимых переменных
...

# СТАЛО:
### Проблема #1: Отсутствие файла `.env.example`
Решено: Создан `.env.example` с шаблоном всех необходимых переменных.
См. `.env.example` в корне проекта.
```

### 2.3 Удаление дублирующейся информации

**Что искать:**
- Дублирующиеся описания функций
- Повторяющиеся инструкции
- Копии одних и тех же примеров

**Действия:**
```bash
# Найти похожие файлы по размеру и содержимому
fdupes -r docs/

# Найти дублирующиеся секции в файлах
grep -r "## .*" docs/ | sort | uniq -d
```

**Что делать:**
- ✅ Объединить дубликаты в один файл
- ✅ Создать ссылки вместо копирования
- ✅ Удалить устаревшие версии

### 2.4 Оптимизация структуры документации

**Проверка структуры:**
```bash
# Проверить структуру docs/
tree docs/ -L 3 -I 'node_modules'

# Найти файлы без ссылок
find docs/ -name "*.md" -exec grep -L "\[.*\](" {} \;
```

**Оптимизация:**
- ✅ Переместить устаревшие документы в `docs/archive/`
- ✅ Объединить связанные документы
- ✅ Удалить пустые директории
- ✅ Обновить `docs/README.md` с актуальной структурой

---

## 🗑️ ЭТАП 3: Удаление лишних файлов

### 3.1 Удаление старых тестовых файлов

**Директории:**
- `test/` - проверить все тестовые файлы
- `test_cursor_cli/results/` - результаты тестов
- `results/` - результаты тестов в корне

**Критерии удаления:**
- ❌ Временные файлы результатов тестов (`.csv`, `.json` с результатами)
- ❌ Старые логи тестов (старше 1 месяца)
- ❌ Сломанные тесты без планов исправления

**Что оставлять:**
- ✅ Рабочие тесты
- ✅ Конфигурационные файлы тестов
- ✅ Важные результаты тестов (если нужны для анализа)

**Действия:**
```bash
# Найти временные файлы результатов
find test/ results/ -name "*.csv" -o -name "*_test.json" -o -name "*_results.json"

# Найти старые логи
find logs/ -name "*.log" -mtime +30
```

### 3.2 Удаление репортов и статусов

**Файлы для удаления:**
- `docs/testing/*REPORT*.md` - старые отчеты о тестировании
- `docs/testing/*STATUS*.md` - файлы со статусами
- `docs/archive/reports/*` - архивные отчеты (оставить только важные)

**Критерии:**
- ❌ Промежуточные отчеты (оставить финальные)
- ❌ Дублирующиеся отчеты
- ❌ Отчеты без актуальной информации

**Действия:**
```bash
# Найти все файлы с REPORT, STATUS, SUMMARY
find docs/ -type f \( -name "*REPORT*.md" -o -name "*STATUS*.md" -o -name "*SUMMARY*.md" \)

# Проверить размеры
find docs/ -type f -name "*REPORT*.md" -exec ls -lh {} \;
```

### 3.3 Очистка временных файлов

**Что удалять:**
- ❌ `*.pyc`, `__pycache__/` (уже в .gitignore, но проверить)
- ❌ `.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`
- ❌ Временные файлы в корне проекта
- ❌ Старые backup файлы (`.bak`, `.backup`)

**Действия:**
```bash
# Очистка кэшей Python
find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null
find . -type f -name "*.pyc" -delete 2>/dev/null
find . -type f -name "*.pyo" -delete 2>/dev/null

# Очистка кэшей инструментов
rm -rf .pytest_cache .mypy_cache .ruff_cache .coverage htmlcov/

# Удаление backup файлов
find . -type f \( -name "*.bak" -o -name "*.backup" \) -delete
```

---

## 📦 ЭТАП 4: Сжатие и оптимизация

### 4.1 Оптимизация документации

**Действия:**
1. **Удалить пустые секции:**
   ```bash
   # Найти файлы с пустыми секциями
   grep -r "^## .*$" docs/ | while read line; do
     file=$(echo "$line" | cut -d: -f1)
     section=$(echo "$line" | cut -d: -f2)
     # Проверить, есть ли содержимое после секции
   done
   ```

2. **Объединить короткие файлы:**
   - Файлы меньше 50 строк можно объединить с похожими
   - Создать индексные файлы вместо множества маленьких

3. **Удалить избыточные примеры:**
   - Оставить по 1-2 примера на функцию
   - Удалить устаревшие примеры

### 4.2 Обновление ссылок

**Проверка ссылок:**
```bash
# Использовать скрипт проверки ссылок
python scripts/check_doc_links.py

# Найти битые ссылки
grep -r "\[.*\](" docs/ | grep -v "http" | while read line; do
  # Проверить существование файла
done
```

**Исправления:**
- ✅ Обновить ссылки на удаленные файлы
- ✅ Исправить битые ссылки
- ✅ Добавить недостающие ссылки

### 4.3 Создание индексов

**Создать/обновить:**
- `docs/README.md` - главный индекс документации
- `docs/guides/README.md` - индекс руководств
- `docs/testing/README.md` - индекс тестирования

---

## ✅ ЧЕКЛИСТ РЕВИЗИИ

### Подготовка
- [ ] Создать backup проекта (git commit или архив)
- [ ] Проверить текущий статус git
- [ ] Убедиться, что нет незакоммиченных важных изменений

### Ревизия кода
- [ ] Удалить неиспользуемые импорты
- [ ] Удалить закомментированный код
- [ ] Исправить предупреждения линтера
- [ ] Обновить type hints
- [ ] Удалить сломанные тесты

### Ревизия документации
- [ ] Удалить устаревшие отчеты (старше 3 месяцев)
- [ ] Удалить статусы "✅ ВЫПОЛНЕНО" из заголовков
- [ ] Очистить выполненные задачи из чеклистов
- [ ] Удалить дублирующиеся документы
- [ ] Обновить структуру документации

### Удаление файлов
- [ ] Удалить временные файлы результатов тестов
- [ ] Удалить старые логи (старше 1 месяца)
- [ ] Удалить промежуточные отчеты
- [ ] Очистить кэши Python и инструментов
- [ ] Удалить backup файлы

### Оптимизация
- [ ] Объединить короткие документы
- [ ] Удалить пустые секции
- [ ] Обновить все ссылки
- [ ] Создать/обновить индексы документации
- [ ] Обновить `docs/README.md`

### Финальная проверка
- [ ] Запустить все тесты
- [ ] Проверить, что проект запускается
- [ ] Проверить документацию на читаемость
- [ ] Создать коммит с изменениями
- [ ] Обновить CHANGELOG.md

---

## 🎯 ПРИОРИТЕТЫ

### Высокий приоритет (сделать в первую очередь)
1. ✅ Удалить статусы "✅ ВЫПОЛНЕНО" из заголовков документов
2. ✅ Очистить `docs/IMPROVEMENT_TODO.md` от выполненных задач
3. ✅ Удалить промежуточные отчеты из `docs/testing/`
4. ✅ Удалить временные файлы результатов тестов

### Средний приоритет
5. ✅ Объединить дублирующиеся документы
6. ✅ Обновить структуру документации
7. ✅ Удалить устаревшие отчеты (старше 3 месяцев)
8. ✅ Очистить кэши и временные файлы

### Низкий приоритет (можно отложить)
9. ✅ Оптимизация структуры кода
10. ✅ Объединение коротких документов
11. ✅ Создание дополнительных индексов

---

## 📝 КОНКРЕТНЫЕ ДЕЙСТВИЯ

### Файлы для очистки статусов

**1. `docs/IMPROVEMENT_TODO.md`**
- Удалить все секции с "✅ ВЫПОЛНЕНО"
- Оставить только актуальные задачи
- Или полностью удалить, если все задачи выполнены

**2. `docs/planning/improvements_summary.md`**
- Удалить статусы "✅" из чеклистов
- Удалить секции "Выполнено"
- Оставить только актуальные рекомендации

**3. `docs/planning/implementation_roadmap.md`**
- Удалить "✅" из выполненных этапов
- Обновить статусы на актуальные
- Удалить устаревшие планы

**4. Все файлы в `docs/testing/`**
- Удалить промежуточные отчеты
- Оставить только финальные отчеты
- Удалить файлы со статусами

**5. Все файлы в `docs/archive/reports/`**
- Удалить дублирующиеся отчеты
- Удалить промежуточные отчеты
- Оставить только уникальные и важные

### Файлы для удаления

**Отчеты о тестировании (оставить только финальные):**
- `docs/testing/FULL_TEST_REPORT.md` (если есть FINAL версия)
- `docs/testing/COMPREHENSIVE_TEST_REPORT.md` (если устарел)
- `docs/testing/API_TESTS_FIXES.md` (если исправления применены)
- `docs/testing/FALLBACK_IMPLEMENTATION_SUMMARY.md` (если устарел)

**Статусы и промежуточные отчеты:**
- `docs/testing/API_KEY_STATUS.md` (если не актуален)
- `docs/testing/API_KEY_VERIFICATION.md` (если не актуален)
- Все файлы с паттерном `*STATUS*.md`

**Результаты тестов:**
- `results/*.csv` (временные результаты)
- `results/*.json` (временные результаты)
- `test_cursor_cli/results/*` (старые результаты)

---

## 🔧 СКРИПТЫ ДЛЯ АВТОМАТИЗАЦИИ

### Скрипт для поиска файлов со статусами

```python
#!/usr/bin/env python3
"""Найти все файлы с выполненными задачами и статусами"""
import re
from pathlib import Path

def find_status_files(docs_dir: Path):
    """Найти файлы со статусами выполнения"""
    patterns = [
        r'✅\s*ВЫПОЛНЕНО',
        r'✅\s*ЗАВЕРШЕНО',
        r'Статус.*✅',
        r'\[x\]',  # Выполненные чекбоксы
    ]
    
    results = []
    for md_file in docs_dir.rglob('*.md'):
        content = md_file.read_text(encoding='utf-8')
        for pattern in patterns:
            if re.search(pattern, content, re.IGNORECASE):
                results.append(md_file)
                break
    
    return results

if __name__ == '__main__':
    docs_dir = Path('docs')
    files = find_status_files(docs_dir)
    for f in files:
        print(f)
```

### Скрипт для удаления выполненных задач

```python
#!/usr/bin/env python3
"""Удалить выполненные задачи из markdown файлов"""
import re
from pathlib import Path

def clean_completed_tasks(file_path: Path):
    """Удалить выполненные задачи из файла"""
    content = file_path.read_text(encoding='utf-8')
    
    # Удалить секции с "✅ ВЫПОЛНЕНО" в заголовке
    content = re.sub(
        r'^### .*?✅\s*ВЫПОЛНЕНО.*?\n(?:.*?\n)*?(?=^### |^## |\Z)',
        '',
        content,
        flags=re.MULTILINE | re.DOTALL
    )
    
    # Удалить выполненные чекбоксы
    content = re.sub(r'^- \[x\].*?\n', '', content, flags=re.MULTILINE)
    
    file_path.write_text(content, encoding='utf-8')
    print(f"Очищен: {file_path}")

if __name__ == '__main__':
    # Использовать осторожно! Сначала сделать backup
    target_file = Path('docs/IMPROVEMENT_TODO.md')
    if target_file.exists():
        clean_completed_tasks(target_file)
```

---

## ⚠️ ВАЖНЫЕ ПРЕДУПРЕЖДЕНИЯ

1. **Всегда делайте backup перед массовым удалением:**
   ```bash
   git add -A
   git commit -m "Backup before revision"
   ```

2. **Проверяйте ссылки перед удалением файлов:**
   ```bash
   # Найти все ссылки на файл
   grep -r "filename" docs/
   ```

3. **Не удаляйте файлы, на которые есть ссылки в коде:**
   ```bash
   # Проверить использование в коде
   grep -r "filename" src/
   ```

4. **Сохраняйте важную историческую информацию:**
   - Перемещайте в `docs/archive/` вместо удаления
   - Создавайте краткие резюме вместо полных отчетов

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

После ревизии:
- ✅ Документация без устаревших статусов
- ✅ Нет дублирующихся отчетов
- ✅ Чистый код без неиспользуемых импортов
- ✅ Оптимизированная структура проекта
- ✅ Актуальные ссылки в документации
- ✅ Уменьшение размера проекта на 20-30%

---

## 🚀 БЫСТРЫЙ СТАРТ (Минимальная ревизия за 30 минут)

Если нужно быстро причесать проект:

### Шаг 1: Backup (2 минуты)
```bash
git add -A
git commit -m "Backup before revision"
```

### Шаг 2: Удалить статусы из документации (10 минут)
```bash
# Найти файлы со статусами
grep -r "✅ ВЫПОЛНЕНО" docs/ | cut -d: -f1 | sort -u
grep -r "✅ ЗАВЕРШЕНО" docs/ | cut -d: -f1 | sort -u

# Вручную удалить статусы из заголовков в найденных файлах
```

### Шаг 3: Очистить IMPROVEMENT_TODO.md (5 минут)
- Открыть `docs/IMPROVEMENT_TODO.md`
- Удалить все секции с "✅ ВЫПОЛНЕНО"
- Оставить только актуальные задачи или удалить файл полностью

### Шаг 4: Удалить временные файлы (5 минут)
```bash
# Удалить результаты тестов
rm -f results/*.csv results/*.json
rm -rf test_cursor_cli/results/*

# Очистить кэши
find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null
rm -rf .pytest_cache .mypy_cache .ruff_cache
```

### Шаг 5: Удалить промежуточные отчеты (8 минут)
```bash
# Просмотреть и удалить устаревшие отчеты
ls -lh docs/testing/*REPORT*.md
ls -lh docs/testing/*STATUS*.md

# Удалить выбранные файлы вручную
```

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

После завершения ревизии:
1. Обновить `CHANGELOG.md` с описанием ревизии
2. Создать коммит с понятным сообщением
3. Обновить `README.md` если нужно
4. Продолжить работу над проектом с чистой базой

---

## 📌 КОНКРЕТНЫЙ ПЛАН ДЕЙСТВИЙ ДЛЯ ЭТОГО ПРОЕКТА

### Файлы для немедленной очистки:

1. **`docs/IMPROVEMENT_TODO.md`**
   - Удалить все секции с "✅ ВЫПОЛНЕНО" (строки 11-195)
   - Оставить только раздел "Отложенные задачи" или удалить файл

2. **`docs/planning/improvements_summary.md`**
   - Удалить "✅" из всех чеклистов (строки 113-163)
   - Удалить секции "Выполнено"
   - Оставить только рекомендации

3. **`docs/planning/implementation_roadmap.md`**
   - Удалить "✅" из выполненных этапов
   - Обновить статусы

4. **`docs/testing/` - удалить промежуточные отчеты:**
   - `FULL_TEST_REPORT.md` (если есть FINAL версия)
   - `COMPREHENSIVE_TEST_REPORT.md` (если устарел)
   - `API_TESTS_FIXES.md` (если исправления применены)
   - `FALLBACK_IMPLEMENTATION_SUMMARY.md` (если устарел)

5. **`docs/archive/reports/` - удалить дубликаты:**
   - Просмотреть все файлы
   - Удалить промежуточные версии (оставить FINAL)
   - Удалить дублирующиеся отчеты

6. **`results/` - удалить временные файлы:**
   - `flags_test.csv`
   - `models_simple.csv`
   - `summary.json`

### Команды для быстрого выполнения:

```bash
# 1. Backup
git add -A && git commit -m "Backup before revision"

# 2. Найти все файлы со статусами
grep -r "✅ ВЫПОЛНЕНО\|✅ ЗАВЕРШЕНО" docs/ --include="*.md" | cut -d: -f1 | sort -u > /tmp/files_with_status.txt

# 3. Удалить временные результаты
rm -f results/*.csv results/*.json

# 4. Очистить кэши
make clean

# 5. Проверить размер документации
du -sh docs/
find docs/ -name "*.md" | wc -l
```

---

**Последнее обновление:** 2026-01-26
