# Changelog

Все значимые изменения в проекте Code Agent будут документироваться в этом файле.

## [Unreleased]

### Добавлено

- 📝 **Инструкции для AI ассистента (2026-01-21)**
  - Добавлена политика запрета автоматических git коммитов
  - AI должен запрашивать подтверждение перед изменениями
  - Коммиты только по явному запросу пользователя

### Исправлено

- 🐛 **Исправление CancelledError при остановке сервера (2026-01-21)**
  - Добавлена правильная обработка asyncio.CancelledError в методах shutdown
  - Предотвращение двойного закрытия LLM manager при graceful shutdown
  - Изоляция основного цикла сервера в отдельный метод для лучшей обработки ошибок
  - Добавлена обработка CancelledError в main.py
  - Предотвращение проброса CancelledError из server.start()
  - Сервер теперь корректно завершается после успешного выполнения задач без CancelledError

- 🐛 **Исправление ошибок проверки репорта LLM Manager (2026-01-21)**
  - Добавлена комплексная обработка ошибок в `_check_report_and_decide_next_action`
  - Обработка ошибок чтения файлов репорта (IOError, OSError, UnicodeDecodeError)
  - Обработка ошибок инициализации LLM Manager
  - Обработка ошибок анализа через LLM Manager
  - Гарантированное продолжение выполнения по плану при любых ошибках LLM
  - Предотвращение остановки сервера при проблемах с анализом репорта
  - Добавлен вывод ошибок в консоль для лучшей видимости проблем

### Добавлено

- ✅ **Улучшения LLMManager для гарантированного выполнения (2026-01-20)**
  - Периодическая проверка работоспособности моделей (health check) каждые 5 минут
  - Автоматическое отключение моделей с успешностью < 30%
  - Автоматическое включение обратно при успешности ≥ 70%
  - Blacklist для моделей, нарушающих JSON mode
  - Агрессивная стратегия восстановления при невалидном JSON:
    - Автоматическое переключение на best_of_two режим
    - Извлечение JSON из текстовых ответов через несколько методов
    - Retry механизм с разными моделями
  - Метод `_extract_json_from_text()` для агрессивного извлечения JSON

- ✅ **Улучшения проверки полезности задач (2026-01-20)**
  - Улучшен промпт проверки полезности с контекстом проекта Code Agent
  - Добавлены примеры валидных технических задач
  - Изменено дефолтное значение с 50% на 75% для технических задач
  - Защита от подозрительных оценок (0% для технических задач → автоматически 75%)
  - Использование best_of_two для проверки полезности вместо самой быстрой модели

- ✅ **Улучшения логирования (2026-01-20)**
  - Компактные цветные блоки для HTTP сервера и Cursor CLI
  - Корректное отображение времени выполнения инструкций (с учетом реального времени выполнения)
  - Разделение времени выполнения и ожидания файла в логах

### Изменено

- 🔧 **LLMManager** (`src/llm/llm_manager.py`)
  - Убран `raise RuntimeError` - теперь всегда возвращается ответ (даже дефолтный)
  - Улучшен fallback механизм - гарантирован ответ от последней модели или дефолтный
  - Добавлена агрессивная стратегия восстановления при невалидном JSON
  - Для JSON mode запросов автоматически используется best_of_two по умолчанию
  - Добавлена умная сортировка моделей для JSON mode (приоритет качественным моделям)

- 🔧 **Проверка полезности задач** (`src/server.py`)
  - Улучшен промпт с контекстом проекта и примерами технических задач
  - Используется best_of_two для критичных проверок
  - Улучшено извлечение JSON из текстовых ответов (несколько методов)
  - Дефолтное значение изменено с 50% на 75% для предотвращения ложного отбрасывания

- 🔧 **Логирование** (`src/server.py`, `src/task_logger.py`, `src/cursor_cli_interface.py`)
  - Объединены логи инициализации HTTP сервера в один цветной блок
  - Объединены логи выполнения через Cursor CLI в один цветной блок
  - Исправлено логирование времени выполнения инструкций
  - Детальная информация перенесена на уровень DEBUG

### Добавлено

- ✅ **HTTP сервер на фиксированном порту 3456**
  - Flask HTTP API для мониторинга и управления сервером
  - Эндпоинты: `/` (информация), `/status` (статус), `/health` (health check)
  - Автоматический запуск HTTP сервера в отдельном потоке
  - Настройки в `config/config.yaml`: `server.http_enabled`, `server.http_port`

- ✅ **Автоматическое управление портом**
  - Проверка занятости порта 3456 при запуске
  - Автоматическое завершение старого процесса, использующего порт
  - Поддержка Windows (netstat/taskkill) и Linux/Mac (lsof/kill)
  - Ожидание освобождения порта перед запуском нового сервера

- ✅ **Автоперезапуск при изменении .py файлов**
  - Отслеживание изменений в `src/` и корне проекта через watchdog
  - Автоматический перезапуск при изменении любых `.py` файлов
  - Игнорирование изменений в `__pycache__`, `.pyc` и тестовых директориях
  - Cooldown 5 секунд между перезапусками
  - Максимум 10 перезапусков подряд (защита от бесконечного цикла)
  - Настройки в `config/config.yaml`: `server.auto_reload`, `server.reload_on_py_changes`

- ✅ **Полный доступ для Cursor CLI без запросов разрешений**
  - Создан `.cursor/cli-config.json` с конфигурацией разрешений
  - Создан `.cursor/mcp-approvals.json` с предварительным одобрением MCP серверов
  - Добавлены флаги `--force` и `--approve-mcps` во все команды Cursor CLI
  - Обновлен `config/config.yaml` с настройками разрешений и автоматизации
  - Создана документация `docs/integration/full_access_setup.md`

### Изменено

- 🔧 **Точка входа сервера** (`main.py`)
  - Обновлен для поддержки проверки и освобождения порта
  - Добавлена логика перезапуска при изменении кода
  - Улучшена обработка ошибок и корректное завершение процессов

- 🔧 **Сервер Code Agent** (`src/server.py`)
  - Добавлен HTTP сервер на порту 3456 (Flask)
  - Добавлен file watcher для отслеживания изменений .py файлов
  - Добавлены методы для работы с портом: `_check_port_in_use()`, `_kill_process_on_port()`
  - Добавлены методы для HTTP сервера: `_setup_http_server()`
  - Добавлены методы для автоперезапуска: `_setup_file_watcher()`, `_check_reload_needed()`
  - Обновлен метод `start()` для поддержки HTTP сервера и file watcher

- 🔧 **Зависимости** (`pyproject.toml`)
  - Добавлен `flask>=2.3.0` для HTTP сервера
  - `watchdog` уже был в зависимостях, используется для автоперезапуска

- 🔧 **Конфигурация** (`config/config.yaml`)
  - Добавлены настройки HTTP сервера: `server.http_enabled`, `server.http_port`
  - Добавлены настройки автоперезапуска: `server.auto_reload`, `server.reload_on_py_changes`

- 🔧 **Cursor CLI интерфейс** (`src/cursor_cli_interface.py`)
  - Добавлены флаги `--force --approve-mcps` для Docker выполнения
  - Добавлены флаги `--force --approve-mcps` для WSL выполнения
  - Добавлены флаги `--force --approve-mcps` для локального выполнения
  - Все команды теперь выполняются без запросов разрешений

### Безопасность

- 🔒 **Настроены безопасные ограничения**
  - Запрещена запись в файлы с секретами (*.env, credentials*, secrets*)
  - Запрещены опасные команды (rm -rf /, dd, mkfs, format)
  - Разрешены только операции в рамках проекта (src/, docs/, test/, config/)
  - Настроен deny-список для критичных операций

## [0.3.0] - 2025-01-26

### Добавлено

- **LLM Manager система** (`src/llm/`)
  - `LLMManager`: Управление несколькими LLM моделями
    - Автоматический выбор самой быстрой модели
    - Fallback на резервные модели при ошибках
    - Параллельное выполнение через две модели с выбором лучшего ответа
    - Оценка ответов моделями
  - `LLMTestRunner`: Модуль тестирования моделей
    - Проверка доступности моделей
    - Измерение времени отклика
    - Экспорт результатов в Markdown
  - `CrewAILLMWrapper`: Интеграция LLMManager с CrewAI
    - Прозрачная интеграция с агентами CrewAI
    - Поддержка всех стратегий использования моделей
- **Конфигурация LLM** (`config/llm_settings.yaml`)
  - Настройка провайдеров (OpenRouter)
  - Определение ролей моделей (primary, duplicate, reserve, fallback)
  - Настройка стратегий использования
- **Документация**: `docs/llm_integration.md`
  - Описание архитектуры LLM системы
  - Примеры использования
  - Рекомендации по настройке

- **Этап 2: Cursor CLI интерфейс** (`src/cursor_cli_interface.py`)
  - Модуль для взаимодействия с Cursor через CLI
  - Автоматическая проверка доступности Cursor CLI в системе
  - Поддержка выполнения команд через `cursor-agent`
  - Обработка ошибок и таймауты для CLI команд
  - Работа в headless режиме
  - Fallback при недоступности CLI
  - Интеграция в `src/server.py` с проверкой доступности
  - Конфигурация CLI в `config/config.yaml`:
    - Путь к CLI (автопоиск в PATH если не указан)
    - Таймаут выполнения команд
    - Режим headless
    - Приоритет использования CLI

### Изменено

- **Executor Agent** (`src/agents/executor_agent.py`):
  - Добавлена поддержка LLMManager через параметр `use_llm_manager`
  - Добавлены параметры `llm_config_path` и `use_parallel` для настройки LLM
- **Config Loader** (`src/config_loader.py`):
  - Исправлена подстановка переменных окружения в LLM конфигурации
- Обновлен `src/server.py`:
  - Добавлена инициализация Cursor CLI интерфейса
  - Добавлен метод `execute_cursor_instruction()` для выполнения инструкций через CLI
  - Добавлен метод `is_cursor_cli_available()` для проверки доступности
  - Логирование статуса CLI при инициализации

- Обновлен `config/config.yaml`:
  - Добавлена секция `cursor.cli` с настройками CLI интерфейса

## [0.2.0] - 2025-01-26

### Добавлено

- Подробная документация по интеграции с Cursor (`docs/cursor_integration.md`)
  - Описание всех 9 инструкций для выполнения задач
  - Детальная схема взаимодействия с Cursor IDE
  - Формат протокола `codeAgentProjectStatus.md`
  - Конфигурация инструкций
  - Обработка ошибок
  
- Детальное описание рабочего процесса (`docs/workflow_detailed.md`)
  - Пошаговое описание всех этапов выполнения задачи
  - Примеры инструкций для Cursor
  - Формат записей в протоколе
  - Обработка ошибок и исключений
  - Расширенные возможности (иерархические планы, условное выполнение)

- Обновление архитектурной документации (`docs/architecture.md`)
  - Уточнено разделение ролей между Code Agent и Cursor
  - Описание процесса взаимодействия с Cursor
  - Обновлен рабочий цикл выполнения задач

- Обновление README (`README.md`)
  - Добавлены ссылки на новую документацию
  - Обновлено описание формата `codeAgentProjectStatus.md`
  - Уточнено описание возможностей агента

- Добавлен README в каталоге документации (`docs/README.md`)
  - Навигация по документации
  - Рекомендации по изучению
  - Быстрый старт

- Документация об ограничениях LLM (`docs/llm_limitations.md`)
  - ⚠️ Важно: Code Agent использует слабые языковые модели
  - Принцип работы: строго по инструкциям, без deep thinking
  - Рекомендуемые модели: GPT-3.5-turbo, Claude Haiku, локальные модели
  - Разделение ответственности: Code Agent (слабая LLM) vs Cursor Agents (мощные LLM)

- Документация о взаимодействии через репорты (`docs/cursor_communication.md`)
  - ⚠️ Важно: Механизм взаимодействия Code Agent и Cursor через файлы-репорты
  - Мощные модели Cursor: понимают контекст с полуслова, подробно протоколируют
  - Процесс ожидания: Code Agent должен правильно дожидаться выполнения работы Cursor
  - Контрольные фразы для синхронизации
  - Алгоритм ожидания и обработка ошибок
  - Конфигурация ожидания

### Изменено

- **Процесс работы с документацией**: Code Agent теперь только проверяет наличие файлов в `docs/`, не читает их полностью. Вся работа с документацией выполняется агентами Cursor.

- **Процесс выполнения задач**: Code Agent генерирует инструкции/команды для Cursor, а не код напрямую. Выполнение кода, тестирование, архитектурные задачи выполняются агентами Cursor.

- **Формат codeAgentProjectStatus.md**: Описан как последовательный протокол всех действий Code Agent и ответов агентов Cursor с детальной структурой записей.

### Уточнения по архитектуре

- Code Agent работает как **координатор задач**, а не исполнитель кода
- **LLM в Code Agent** (слабая модель):
  - Анализирует только задачи из todo-листа простыми действиями
  - Работает строго по предопределенным инструкциям из конфигурации
  - НЕ делает deep thinking или сложный анализ
  - Только парсинг задач, выбор шаблонов, простые проверки
- Выполнение кода происходит в Cursor через агентов, не через CodeInterpreterTool
- Взаимодействие с Cursor планируется через интерфейс (скриншоты, мышь, горячие клавиши)
- Каждый пункт todo выполняется через систему инструкций с файлами-репортами
- **Cursor использует очень умные модели последнего поколения** (GPT-4, Claude Opus):
  - Понимают контекст с полуслова
  - Могут подробно протоколировать работу
  - Code Agent должен правильно дожидаться выполнения через репорты

## [0.1.0] - 2025-01-XX

### Добавлено

- Базовая структура проекта Code Agent на базе CrewAI
- Основной сервер агента (`src/server.py`) с бесконечным циклом работы
- Менеджер конфигурации (`src/config_loader.py`) с поддержкой YAML и переменных окружения
- Менеджер статусов (`src/status_manager.py`) для работы с файлом статусов проекта
- Менеджер todo-листа (`src/todo_manager.py`) с поддержкой txt, yaml, md форматов
- Агент-исполнитель (`src/agents/executor_agent.py`) на базе CrewAI
- Конфигурационные файлы (`config/config.yaml`, `config/agents.yaml`)
- Документация:
  - README.md с описанием проекта и установкой
  - docs/architecture.md с описанием архитектуры
  - docs/setup.md с руководством по настройке
  - docs/api.md с API reference
- Файл зависимостей `pyproject.toml`
- `.gitignore` для Python проектов

### Возможности

- Работа агента в бесконечном цикле над проектами
- Чтение документации проекта из папки `docs`
- Чтение и выполнение задач из todo-листа
- Автоматическое обновление файла статусов `codeAgentProjectStatus.md`
- Поддержка различных форматов todo-листа (txt, yaml, md)
- Настройка через YAML конфигурацию и переменные окружения
- Интеграция с CrewAI для выполнения задач

### Решение по архитектуре

- Файл `codeAgentProjectStatus.md` хранится в корне директории проекта (например, `D:\Space\your-project\codeAgentProjectStatus.md`)
- Каждая задача выполняется в отдельной сессии агента
- Документация читается из папки `docs` в директории проекта
