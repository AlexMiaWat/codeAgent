# План выполнения задачи: Создать `src/core/` с разделением ответственности

**Задача:** Создать `src/core/` с разделением ответственности

**Дата создания:** 2026-01-23

**Сессия генерации:** 1769160441

**Статус:** Планирование

## 🎯 Цели и задачи

Создать модульную архитектуру `src/core/` с четким разделением ответственности для замены монолитного `server.py`. Основные компоненты должны быть независимыми, тестируемыми и расширяемыми.

### Основные требования

- **Модульность:** Каждый компонент отвечает за одну ответственность
- **Интерфейсы:** Все компоненты реализуют четкие интерфейсы
- **Dependency Injection:** Компоненты не создают зависимости самостоятельно
- **SOLID принципы:** Соблюдение принципов объектно-ориентированного дизайна
- **Тестируемость:** Каждый компонент можно тестировать изолированно
- **Производительность:** Не ухудшать производительность системы

## 📋 Анализ текущей структуры

### Существующая инфраструктура `src/core/`

1. **`types.py`** - Базовые типы данных и перечисления:
   - `ErrorSeverity`, `TaskStatus`, `ComponentStatus`
   - `ServerConfig`, `TaskResult`, `MetricsData`
   - `ErrorInfo`, `ComponentHealth`, `ServerState`

2. **`interfaces.py`** - Абстрактные интерфейсы:
   - `IConfigurable`, `IErrorHandler`, `IMetricsCollector`
   - `ITaskOrchestrator`, `IServerComponent`
   - `IHttpServer`, `IFileWatcher`

3. **`abstract_base.py`** - Базовые реализации:
   - `BaseComponent` - общая функциональность компонентов
   - `ConfigurableComponent` - компоненты с конфигурацией
   - `MetricsEnabledComponent` - компоненты с метриками

4. **`__init__.py`** - Экспорты базовых типов и интерфейсов

### Текущие проблемы

- Отсутствуют конкретные реализации компонентов
- Интерфейсы не реализованы
- Нет интеграции с остальной системой
- Отсутствуют тесты

## 🏗 План реализации

### Этап 1: Конфигурационный менеджмент (1 день)

1. **ConfigurationManager** (`src/core/configuration_manager.py`)
   - Наследование от `ConfigurableComponent`
   - Загрузка YAML конфигураций
   - Валидация и нормализация настроек
   - Кэширование конфигурации
   - Горячая перезагрузка

2. **Интеграция с существующей конфигурацией**
   - Поддержка `config/config.yaml`
   - Переменные окружения
   - Валидация через Pydantic

### Этап 2: Обработка ошибок (1 день)

3. **ErrorHandler** (`src/core/error_handler.py`)
   - Типизация ошибок: `CursorError`, `ConfigurationError`, `TaskError`
   - Recovery стратегии с exponential backoff
   - Структурированное логирование ошибок
   - История ошибок с анализом паттернов

### Этап 3: HTTP сервер (1-2 дня)

4. **HttpServer** (`src/core/http_server.py`)
   - Flask приложение с REST API
   - Endpoints: `/`, `/status`, `/health`, `/stop`, `/restart`
   - Middleware для логирования и метрик
   - Асинхронная обработка запросов

### Этап 4: Файловый мониторинг (1 день)

5. **FileWatcher** (`src/core/file_watcher.py`)
   - Интеграция с watchdog
   - Мониторинг изменений конфигурации
   - Автоматическая перезагрузка компонентов
   - Фильтры для игнорируемых файлов

### Этап 5: Метрики и телеметрия (1 день)

6. **MetricsCollector** (`src/core/metrics_collector.py`)
   - Сбор метрик производительности
   - Статистика выполнения задач
   - Экспорт в JSON/Prometheus формат
   - Агрегация и анализ трендов

### Этап 6: Оркестрация задач (2 дня)

7. **TaskOrchestrator** (`src/core/task_orchestrator.py`)
   - Управление жизненным циклом задач
   - Интеграция с CrewAI/Cursor
   - Параллельное выполнение задач
   - Отслеживание зависимостей между задачами

### Этап 7: Ядро сервера (2 дня)

8. **ServerCore** (`src/core/server_core.py`)
   - Основной цикл выполнения
   - Управление сессиями
   - Координация всех компонентов
   - Graceful shutdown

### Этап 8: Интеграция и тестирование (2-3 дня)

9. **Обновление `__init__.py`**
   - Экспорт всех новых компонентов
   - Backward compatibility

10. **Интеграционные тесты**
    - Тесты компонентов в изоляции
    - Тесты взаимодействия компонентов
    - Нагрузочное тестирование

## 📊 Метрики успеха

### Качественные метрики

- **Размер модулей:** Каждый компонент < 800 строк
- **Test coverage:** > 85% для новых компонентов
- **Cyclomatic complexity:** < 15 для методов
- **Maintainability index:** > 75

### Количественные метрики

- **Количество компонентов:** 7 основных компонентов
- **Количество интерфейсов:** 6 реализованных интерфейсов
- **Количество тестов:** > 50 unit + integration тестов
- **Производительность:** Не > 5% degradation

### Функциональные требования

- [ ] Все компоненты реализуют соответствующие интерфейсы
- [ ] Dependency injection работает корректно
- [ ] Компоненты могут запускаться независимо
- [ ] Горячая перезагрузка работает без сбоев
- [ ] Все endpoints HTTP API функционируют
- [ ] Метрики собираются и экспортируются
- [ ] Обработка ошибок типизирована и recovery работает

## 🚧 Риски и mitigation

### 1. Циклические зависимости
**Риск:** Компоненты могут создать циклические импорты
**Mitigation:**
- Строгое разделение интерфейсов и реализаций
- Использование dependency injection контейнера
- Регулярный анализ зависимостей

### 2. Регрессии функциональности
**Риск:** При декомпозиции может потеряться функциональность
**Mitigation:**
- Комплексное тестирование на каждом этапе
- Постепенная миграция с параллельным запуском старой версии
- Детальное логирование всех операций

### 3. Производительность
**Риск:** Дополнительные абстракции могут замедлить систему
**Mitigation:**
- Профилирование на каждом этапе
- Оптимизация критических путей
- Кэширование и lazy loading

### 4. Сложность тестирования
**Риск:** Сложно тестировать взаимодействие компонентов
**Mitigation:**
- Mock объекты для внешних зависимостей
- Интеграционные тесты с реальными компонентами
- Test doubles для сложных зависимостей

## 📅 Timeline

- **День 1:** ConfigurationManager + ErrorHandler
- **День 2-3:** HttpServer + FileWatcher
- **День 4:** MetricsCollector
- **День 5-6:** TaskOrchestrator
- **День 7-8:** ServerCore
- **День 9-10:** Интеграция, тестирование, документация

## ✅ Критерии завершения

### Технические критерии
- [ ] Все компоненты созданы и реализуют интерфейсы
- [ ] Код проходит статический анализ (mypy, flake8)
- [ ] Все тесты проходят с coverage > 85%
- [ ] Производительность не ухудшилась > 5%
- [ ] Документация API обновлена

### Функциональные критерии
- [ ] Сервер может запуститься с новыми компонентами
- [ ] HTTP API работает корректно
- [ ] Файловый мониторинг реагирует на изменения
- [ ] Метрики собираются и экспортируются
- [ ] Обработка ошибок работает с recovery

### Качественные критерии
- [ ] Код читаемый и хорошо документированный
- [ ] SOLID принципы соблюдены
- [ ] Компоненты слабо связаны
- [ ] Легко добавлять новые компоненты

## 🔗 Связанные документы

- **Архитектура:** `docs/core/architecture.md`
- **Структура проекта:** `docs/core/project_structure.md`
- **Предыдущий план:** `docs/results/current_plan_task_1769159383.md`
- **Текущие задачи:** `todo/CURRENT.md`