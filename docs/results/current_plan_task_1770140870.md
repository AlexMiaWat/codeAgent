# Task: Создать `src/core/` с разделением ответственности

## Plan:

1.  **Explore existing `src` directory:** Understand current structure.
2.  **Create `src/core` directory:** Establish the main directory for core logic.
3.  **Identify core concerns:** Determine what functionalities belong in `core` and how to separate them (e.g., configuration, utilities, common models, etc.).
4.  **Create subdirectories within `src/core`:** Based on identified concerns, create subdirectories like `config`, `utils`, `models`, `exceptions`, etc.
5.  **Move or create initial files:** Populate these subdirectories with initial placeholder files or move existing relevant files into them.
6.  **Update imports (if necessary):** If files are moved, adjust import paths in other parts of the codebase.
