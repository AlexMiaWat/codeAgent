# Задача #1: Декомпозиция монолитного server.py
**Идентификатор:** task_1_20260122_224954
**Приоритет:** 🔥 Критический
**Сессия:** 20260122_224954

## 1. Техническое описание задачи

### Текущая ситуация
Файл `src/server.py` представляет собой монолитный класс `CodeAgentServer` объемом 4500+ строк кода, содержащий всю бизнес-логику системы:

- **Основной цикл выполнения** (run_iteration, start)
- **Управление задачами** (execute_task, _execute_task_via_cursor, _execute_task_via_crewai)
- **Конфигурирование** (_validate_config, _check_recovery_needed)
- **Обработка ошибок** (_handle_cursor_error, _restart_cursor_environment)
- **HTTP сервер** (_setup_http_server)
- **Мониторинг файловой системы** (_setup_file_watcher)
- **Логирование и телеметрия** (интегрировано в методы)
- **Валидация результатов** (_verify_real_work_done, _check_task_usefulness)

### Проблемы текущей архитектуры
1. **Нарушение принципа единственной ответственности** - один класс отвечает за все аспекты работы
2. **Сложность тестирования** - невозможно изолированно тестировать отдельные компоненты
3. **Трудность сопровождения** - изменения в одном компоненте могут повлиять на другие
4. **Отсутствие переиспользования** - компоненты невозможно использовать в других контекстах
5. **Сложность масштабирования** - добавление новых функций требует модификации монолита

### Цель декомпозиции
Создать модульную архитектуру с четким разделением ответственности, где каждый компонент отвечает за конкретную функциональность и может быть:
- Независимо протестирован
- Легко модифицирован
- Переиспользован в других модулях
- Заменен альтернативной реализацией

## 2. Архитектурные решения и обоснование

### 2.1 Общая архитектура после декомпозиции

```
src/core/
├── __init__.py
├── server_core.py          # ServerCore - базовый цикл выполнения
├── task_orchestrator.py    # TaskOrchestrator - координация задач
├── configuration_manager.py # ConfigurationManager - унифицированное конфигурирование
├── error_handler.py        # ErrorHandler - типизированные ошибки и recovery
├── metrics_collector.py    # MetricsCollector - телеметрия производительности
└── interfaces.py           # Абстрактные интерфейсы (IServer, ITaskManager, etc.)
```

### 2.2 Компоненты и их ответственность

#### ServerCore (`server_core.py`)
**Ответственность:** Управление жизненным циклом сервера и основным циклом выполнения

**Обоснование:** Выделение базовой логики сервера позволяет:
- Изолировать запуск/остановку от бизнес-логики
- Упростить тестирование жизненного цикла
- Обеспечить graceful shutdown
- Разделить инфраструктурные заботы от функциональных

**Интерфейс:**
```python
class ServerCore:
    def start(self) -> None:
        """Запуск сервера с graceful shutdown"""

    def stop(self) -> None:
        """Остановка сервера"""

    def run_iteration(self, iteration: int = 1) -> bool:
        """Выполнение одной итерации цикла"""

    def is_running(self) -> bool:
        """Проверка состояния сервера"""
```

#### TaskOrchestrator (`task_orchestrator.py`)
**Ответственность:** Координация выполнения задач и управление workflow

**Обоснование:** Централизация логики оркестрации позволяет:
- Единообразно управлять различными типами задач
- Реализовать сложные workflow (sequential, parallel, conditional)
- Отделить стратегию выполнения от конкретных реализаций
- Упростить добавление новых типов задач

**Интерфейс:**
```python
class TaskOrchestrator:
    def execute_task(self, task: TodoItem) -> TaskResult:
        """Выполнение задачи с выбором стратегии"""

    def validate_result(self, task: TodoItem, result: str) -> bool:
        """Валидация результатов выполнения"""

    def should_use_cursor(self, task: TodoItem) -> bool:
        """Определение необходимости использования Cursor"""
```

#### ConfigurationManager (`configuration_manager.py`)
**Ответственность:** Унифицированное управление конфигурацией всех компонентов

**Обоснование:** Централизация конфигурирования обеспечивает:
- Единообразную валидацию настроек
- Горячую перезагрузку конфигурации
- Типобезопасность настроек
- Разделение конфигурации по компонентам

**Интерфейс:**
```python
class ConfigurationManager:
    def load_config(self, path: Optional[str] = None) -> AppConfig:
        """Загрузка и валидация конфигурации"""

    def validate_config(self, config: dict) -> List[str]:
        """Валидация конфигурационных данных"""

    def reload_config(self) -> bool:
        """Горячая перезагрузка конфигурации"""
```

#### ErrorHandler (`error_handler.py`)
**Ответственность:** Типизированная обработка ошибок и стратегии восстановления

**Обоснование:** Специализированная обработка ошибок позволяет:
- Классифицировать ошибки по типам и severity
- Применять соответствующие стратегии recovery
- Логировать ошибки с контекстом
- Предоставлять meaningful сообщения пользователям

**Иерархия ошибок:**
```python
class CodeAgentError(Exception):
    """Базовое исключение Code Agent"""

class ConfigurationError(CodeAgentError):
    """Ошибки конфигурации"""

class TaskExecutionError(CodeAgentError):
    """Ошибки выполнения задач"""

class CursorError(TaskExecutionError):
    """Ошибки интеграции с Cursor"""
```

#### MetricsCollector (`metrics_collector.py`)
**Ответственность:** Сбор и анализ метрик производительности

**Обоснование:** Централизованная телеметрия обеспечивает:
- Мониторинг производительности компонентов
- Выявление узких мест
- Анализ эффективности алгоритмов
- Планирование capacity

**Метрики:**
- Время выполнения задач
- Успешность выполнения
- Использование ресурсов
- Частота ошибок
- Производительность LLM запросов

### 2.3 Паттерны проектирования

#### Dependency Injection
Использование IoC контейнера для управления зависимостями между компонентами.

**Преимущества:**
- Легкая замена реализаций в тестах
- Явное объявление зависимостей
- Упрощение конфигурирования

#### Strategy Pattern
Для различных режимов работы и стратегий выполнения задач.

**Применение:**
- Разные стратегии выполнения задач (Cursor, CrewAI, Hybrid)
- Разные режимы валидации результатов
- Разные стратегии обработки ошибок

#### Observer Pattern
Для реактивного обновления состояния и уведомлений.

**Применение:**
- Уведомление о изменении статуса задач
- Мониторинг изменений конфигурации
- События жизненного цикла сервера

## 3. План реализации (пошаговый)

### Этап 1: Создание базовой структуры (1-2 дня)
1. **Создать директорию `src/core/`**
2. **Определить абстрактные интерфейсы** в `interfaces.py`
3. **Создать базовые классы-заглушки** для всех компонентов
4. **Настроить базовую dependency injection структуру**

### Этап 2: ConfigurationManager (2-3 дня)
1. **Извлечь логику конфигурирования** из server.py
2. **Реализовать типизированную конфигурацию** с Pydantic
3. **Добавить валидацию и hot reload**
4. **Интегрировать с остальными компонентами**

### Этап 3: ErrorHandler (1-2 дня)
1. **Определить иерархию исключений**
2. **Извлечь логику обработки ошибок** из server.py
3. **Реализовать стратегии recovery**
4. **Добавить контекстное логирование**

### Этап 4: MetricsCollector (1-2 дня)
1. **Определить метрики и их сбор**
2. **Реализовать коллектор с async поддержкой**
3. **Добавить экспорт метрик (JSON/HTTP endpoint)**
4. **Интегрировать с существующими компонентами**

### Этап 5: TaskOrchestrator (3-4 дня)
1. **Извлечь логику управления задачами** из server.py
2. **Реализовать стратегии выполнения**
3. **Добавить валидацию результатов**
4. **Интегрировать с ErrorHandler и MetricsCollector**

### Этап 6: ServerCore (2-3 дня)
1. **Извлечь логику жизненного цикла** сервера
2. **Реализовать graceful shutdown**
3. **Интегрировать с TaskOrchestrator**
4. **Добавить HTTP endpoints для управления**

### Этап 7: Интеграция и рефакторинг (2-3 дня)
1. **Обновить main.py** для использования новой архитектуры
2. **Перенести оставшуюся логику** (HTTP server, file watcher)
3. **Обновить импорты и зависимости**
4. **Протестировать интеграцию**

### Этап 8: Тестирование и оптимизация (1-2 дня)
1. **Написать unit тесты** для всех компонентов
2. **Протестировать интеграцию** компонентов
3. **Оптимизировать производительность**
4. **Документировать API**

## 4. Потенциальные риски и их митигация

### Риск 1: Регрессии функциональности
**Вероятность:** Высокая
**Влияние:** Критическое - потеря существующей функциональности

**Митигация:**
- Создание comprehensive integration тестов перед началом рефакторинга
- Постепенная миграция с параллельным запуском старой и новой версий
- Детальное логирование всех операций для сравнения поведения

### Риск 2: Снижение производительности
**Вероятность:** Средняя
**Влияние:** Высокое - дополнительные накладные расходы на DI и интерфейсы

**Митигация:**
- Профилирование производительности на каждом этапе
- Оптимизация критических путей выполнения
- Использование async/await для I/O операций
- Кэширование часто используемых объектов

### Риск 3: Увеличение сложности сопровождения
**Вероятность:** Средняя
**Влияние:** Среднее - больше файлов и абстракций

**Митигация:**
- Детальная документация всех интерфейсов и контрактов
- Строгое следование принципам SOLID
- Code reviews с фокусом на архитектуру
- Автоматическая генерация документации из докстрингов

### Риск 4: Проблемы с обратной совместимостью
**Вероятность:** Низкая
**Влияние:** Высокое - нарушение работы существующих интеграций

**Митигация:**
- Сохранение публичных API без изменений
- Версионирование интерфейсов
- Graceful degradation для старых клиентов
- Детальное тестирование всех внешних интеграций

### Риск 5: Длительность рефакторинга
**Вероятность:** Высокая
**Влияние:** Среднее - задержка других задач

**Митигация:**
- Разбиение на небольшие, независимые этапы
- Возможность отката на любом этапе
- Параллельная разработка новых фич
- Регулярные релизы промежуточных версий

## 5. Критерии приемки

### Функциональные критерии
- [ ] Все существующие функции работают без изменений
- [ ] Производительность не ухудшилась более чем на 10%
- [ ] Все интеграционные тесты проходят
- [ ] HTTP API endpoints работают корректно

### Архитектурные критерии
- [ ] Каждый компонент имеет единственную ответственность
- [ ] Все компоненты могут быть протестированы изолированно
- [ ] Интерфейсы четко определены и документированы
- [ ] Dependency injection работает корректно

### Качественные критерии
- [ ] Код покрыт unit тестами (>80% coverage)
- [ ] Все компоненты имеют документацию
- [ ] Метрики производительности собираются корректно
- [ ] Обработка ошибок типизирована и логгируется

### Интеграционные критерии
- [ ] Существующие конфигурационные файлы работают без изменений
- [ ] Все внешние зависимости (Cursor, CrewAI) интегрированы
- [ ] Логирование и мониторинг работают корректно
- [ ] Автоматический recovery при ошибках функционирует

## 6. Связь с другими задачами и компонентами проекта

### Зависимости от других задач
- **Задача 2**: Внедрение dependency injection и SOLID принципов
  - Текущая задача создает фундамент для DI
  - DI будет внедрен в компоненты, созданные здесь

- **Задача 3**: Quality Gates framework
  - Использует MetricsCollector для сбора метрик качества
  - Интегрируется с ErrorHandler для обработки ошибок валидации

- **Задача 5**: Архитектурная переработка LLM Manager
  - TaskOrchestrator будет использовать новый LLM Manager
  - MetricsCollector будет мониторить LLM производительность

### Влияние на существующие компоненты
- **config_loader.py**: Будет заменен ConfigurationManager
- **task_logger.py**: Интегрируется с MetricsCollector
- **cursor_cli_interface.py**: Используется TaskOrchestrator
- **agents/executor_agent.py**: Интегрируется с новой архитектурой

### Будущие интеграции
- **MCP серверы** (Задача 7): Используют interfaces.py для стандартизации
- **Платформа плагинов** (Задача 8): Расширяет core компоненты
- **Мониторинг enterprise-grade** (Задача 11): Использует MetricsCollector
- **Безопасность enterprise** (Задача 13): Интегрируется с ErrorHandler

### План миграции
1. **Фаза 1**: Создание новых компонентов параллельно со старыми
2. **Фаза 2**: Постепенная миграция функций в новые компоненты
3. **Фаза 3**: Интеграция и тестирование
4. **Фаза 4**: Удаление старого кода и оптимизация

## 7. Ресурсы и временные оценки

### Требуемые ресурсы
- **Разработчик**: 1 senior Python разработчик с опытом архитектуры
- **Время**: 12-16 дней (с тестированием)
- **Инструменты**: pytest, mypy, coverage, profiling tools

### Этапы и сроки
- Этап 1: 1-2 дня
- Этап 2: 2-3 дня
- Этап 3: 1-2 дня
- Этап 4: 1-2 дня
- Этап 5: 3-4 дня
- Этап 6: 2-3 дня
- Этап 7: 2-3 дня
- Этап 8: 1-2 дня

### Контрольные точки
- **День 5**: Базовая структура создана, компоненты интегрированы
- **День 10**: Основная функциональность перенесена и протестирована
- **День 15**: Полная интеграция и оптимизация завершены

## 8. Мониторинг и метрики успеха

### Технические метрики
- **Производительность**: <10% degradation по сравнению с baseline
- **Покрытие тестами**: >80% для новых компонентов
- **Цикломатическая сложность**: <10 для каждого метода
- **Время сборки**: <5 мин для полного тестового набора

### Качественные метрики
- **Удовлетворенность**: Положительная обратная связь от команды
- **Сопровождаемость**: Время на исправление багов <2 часов
- **Расширяемость**: Добавление новой функции <4 часов

### Бизнес метрики
- **Стабильность**: <5% инцидентов после релиза
- **Время разработки**: Сокращение на 20% для новых фич
- **Качество кода**: Увеличение maintainability index

---

**Создатель:** Code Agent
**Дата создания:** 2026-01-23
**Статус:** Запланировано
**Приоритет:** Критический 🔥