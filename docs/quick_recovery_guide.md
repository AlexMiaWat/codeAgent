# Быстрое руководство по восстановлению после сбоя

## Что происходит при сбое?

Когда сервер Code Agent падает или перезапускается (из-за сбоя питания, ошибки, или ручного завершения процесса), система автоматически:

1. ✅ Сохраняет состояние всех задач
2. ✅ Запоминает текущую выполняемую задачу
3. ✅ Фиксирует количество попыток для каждой задачи
4. ✅ Создает резервную копию данных

## Автоматическое восстановление

При следующем запуске сервер **автоматически**:

1. 🔍 Обнаруживает некорректный останов
2. 📋 Показывает информацию о прерванных задачах
3. 🔄 Сбрасывает прерванную задачу для повторного выполнения
4. ▶️ Продолжает работу с последней точки

**Никаких действий от вас не требуется!**

## Пример вывода при восстановлении

```
================================================================================
ОБНАРУЖЕН НЕКОРРЕКТНЫЙ ОСТАНОВ СЕРВЕРА
================================================================================
Последний запуск: 2026-01-18T09:30:00
Сессия: 20260118_093000
Итераций выполнено: 15

Прерванная задача: Реализовать логирование
  - ID: task_1737187800
  - Попыток: 1
  - Начало: 2026-01-18T09:45:00

Незавершенных задач: 3
  - Добавить тесты (состояние: pending)
  - Обновить документацию (состояние: pending)
  - Написать README (состояние: pending)

✓ Прерванная задача сброшена для повторного выполнения
✓ Сервер продолжит работу с последней контрольной точки
================================================================================
```

## Гарантии безопасности

### ✅ Что гарантируется:

- **Нет потери данных**: Все выполненные задачи сохранены
- **Нет дублирования**: Завершенные задачи не выполняются повторно
- **Продолжение работы**: Сервер продолжает с момента остановки
- **Защита от ошибок**: Задачи с ошибками можно повторить

### ❌ Что НЕ сохраняется:

- Состояние выполнения внутри задачи (только начало/конец)
- Временные файлы, созданные во время выполнения
- Незакоммиченные изменения в Cursor

## Частые сценарии

### Сценарий 1: Сбой питания

```
Проблема: Компьютер выключился во время работы сервера
Решение: Просто запустите сервер снова
Результат: Работа продолжится с последней задачи
```

### Сценарий 2: Ошибка в коде

```
Проблема: Сервер упал из-за необработанной ошибки
Решение: Исправьте ошибку и запустите сервер
Результат: Прерванная задача будет выполнена заново
```

### Сценарий 3: Ручная остановка (Ctrl+C)

```
Проблема: Вы остановили сервер через Ctrl+C
Решение: Запустите сервер снова
Результат: Корректный останов, работа продолжится нормально
```

### Сценарий 4: Зависание задачи

```
Проблема: Задача выполняется слишком долго
Решение: Остановите сервер (Ctrl+C) и запустите снова
Результат: Задача будет выполнена с начала (попытка 2)
```

## Проверка состояния

### Через логи

Смотрите файл `logs/code_agent.log`:

```bash
tail -f logs/code_agent.log
```

### Через checkpoint файл

Файл `.codeagent_checkpoint.json` содержит полное состояние:

```bash
cat .codeagent_checkpoint.json | jq '.server_state'
```

### Через статус файл

Файл `codeAgentProjectStatus.md` содержит историю:

```bash
cat codeAgentProjectStatus.md
```

## Настройка повторных попыток

В `config/config.yaml`:

```yaml
server:
  checkpoint:
    # Максимальное количество попыток выполнения задачи
    max_task_attempts: 3
```

Если задача падает 3 раза подряд, она будет пропущена.

## Ручное вмешательство (редко нужно)

### Сброс checkpoint (начать с чистого листа)

```bash
rm .codeagent_checkpoint.json
rm .codeagent_checkpoint.json.backup
```

⚠️ **Внимание**: Это удалит историю выполнения, но не повлияет на завершенные задачи в проекте.

### Восстановление из backup

Если основной checkpoint поврежден:

```bash
cp .codeagent_checkpoint.json.backup .codeagent_checkpoint.json
```

Обычно это происходит автоматически.

### Просмотр незавершенных задач

```python
from src.checkpoint_manager import CheckpointManager
from pathlib import Path

checkpoint = CheckpointManager(Path.cwd())
incomplete = checkpoint.get_incomplete_tasks()

for task in incomplete:
    print(f"- {task['task_text']} (попыток: {task['attempts']})")
```

## Мониторинг

### Признаки проблем:

1. **Задача выполняется более 3 раз** - возможно, есть системная проблема
2. **Много задач в состоянии failed** - проверьте логи на ошибки
3. **Checkpoint файл растет** - включите автоочистку

### Здоровое состояние:

```json
{
  "server_state": {
    "clean_shutdown": true,
    "iteration_count": 42
  },
  "tasks": [...],
  "current_task": null
}
```

## FAQ

**Q: Что делать, если сервер постоянно падает на одной задаче?**

A: Задача будет повторена максимум 3 раза (по умолчанию), затем пропущена. Проверьте логи для диагностики проблемы.

**Q: Можно ли запустить несколько серверов одновременно?**

A: Нет, один checkpoint файл = один сервер. Для нескольких серверов используйте разные директории проектов.

**Q: Что если я удалю checkpoint файл?**

A: Сервер создаст новый при запуске. Завершенные задачи в проекте не пострадают, но история выполнения будет потеряна.

**Q: Как часто сохраняется checkpoint?**

A: После каждого изменения состояния задачи (начало, завершение, ошибка) и при остановке сервера.

**Q: Влияет ли checkpoint на производительность?**

A: Минимально. Операции записи оптимизированы и выполняются асинхронно.

## Поддержка

Если возникли проблемы с восстановлением:

1. Проверьте логи: `logs/code_agent.log`
2. Проверьте checkpoint: `.codeagent_checkpoint.json`
3. Проверьте backup: `.codeagent_checkpoint.json.backup`
4. Создайте issue с логами и описанием проблемы

## Заключение

Система восстановления работает **полностью автоматически**. В 99% случаев вам не нужно ничего делать - просто перезапустите сервер, и он продолжит работу с последней точки остановки.

**Главное правило**: Не паникуйте и просто запустите сервер снова! 🚀
