# План доработки проекта Code Agent

**Основан на:** `SKEPTIC_ANALYSIS_REPORT.md`  
**Дата создания:** 2026-01-26  
**Статус:** 🟡 В процессе планирования

---

## 📋 Обзор

Этот документ содержит структурированный план доработки проекта Code Agent на основе критического анализа, проведенного в `SKEPTIC_ANALYSIS_REPORT.md`.

**Всего проблем:** 16 (4 критические, 12 некритических)  
**Категории:** Конфигурация, Код, Документация, Безопасность

---

## 🎯 Приоритеты выполнения

### Высокий приоритет (критические проблемы)
- [ ] Проблема #1: Отсутствие `.env.example`
- [ ] Проблема #2: Несоответствие путей в конфигурации
- [ ] Проблема #3: Отсутствие валидации конфигурации при старте
- [ ] Проблема #4: Широкие except блоки без логирования

### Средний приоритет (некритические, но важные)
- [ ] Проблема #5: Дублирование кода в тестах
- [ ] Проблема #6: Несоответствие документации и кода
- [ ] Проблема #7: Отсутствие проверки версий зависимостей
- [ ] Проблема #8: Неполная обработка ошибок в `todo_manager.py`
- [ ] Проблема #12: Использование `eval` в коде
- [ ] Проблема #13: Отсутствие type hints
- [ ] Проблема #14: Магические числа в коде
- [ ] Проблема #15: Неоптимальная обработка больших файлов

### Низкий приоритет (улучшения качества)
- [ ] Проблема #9: Устаревшие ссылки в документации
- [ ] Проблема #10: Отсутствие диаграмм архитектуры
- [ ] Проблема #11: Неполное описание API endpoints
- [ ] Проблема #16: Потенциальная утечка API ключей в логах
- [ ] Проблема #17: Отсутствие проверки прав доступа к файлам
- [ ] Проблема #18: Небезопасная обработка путей пользователя
- [ ] Проблема #19: Отсутствие схемы валидации YAML
- [ ] Проблема #20: Несоответствие форматов TODO
- [ ] Проблема #21: Отсутствие конфигурации для тестов

---

## 📝 Детальный план доработки

### 🔴 Критические проблемы

#### Проблема #1: Отсутствие файла `.env.example`

**Файлы для изменения:**
- [ ] Создать `.env.example` в корне проекта

**Шаги выполнения:**
1. [ ] Проанализировать все места использования переменных окружения
2. [ ] Создать `.env.example` с шаблоном всех необходимых переменных
3. [ ] Добавить комментарии с описанием каждой переменной
4. [ ] Обновить `.gitignore` (если нужно)
5. [ ] Обновить `README.md` с инструкцией по использованию `.env.example`

**Оценка времени:** 30 минут  
**Сложность:** Низкая

**Шаблон `.env.example`:**
```bash
# Директория целевого проекта для работы агента
PROJECT_DIR=D:\Space\life

# API ключи
OPENROUTER_API_KEY=your_openrouter_api_key_here
CURSOR_API_KEY=your_cursor_api_key_here

# Дополнительные настройки (если есть)
# LOG_LEVEL=INFO
# MAX_FILE_SIZE=1000000
```

---

#### Проблема #2: Несоответствие путей в конфигурации

**Файлы для изменения:**
- [ ] `config/config.yaml`
- [ ] `docs/guides/API_KEYS_SETUP.md` (или аналогичный)
- [ ] `README.md`

**Шаги выполнения:**
1. [ ] Добавить комментарии в `config/config.yaml` с объяснением `PROJECT_DIR`
2. [ ] Обновить документацию с четким разделением:
   - `codeAgent/` - директория самого агента (где находится `.env`)
   - `${PROJECT_DIR}` - целевой проект для работы агента
3. [ ] Добавить примеры в документацию
4. [ ] Проверить все места, где упоминается `PROJECT_DIR`

**Оценка времени:** 1 час  
**Сложность:** Низкая

---

#### Проблема #3: Отсутствие валидации конфигурации при старте

**Файлы для изменения:**
- [ ] `src/server.py`
- [ ] `src/config_loader.py` (возможно)

**Шаги выполнения:**
1. [ ] Создать метод `_validate_config()` в `CodeAgentServer.__init__()`
2. [ ] Проверить наличие `project_dir` и его существование
3. [ ] Проверить обязательные переменные окружения
4. [ ] Проверить доступность конфигурационных файлов
5. [ ] Добавить понятные сообщения об ошибках
6. [ ] Написать тесты для валидации

**Оценка времени:** 2-3 часа  
**Сложность:** Средняя

**Пример кода:**
```python
def _validate_config(self):
    """Проверка обязательных параметров при инициализации"""
    errors = []
    
    # Проверка project_dir
    if not self.project_dir:
        errors.append("PROJECT_DIR не установлен в переменных окружения")
    elif not self.project_dir.exists():
        errors.append(f"Директория проекта не найдена: {self.project_dir}")
    elif not self.project_dir.is_dir():
        errors.append(f"Путь не является директорией: {self.project_dir}")
    
    # Проверка обязательных API ключей (если нужны)
    # if not os.getenv("OPENROUTER_API_KEY"):
    #     errors.append("OPENROUTER_API_KEY не установлен")
    
    if errors:
        error_msg = "Ошибки конфигурации:\n" + "\n".join(f"  - {e}" for e in errors)
        raise ValueError(error_msg)
```

---

#### Проблема #4: Широкие except блоки без логирования

**Файлы для изменения:**
- [ ] `main.py`
- [ ] `src/cursor_cli_interface.py`
- [ ] `src/server.py`
- [ ] Другие файлы с `except Exception`

**Шаги выполнения:**
1. [ ] Найти все места с `except Exception as e:`
2. [ ] Заменить `print()` на `logger.error()` с `exc_info=True`
3. [ ] Добавить контекстную информацию (где произошла ошибка, какие параметры)
4. [ ] Убедиться, что используется правильный logger
5. [ ] Протестировать обработку ошибок

**Оценка времени:** 3-4 часа  
**Сложность:** Средняя

**Пример улучшения:**
```python
# Было:
except Exception as e:
    print(f"Критическая ошибка: {e}")

# Стало:
except Exception as e:
    logger.error(
        "Критическая ошибка при выполнении задачи",
        exc_info=True,
        extra={
            "task": task_name,
            "params": task_params,
            "context": "execution"
        }
    )
    raise  # или обработать соответственно
```

---

### ⚠️ Некритические проблемы

#### Проблема #5: Дублирование кода в тестах

**Файлы для изменения:**
- [ ] `test/conftest.py`
- [ ] `test/test_server_sequential_startup.py`
- [ ] `test/test_server_startup_simple.py`
- [ ] `test/test_concurrent_server_startup.py`

**Шаги выполнения:**
1. [ ] Вынести проверку `.env` в фикстуру в `conftest.py`
2. [ ] Вынести другие дублирующиеся проверки в фикстуры
3. [ ] Обновить тесты для использования фикстур
4. [ ] Убедиться, что тесты все еще проходят

**Оценка времени:** 1-2 часа  
**Сложность:** Низкая

---

#### Проблема #6: Несоответствие документации и кода

**Файлы для изменения:**
- [ ] `README.md`
- [ ] `config/config.yaml`

**Шаги выполнения:**
1. [ ] Проверить все упоминания `todo.txt` в документации
2. [ ] Решить: обновить README или изменить дефолт в конфиге
3. [ ] Внести изменения
4. [ ] Проверить согласованность во всех документах

**Оценка времени:** 30 минут  
**Сложность:** Низкая

---

#### Проблема #7: Отсутствие проверки версий зависимостей

**Файлы для изменения:**
- [ ] `pyproject.toml`
- [ ] Возможно создать `requirements-lock.txt`

**Шаги выполнения:**
1. [ ] Проанализировать совместимость версий
2. [ ] Уточнить версии в `pyproject.toml` (указать более конкретные диапазоны)
3. [ ] Добавить комментарии о совместимости
4. [ ] Рассмотреть использование `requirements-lock.txt` для фиксации версий

**Оценка времени:** 1-2 часа  
**Сложность:** Средняя

---

#### Проблема #8: Неполная обработка ошибок в `todo_manager.py`

**Файлы для изменения:**
- [ ] `src/todo_manager.py`

**Шаги выполнения:**
1. [ ] Добавить `try-except` в метод `_load_todos()`
2. [ ] Добавить обработку ошибок в `_load_from_yaml()`, `_load_from_md()`, `_load_from_txt()`
3. [ ] Добавить логирование ошибок
4. [ ] Написать тесты для обработки ошибок

**Оценка времени:** 1 час  
**Сложность:** Низкая

---

#### Проблема #12: Использование `eval` в коде

**Файлы для изменения:**
- [ ] Проверить `docs/archive/reports/TEST_FINAL_RESULTS.md` (возможно устаревший)
- [ ] Найти все использования `eval` в коде

**Шаги выполнения:**
1. [ ] Найти все использования `eval` в проекте
2. [ ] Оценить необходимость использования `eval`
3. [ ] Заменить на безопасные альтернативы
4. [ ] Протестировать изменения

**Оценка времени:** 2-3 часа  
**Сложность:** Средняя-Высокая

---

#### Проблема #13: Отсутствие type hints

**Файлы для изменения:**
- [ ] `src/todo_manager.py`
- [ ] `src/config_loader.py`
- [ ] Другие файлы без полных type hints

**Шаги выполнения:**
1. [ ] Добавить type hints во все функции
2. [ ] Добавить type hints для возвращаемых значений
3. [ ] Проверить с помощью `mypy` или `pyright`
4. [ ] Исправить найденные ошибки типов

**Оценка времени:** 4-6 часов  
**Сложность:** Средняя

---

#### Проблема #14: Магические числа в коде

**Файлы для изменения:**
- [ ] `src/server.py`
- [ ] Другие файлы с магическими числами

**Шаги выполнения:**
1. [ ] Найти все магические числа (3, 1000000, таймауты и т.д.)
2. [ ] Вынести в константы класса или модуля
3. [ ] Добавить комментарии с объяснением
4. [ ] Рассмотреть возможность вынесения в конфиг

**Оценка времени:** 1-2 часа  
**Сложность:** Низкая

**Пример:**
```python
# Было:
_max_cursor_errors = 3

# Стало:
MAX_CURSOR_ERRORS = 3  # Максимальное количество ошибок Cursor перед остановкой
```

---

#### Проблема #15: Неоптимальная обработка больших файлов

**Файлы для изменения:**
- [ ] `src/cursor_file_interface.py` (или где читаются файлы)
- [ ] `src/todo_manager.py`

**Шаги выполнения:**
1. [ ] Добавить проверку размера файла перед чтением
2. [ ] Использовать `max_file_size` из конфига
3. [ ] Добавить понятные сообщения об ошибках для больших файлов
4. [ ] Написать тесты

**Оценка времени:** 1-2 часа  
**Сложность:** Низкая-Средняя

---

### 📚 Проблемы документации

#### Проблема #9: Устаревшие ссылки в документации

**Файлы для изменения:**
- [ ] Все файлы в `docs/`

**Шаги выполнения:**
1. [ ] Провести аудит всех ссылок в документации
2. [ ] Проверить существование всех упомянутых файлов
3. [ ] Обновить или удалить устаревшие ссылки
4. [ ] Создать скрипт для автоматической проверки ссылок (опционально)

**Оценка времени:** 2-3 часа  
**Сложность:** Низкая

---

#### Проблема #10: Отсутствие диаграмм архитектуры

**Файлы для изменения:**
- [ ] `docs/core/architecture.md`

**Шаги выполнения:**
1. [ ] Создать диаграмму потока данных (Mermaid или PlantUML)
2. [ ] Создать диаграмму взаимодействия компонентов
3. [ ] Создать диаграмму последовательности выполнения задач
4. [ ] Добавить диаграммы в `architecture.md`

**Оценка времени:** 3-4 часа  
**Сложность:** Средняя

---

#### Проблема #11: Неполное описание API endpoints

**Файлы для изменения:**
- [ ] `docs/core/api_endpoints.md`
- [ ] `src/server.py` (для справки)

**Шаги выполнения:**
1. [ ] Собрать все endpoints из `src/server.py`
2. [ ] Сравнить с документацией
3. [ ] Дополнить документацию недостающими endpoints
4. [ ] Добавить примеры запросов/ответов
5. [ ] Обновить при необходимости

**Оценка времени:** 2-3 часа  
**Сложность:** Низкая-Средняя

---

### 🔒 Проблемы безопасности

#### Проблема #16: Потенциальная утечка API ключей в логах

**Файлы для изменения:**
- [ ] Все файлы с логированием

**Шаги выполнения:**
1. [ ] Найти все места логирования, где могут быть API ключи
2. [ ] Добавить фильтрацию чувствительных данных
3. [ ] Создать функцию для санитизации логов
4. [ ] Протестировать, что ключи не логируются

**Оценка времени:** 2-3 часа  
**Сложность:** Средняя

**Пример:**
```python
def sanitize_for_logging(data: dict) -> dict:
    """Удаляет чувствительные данные из словаря для логирования"""
    sensitive_keys = ['api_key', 'password', 'token', 'secret']
    sanitized = data.copy()
    for key in sensitive_keys:
        if key in sanitized:
            sanitized[key] = '***REDACTED***'
    return sanitized
```

---

#### Проблема #17: Отсутствие проверки прав доступа к файлам

**Файлы для изменения:**
- [ ] `src/todo_manager.py`
- [ ] `src/status_manager.py`
- [ ] Другие файлы, работающие с файловой системой

**Шаги выполнения:**
1. [ ] Добавить проверку прав на чтение перед чтением файлов
2. [ ] Добавить проверку прав на запись перед записью файлов
3. [ ] Добавить понятные сообщения об ошибках
4. [ ] Написать тесты

**Оценка времени:** 1-2 часа  
**Сложность:** Низкая

---

#### Проблема #18: Небезопасная обработка путей пользователя

**Файлы для изменения:**
- [ ] `src/config_loader.py`

**Шаги выполнения:**
1. [ ] Добавить валидацию путей
2. [ ] Запретить `..` в путях
3. [ ] Ограничить абсолютные пути разрешенными директориями
4. [ ] Добавить нормализацию путей
5. [ ] Написать тесты для проверки безопасности

**Оценка времени:** 2-3 часа  
**Сложность:** Средняя

**Пример:**
```python
def validate_path(path: Path, allowed_base: Path) -> Path:
    """Валидация и нормализация пути"""
    # Нормализация
    normalized = path.resolve()
    
    # Проверка на path traversal
    if '..' in str(path):
        raise ValueError(f"Путь содержит '..': {path}")
    
    # Проверка, что путь внутри разрешенной директории
    try:
        normalized.relative_to(allowed_base.resolve())
    except ValueError:
        raise ValueError(f"Путь вне разрешенной директории: {path}")
    
    return normalized
```

---

### ⚙️ Проблемы конфигурации

#### Проблема #19: Отсутствие схемы валидации YAML

**Файлы для изменения:**
- [ ] `src/config_loader.py`
- [ ] Возможно создать `config/schema.yaml` или использовать Pydantic

**Шаги выполнения:**
1. [ ] Выбрать библиотеку для валидации (Pydantic, Cerberus, или jsonschema)
2. [ ] Создать схему валидации для `config.yaml`
3. [ ] Интегрировать валидацию в `config_loader.py`
4. [ ] Добавить понятные сообщения об ошибках валидации
5. [ ] Написать тесты

**Оценка времени:** 3-4 часа  
**Сложность:** Средняя

---

#### Проблема #20: Несоответствие форматов TODO

**Файлы для изменения:**
- [ ] `src/todo_manager.py`
- [ ] `config/config.yaml`

**Шаги выполнения:**
1. [ ] Улучшить логику определения формата в `todo_manager.py`
2. [ ] Сделать более строгую проверку формата
3. [ ] Добавить валидацию формата
4. [ ] Обновить документацию

**Оценка времени:** 1-2 часа  
**Сложность:** Низкая

---

#### Проблема #21: Отсутствие конфигурации для тестов

**Файлы для изменения:**
- [ ] Создать `config/test_config.yaml`
- [ ] Обновить тесты для использования тестовой конфигурации

**Шаги выполнения:**
1. [ ] Создать `config/test_config.yaml` с изолированными путями
2. [ ] Обновить `conftest.py` для использования тестовой конфигурации
3. [ ] Убедиться, что тесты не влияют на основную конфигурацию
4. [ ] Протестировать изменения

**Оценка времени:** 1-2 часа  
**Сложность:** Низкая

---

## 📊 Статистика выполнения

### По приоритетам

| Приоритет | Всего | Выполнено | В процессе | Осталось |
|-----------|-------|-----------|------------|----------|
| Высокий (критические) | 4 | 0 | 0 | 4 |
| Средний | 8 | 0 | 0 | 8 |
| Низкий | 9 | 0 | 0 | 9 |
| **Всего** | **21** | **0** | **0** | **21** |

### По категориям

| Категория | Всего | Выполнено | В процессе | Осталось |
|-----------|-------|-----------|------------|----------|
| Конфигурация | 5 | 0 | 0 | 5 |
| Код | 5 | 0 | 0 | 5 |
| Документация | 3 | 0 | 0 | 3 |
| Безопасность | 3 | 0 | 0 | 3 |
| Тесты | 1 | 0 | 0 | 1 |
| Другое | 4 | 0 | 0 | 4 |

### Оценка времени

- **Минимальное время:** ~30 часов
- **Реалистичное время:** ~40-50 часов
- **С учетом тестирования:** ~50-60 часов

---

## 🎯 Рекомендуемый порядок выполнения

### Фаза 1: Критические исправления (1-2 дня)
1. Проблема #1: `.env.example`
2. Проблема #2: Несоответствие путей
3. Проблема #3: Валидация конфигурации
4. Проблема #4: Улучшение логирования ошибок

### Фаза 2: Улучшение качества кода (3-5 дней)
5. Проблема #8: Обработка ошибок в `todo_manager`
6. Проблема #13: Type hints
7. Проблема #14: Магические числа
8. Проблема #15: Обработка больших файлов
9. Проблема #5: Рефакторинг тестов

### Фаза 3: Безопасность и конфигурация (2-3 дня)
10. Проблема #16: Утечка API ключей
11. Проблема #17: Проверка прав доступа
12. Проблема #18: Валидация путей
13. Проблема #19: Валидация YAML
14. Проблема #21: Тестовая конфигурация

### Фаза 4: Документация и улучшения (2-3 дня)
15. Проблема #6: Несоответствие документации
16. Проблема #9: Устаревшие ссылки
17. Проблема #10: Диаграммы архитектуры
18. Проблема #11: API endpoints
19. Проблема #7: Версии зависимостей
20. Проблема #12: Использование `eval`
21. Проблема #20: Форматы TODO

---

## 📝 Заметки

- После выполнения каждой проблемы, обновлять статус в этом документе
- Перед началом работы над проблемой, создать ветку в Git
- После завершения проблемы, создать коммит с понятным сообщением
- Тестировать изменения после каждого исправления

---

## 🔗 Связанные документы

- [`SKEPTIC_ANALYSIS_REPORT.md`](./SKEPTIC_ANALYSIS_REPORT.md) - Исходный отчет с проблемами
- [`README.md`](../../README.md) - Основная документация проекта
- [`docs/core/architecture.md`](../core/architecture.md) - Архитектура проекта

---

**Последнее обновление:** 2026-01-26  
**Версия плана:** 1.0
