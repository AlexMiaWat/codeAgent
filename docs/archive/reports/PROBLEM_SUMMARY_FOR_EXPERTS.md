# ПРОБЛЕМА: Cursor CLI agent завершается с кодом 137 в Docker

**Дата:** 2026-01-18  
**Приоритет:** Высокий  
**Статус:** Требуется помощь экспертов

---

## Краткое описание

Команда `agent -p "instruction"` через Docker завершается с кодом **137 (SIGKILL)** из-за ошибки библиотеки **Ink**, которая требует TTY для работы.

---

## Суть проблемы

### Ошибка

```
ERROR Raw mode is not supported on the current process.stdin, which Ink uses
       as input stream by default.
       Read about: https://github.com/vadimdemedes/ink/#israwmodesupported
```

### Причина

1. **Cursor CLI (`agent`)** использует библиотеку **Ink** для интерактивного ввода/вывода
2. **Ink требует TTY** для переключения `process.stdin` в "raw mode"
3. **Docker без TTY** (`docker exec -i` без `-t`) не предоставляет TTY
4. **Результат:** Ink не может инициализироваться → процесс завершается с SIGKILL (137)

### Почему это критично

- ❌ Нельзя использовать `agent -p` для автоматизации в CI/CD
- ❌ Нельзя использовать `agent -p` в Docker контейнерах без TTY
- ❌ Флаг `--print` (`-p`) не решает проблему (Ink все равно инициализируется)

---

## Технические детали

### Версии

- **Agent CLI:** `2026.01.17-d239e66`
- **Docker:** Последняя версия
- **ОС хоста:** Windows 10
- **Контейнер:** Ubuntu 22.04

### Команда

```bash
docker exec -i cursor-agent-life bash -c 'cd /workspace && /root/.local/bin/agent -p "instruction"'
# Exit code: 137
```

### Что работает

- ✅ `agent --version` → работает
- ✅ `agent --help` → работает
- ✅ Контейнер запущен и доступен

### Что не работает

- ❌ `agent -p "..."` → код 137 (SIGKILL)
- ❌ `agent ls` → код 137 + ошибка Ink
- ❌ `agent --resume` → код 137

---

## Возможные решения

### 1. Использовать TTY (не подходит для автоматизации)

```bash
docker exec -it cursor-agent-life bash -c 'cd /workspace && /root/.local/bin/agent -p "instruction"'
```

**Проблема:** Команда может "зависать" в ожидании интерактивного ввода.

### 2. Отключить Ink в non-interactive режиме

**Требуется:** Способ указать `agent`, что нужно работать в полностью non-interactive режиме без Ink.

**Вопрос:** Есть ли флаг или переменная окружения для отключения Ink?

### 3. Использовать Background Agents API

**Требуется:** Проверить доступность API для программного выполнения инструкций.

**Вопрос:** Есть ли REST API для отправки инструкций без CLI?

### 4. Обход через файловую систему

**Текущее решение:** Используем файловый интерфейс (создание файлов с инструкциями).

**Ограничение:** Требует дополнительного мониторинга и не идеально для автоматизации.

---

## Вопросы для экспертов

### 1. Cursor CLI и Docker

**Вопрос:** Поддерживается ли использование `agent -p` в Docker без TTY для автоматизации?

**Контекст:** Если нет, какие есть альтернативы для автоматического выполнения инструкций?

### 2. Ink и non-interactive режим

**Вопрос:** Как отключить Ink для non-interactive режима в `agent`?

**Контекст:** Есть ли флаг, переменная окружения или другой способ?

### 3. Известные проблемы

**Вопрос:** Есть ли известные проблемы или планы по исправлению этой проблемы?

**Контекст:** Версия `2026.01.17-d239e66`, проблема воспроизводится всегда.

### 4. Альтернативные решения

**Вопрос:** Какие есть альтернативные способы выполнить инструкцию через Cursor программно?

**Контекст:** Нужна полная автоматизация без человеческого вмешательства.

### 5. Roadmap

**Вопрос:** Планируется ли улучшение поддержки headless/non-interactive режима?

**Контекст:** Это критично для интеграции с CI/CD и автоматизацией.

---

## Дополнительная информация

### Ссылки

- **Ink Raw Mode Error:** https://github.com/vadimdemedes/ink/#israwmodesupported
- **Cursor CLI Docs:** https://cursor.com/docs/cli/overview
- **Подробный отчет:** `docs/problem_diagnosis_cursor_cli_137.md`

### Воспроизведение

```bash
# Минимальный пример для воспроизведения
docker exec -i cursor-agent-life bash -c 'cd /workspace && /root/.local/bin/agent -p "hello"'
echo "Exit code: $?"
# Результат: Exit code: 137
```

### Логи

Полные логи и подробности в файле `docs/problem_diagnosis_cursor_cli_137.md`.

---

## Контакты

Если требуется дополнительная информация или доступ к окружению, пожалуйста, укажите:
- Что именно нужно проверить
- Какие логи или файлы требуются
- Как можно воспроизвести проблему в другом окружении

---

**Спасибо за помощь!** 🙏
