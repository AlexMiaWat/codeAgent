# Текущее взаимодействие Code Agent с Cursor

## Текущее состояние (2026-01-26)

### ✅ Реализовано

#### 1. Cursor CLI интерфейс (`src/cursor_cli_interface.py`)

**Статус:** ✅ Реализован и интегрирован

**Возможности:**
- Автоматический поиск Cursor CLI в системном PATH (`cursor-agent`, `cursor`, `cursor-cli`)
- Выполнение инструкций через команду `cursor-agent -p <prompt>`
- Поддержка headless режима (`--headless`)
- Указание рабочей директории (`--cwd`)
- Таймауты выполнения команд
- Обработка ошибок и таймаутов

**Методы:**
- `execute(prompt, working_dir, timeout)` - базовое выполнение команды
- `execute_instruction(instruction, task_id, working_dir, timeout)` - выполнение инструкции для задачи
- `is_available()` - проверка доступности CLI
- `check_version()` - получение версии CLI

**Пример использования:**
```python
from src.cursor_cli_interface import CursorCLIInterface

cli = CursorCLIInterface()
if cli.is_available():
    result = cli.execute(
        prompt="Создай новый файл test.py",
        working_dir="/path/to/project",
        timeout=300
    )
    if result.success:
        print(result.stdout)
```

#### 2. Интеграция в CodeAgentServer (`src/server.py`)

**Статус:** ✅ Интегрировано при инициализации

**Реализация:**
- При создании `CodeAgentServer` автоматически инициализируется `CursorCLIInterface`
- Метод `_init_cursor_cli()` загружает настройки из `config.yaml`
- Метод `execute_cursor_instruction()` для выполнения инструкций
- Метод `is_cursor_cli_available()` для проверки доступности

**Настройки в `config.yaml`:**
```yaml
cursor:
  interface_type: cli  # "cli" или "file"
  cli:
    cli_path: null  # Путь к CLI (null = автопоиск)
    timeout: 300    # Таймаут выполнения (секунды)
    headless: true  # Headless режим
    prefer_cli: true  # Приоритет CLI над файловым интерфейсом
```

#### 3. Конфигурация инструкций (`config/config.yaml`)

**Статус:** ✅ Настроены шаблоны инструкций

**Доступные инструкции:**
1. Разработка новой функциональности
2. Рефакторинг кода
3. Тестирование
4. Обновление документации
5. Ревью Скептика
6. Исправление по ревью
7. Полное тестирование
8. Коммит и отправка

Каждая инструкция имеет:
- `template` - шаблон инструкции
- `for_cursor: true` - указание, что инструкция для Cursor
- `wait_for_file` - файл результата для ожидания
- `control_phrase` - контрольная фраза завершения
- `timeout` - таймаут выполнения

## Как это работает сейчас

### 1. Инициализация

```python
server = CodeAgentServer(config_path="config/config.yaml")
# Автоматически:
# - Загружает конфигурацию
# - Ищет Cursor CLI в PATH
# - Инициализирует CursorCLIInterface
# - Логирует статус доступности CLI
```

### 2. Проверка доступности

```python
if server.is_cursor_cli_available():
    print("Cursor CLI доступен")
else:
    print("Cursor CLI недоступен, будет использоваться fallback")
```

### 3. Выполнение инструкции

```python
result = server.execute_cursor_instruction(
    instruction="Создай новый API endpoint",
    task_id="task_001",
    timeout=600
)

if result["success"]:
    print("Инструкция выполнена успешно")
    print(result["stdout"])
else:
    print("Ошибка выполнения:", result["error_message"])
```

### 4. Формат команды Cursor CLI

При выполнении формируется команда:
```bash
cursor-agent -p "инструкция" --headless --cwd "/path/to/project"
```

## Что НЕ реализовано

### ❌ Интеграция в основной цикл выполнения задач

**Проблема:** Метод `execute_cursor_instruction()` реализован, но **НЕ используется** в основном цикле выполнения задач (`_execute_task()`).

**Текущее поведение:**
- `_execute_task()` создает задачу для CrewAI агента
- CrewAI агент выполняет задачу напрямую (через CodeInterpreterTool)
- Cursor CLI **НЕ вызывается** автоматически

**Что нужно:**
- Интеграция вызова `execute_cursor_instruction()` в `_execute_task()`
- Определение, когда использовать Cursor CLI вместо CrewAI
- Ожидание файла результата и контрольной фразы

### ❌ Файловый интерфейс (fallback)

**Статус:** ❌ Не реализован

**План (из документации):**
- Создание файла `cursor_commands/instruction_{task_id}.txt`
- Ожидание файла `cursor_results/result_{task_id}.txt`
- Парсинг контрольной фразы

**Реализация:** Отсутствует модуль `src/cursor_file_interface.py`

### ❌ Background Agents API

**Статус:** ❌ Не реализован

**План (из документации):**
- Использование официального API Cursor для репозиториев
- Асинхронное выполнение задач через API

## Текущий поток выполнения

```
1. CodeAgentServer.start()
   └─> Бесконечный цикл
       └─> todo_manager.get_pending_tasks()
           └─> Для каждой задачи:
               └─> _execute_task(todo_item)
                   └─> _create_task_for_agent()
                   └─> Crew(agents=[self.agent], tasks=[task])
                       └─> crew.kickoff()  ⚠️ НЕ использует Cursor CLI
```

## Рекомендуемый поток (для реализации)

```
1. CodeAgentServer.start()
   └─> Бесконечный цикл
       └─> todo_manager.get_pending_tasks()
           └─> Для каждой задачи:
               └─> _execute_task(todo_item)
                   ├─> Определить тип задачи
                   ├─> Если задача для Cursor:
                   │   └─> execute_cursor_instruction()
                   │       └─> cursor-agent -p "instruction"
                   │       └─> Ожидание файла результата
                   │       └─> Проверка контрольной фразы
                   └─> Иначе:
                       └─> CrewAI выполнение (текущее)
```

## Настройки конфигурации

### `config/config.yaml`

```yaml
cursor:
  # Тип интерфейса: "cli" или "file"
  interface_type: cli
  
  cli:
    # Путь к CLI (null = автопоиск в PATH)
    cli_path: null
    
    # Таймаут выполнения команд (секунды)
    timeout: 300
    
    # Использовать headless режим
    headless: true
    
    # Приоритет CLI над файловым интерфейсом
    prefer_cli: true
```

## Примеры инструкций

### Инструкция для разработки

```yaml
- instruction_id: 1
  name: "Разработка новой функциональности"
  for_cursor: true
  template: |
    Реализуй следующую функциональность:
    {task_description}
    
    1. Создай/обнови необходимые файлы
    2. Проверь соответствие архитектуре
    3. Отчет в docs/results/last_result.md
    4. В конце "Отчет завершен!"
  wait_for_file: "docs/results/last_result.md"
  control_phrase: "Отчет завершен!"
  timeout: 600
```

### Как это должно работать

```python
# 1. Code Agent определяет тип задачи
instruction_template = config.get_instruction_template(task_type)

# 2. Формирует инструкцию
instruction = instruction_template.format(task_description=task.text)

# 3. Выполняет через Cursor CLI
result = server.execute_cursor_instruction(
    instruction=instruction,
    task_id=task.id,
    timeout=600
)

# 4. Ждет файл результата
wait_for_file("docs/results/last_result.md", timeout=600)

# 5. Проверяет контрольную фразу
if file_contains("Отчет завершен!"):
    mark_task_done(task)
```

## Выводы

### ✅ Что работает

1. **Cursor CLI интерфейс** - полностью реализован
2. **Интеграция в сервер** - базовая интеграция есть
3. **Конфигурация** - настройки готовы

### ❌ Что не работает

1. **Автоматическое использование в цикле** - CLI не вызывается автоматически
2. **Определение типа задачи** - нет логики выбора Cursor vs CrewAI
3. **Ожидание файлов результатов** - нет реализации wait_for_file
4. **Файловый fallback** - не реализован

### 📋 Что нужно сделать

1. Интегрировать `execute_cursor_instruction()` в `_execute_task()`
2. Реализовать логику выбора интерфейса (Cursor CLI vs CrewAI)
3. Реализовать ожидание файлов результатов
4. Реализовать файловый интерфейс как fallback
