# Исследование Cursor CLI для автоматизации

**Дата создания:** 2026-01-23  
**Объединено из:** cursor_cli_automation_research.md, cursor_cli_research.md, cursor_cli_windows_solution.md

---

## 🔍 Исследование автоматизации через Cursor CLI

### Текущая ситуация

**Проблема:** `cursor-agent` команда НЕ найдена в системе.

**Что есть:**
- ✅ `cursor.CMD` / `cursor.exe` - это обычный редактор Cursor IDE
- ❌ `cursor-agent` - НЕ найден в PATH

**Вывод:** `cursor-agent` - это отдельная CLI утилита, которая должна быть установлена отдельно.

### Что нужно для автоматического выполнения

По документации Cursor, для автоматического выполнения нужна команда `cursor-agent` с параметрами:

```bash
cursor-agent -p --force "инструкция"
```

Где:
- `-p` / `--print` - non-interactive режим
- `--force` - автоматическое применение изменений
- `--mode=plan` или `--mode=ask` - режим работы

---

## 📋 Проблема с текущей реализацией

Текущая реализация использует `cursor.CMD -p`, но это **НЕ команда для автоматического выполнения инструкций**.

Из `cursor.CMD --help` видно, что это обычный редактор Cursor IDE с параметрами для открытия файлов/папок, но **НЕ для выполнения инструкций**.

### Вывод из `cursor.CMD --help`

```
Usage: cursor.exe [options][paths...]
Options:
  -d --diff <file> <file>                    Compare two files
  -m --merge <path1> <path2> <base> <result> Perform a three-way merge
  -a --add <folder>                          Add folder(s) to the last active window
  -g --goto <file:line[:character]>          Open a file at the path
  -n --new-window                            Force to open a new window
  -r --reuse-window                          Force to open a file or folder
  -w --wait                                  Wait for the files to be closed
  -h --help                                  Print usage
```

**Проблема:** Нет параметров для выполнения инструкций (`-p`, `--print`, `--force`).

### Что нужно найти

Для автоматического выполнения нужна команда **`cursor-agent`**, которая:

1. Поддерживает режимы `--mode=plan` и `--mode=ask`
2. Позволяет передавать инструкции через `-p` или stdin
3. Работает в headless режиме
4. Возвращает структурированные результаты

---

## 🪟 Решение для автоматизации Cursor на Windows

### Проблема

**Официальный `cursor-agent` НЕ поддерживает Windows напрямую:**
- Установщик `curl https://cursor.com/install -fsS | bash` работает только на Linux/macOS/WSL
- На Windows (Git Bash) получаем: `Unsupported operating system: MINGW64_NT-10.0-26200`

### Варианты решения для Windows

#### Вариант 1: WSL (Windows Subsystem for Linux) ✅ РЕКОМЕНДУЕТСЯ

Если WSL доступен:

```bash
# В WSL
curl https://cursor.com/install -fsS | bash
export PATH="$HOME/.local/bin:$PATH"
cursor-agent --version
```

**Преимущества:**
- Официальная поддержка Cursor CLI
- Полная автоматизация
- Все возможности `cursor-agent`

**Недостатки:**
- Требует установки WSL
- Проект должен быть доступен из WSL

#### Вариант 2: Файловый интерфейс (текущая реализация)

Использовать файловый интерфейс вместо CLI:

```python
# Создать файл с инструкцией
with open("cursor_commands/instruction.txt", "w") as f:
    f.write("инструкция")

# Ожидать результат
wait_for_file("cursor_results/result.txt")
```

**Преимущества:**
- Работает на всех платформах
- Не требует установки дополнительных инструментов

**Недостатки:**
- Менее надежный
- Требует ручного открытия Cursor
- Сложнее в автоматизации

#### Вариант 3: Docker с Linux

Запуск Cursor в Docker контейнере с Linux:

```dockerfile
FROM ubuntu:20.04
# Установка cursor-agent
RUN curl https://cursor.com/install -fsS | bash
```

**Преимущества:**
- Изолированная среда
- Стабильная работа

**Недостатки:**
- Сложная настройка
- Требует Docker
- Может быть медленнее

#### Вариант 4: GitHub Actions / CI

Использование официальных runners с Linux:

```yaml
- name: Install Cursor CLI
  run: |
    curl https://cursor.com/install -fsS | bash
    export PATH="$HOME/.local/bin:$PATH"
    cursor-agent --version
```

**Преимущества:**
- Автоматическая настройка
- Официальная поддержка

**Недостатки:**
- Только в CI/CD
- Не для локальной разработки

### Рекомендация

**Для Windows рекомендуется Вариант 1 (WSL)** как наиболее надежное решение для локальной разработки.

---

## 🛠️ Варианты решения для автоматизации

### Вариант 1: Установка/настройка cursor-agent

Нужно найти способ установки `cursor-agent`:

#### Через официальный установщик:
```bash
curl https://cursor.com/install -fsS | bash
```

#### Ручная установка:
- Скачать бинарный файл с GitHub releases
- Добавить в PATH

#### Проверка установки:
```bash
cursor-agent --version
cursor-agent --help
```

### Вариант 2: Использование Background Agents API

Cursor предоставляет API для фоновых агентов:

```javascript
// Через JavaScript API
const cursor = require('cursor-agent');
const result = await cursor.execute('инструкция');
```

### Вариант 3: Файловый интерфейс (fallback)

Если CLI недоступен, использовать файловый интерфейс:

1. Создать файл `cursor_commands/instruction_{task_id}.txt`
2. Записать инструкцию
3. Ожидать файл `cursor_results/result_{task_id}.txt`
4. Проверить контрольную фразу

### Вариант 4: WebSocket или HTTP API

Если Cursor предоставляет API endpoints для автоматизации.

---

## 📊 Сравнение подходов

| Подход | Платформы | Надежность | Сложность | Автоматизация |
|--------|-----------|------------|-----------|---------------|
| cursor-agent CLI | Linux/macOS | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| WSL на Windows | Windows | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| Файловый интерфейс | Все | ⭐⭐⭐ | ⭐ | ⭐⭐ |
| Docker | Все | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| Background API | Все | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

---

## 🎯 Рекомендации

1. **Приоритет:** cursor-agent CLI как основной интерфейс
2. **Fallback:** Файловый интерфейс для Windows без WSL
3. **Тестирование:** Проверить на всех целевых платформах
4. **Документация:** Описать установку и настройку для каждой платформы

---

*Объединено из отдельных исследований для лучшей организации информации*