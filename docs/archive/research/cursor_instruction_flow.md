# Передача инструкций в Cursor и создание нового чата

## Текущая реализация (2026-01-18)

### Как сейчас передается инструкция

#### 1. Через Cursor CLI (если доступен)

**Команда:**
```bash
cursor-agent -p "инструкция" --headless --cwd "/path/to/project"
```

**Реализация в коде:**
```python
# src/cursor_cli_interface.py, метод execute()
cmd = [self.cli_command, "-p", prompt]
if self.headless:
    cmd.append("--headless")
if working_dir:
    cmd.extend(["--cwd", working_dir])
```

**Ограничения:**
- ⚠️ Команда `cursor-agent -p` может не создавать **новый чат** автоматически
- ⚠️ Может использовать текущий открытый чат в Cursor
- ⚠️ Не гарантирует изоляцию задач друг от друга

#### 2. Через файловый интерфейс (fallback)

**Реализация:**
```python
# src/cursor_file_interface.py
cursor_file.write_instruction(instruction, task_id)
# Создает: cursor_commands/instruction_task_001.txt
```

**Проблема:**
- ⚠️ Требует **ручного** чтения файла пользователем или расширением Cursor
- ⚠️ Пользователь должен **вручную создать новый чат** в Cursor
- ⚠️ Пользователь должен **вручную скопировать** инструкцию в чат

### Как ДОЛЖНО работать (идеальная реализация)

#### Создание нового чата

**Проблема:** Cursor CLI (`cursor-agent`) может не иметь прямого параметра для создания нового чата.

**Возможные решения:**

1. **Проверить дополнительные параметры CLI:**
   ```bash
   cursor-agent --help  # Посмотреть все доступные параметры
   cursor-agent -p "инструкция" --new-chat
   cursor-agent -p "инструкция" --session new
   cursor-agent -p "инструкция" --chat-id new
   ```

2. **Использовать комбинацию параметров:**
   ```bash
   cursor-agent -p "Инструкция для новой задачи" --reset-context
   cursor-agent -p "Инструкция" --clear-history
   ```

3. **Если CLI не поддерживает создание нового чата:**
   - Использовать файловый интерфейс с явным указанием создания нового чата
   - Создавать файл с маркером "NEW_CHAT" для расширения Cursor
   - Использовать Background Agents API (если задача для репозитория)

## Рекомендуемый подход

### Вариант 1: Проверка доступных параметров CLI (первый шаг)

```python
# src/cursor_cli_interface.py
def execute_with_new_chat(
    self,
    prompt: str,
    working_dir: Optional[str] = None,
    timeout: Optional[int] = None
) -> CursorCLIResult:
    """
    Выполнить инструкцию в новом чате Cursor
    
    Пробует различные способы создания нового чата
    """
    # Вариант 1: --new-chat
    cmd1 = [self.cli_command, "-p", prompt, "--new-chat"]
    
    # Вариант 2: --session new
    cmd2 = [self.cli_command, "-p", prompt, "--session", "new"]
    
    # Вариант 3: --reset-context
    cmd3 = [self.cli_command, "-p", prompt, "--reset-context"]
    
    # Пробуем по очереди
    for cmd in [cmd1, cmd2, cmd3]:
        try:
            result = subprocess.run(
                cmd,
                capture_output=True,
                text=True,
                timeout=timeout or self.default_timeout,
                cwd=working_dir
            )
            if result.returncode == 0:
                # Успех, используем этот вариант
                return self._parse_result(result)
        except:
            continue
    
    # Если ничего не сработало, используем обычный способ
    return self.execute(prompt, working_dir, timeout)
```

### Вариант 2: Файловый интерфейс с маркерами (надежный fallback)

```python
# src/cursor_file_interface.py
def write_instruction_for_new_chat(
    self,
    instruction: str,
    task_id: str,
    metadata: Optional[Dict[str, Any]] = None
) -> Path:
    """
    Записать инструкцию для нового чата
    
    Создает файл с явным маркером NEW_CHAT для расширения Cursor
    """
    filename = f"instruction_{task_id}_NEW_CHAT.txt"
    file_path = self.commands_dir / filename
    
    content_parts = [
        "# НОВЫЙ ЧАТ",
        f"# Task ID: {task_id}",
        f"# Created: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}",
        "# ACTION: Создай НОВЫЙ чат в Cursor и выполни инструкцию ниже",
        "",
        "# Инструкция:",
        "",
        instruction
    ]
    
    content = "\n".join(content_parts)
    file_path.write_text(content, encoding='utf-8')
    
    return file_path
```

### Вариант 3: Использование .cursor/rules/ (для постоянных инструкций)

Если инструкции повторяются, можно создать файл в `.cursor/rules/`:

```python
def create_cursor_rule(
    self,
    rule_name: str,
    instruction: str
) -> Path:
    """
    Создать правило для Cursor в .cursor/rules/
    
    Cursor автоматически применяет правила из .cursor/rules/*.md
    """
    rules_dir = self.project_dir / ".cursor" / "rules"
    rules_dir.mkdir(parents=True, exist_ok=True)
    
    rule_file = rules_dir / f"{rule_name}.md"
    rule_file.write_text(instruction, encoding='utf-8')
    
    return rule_file
```

## Рекомендации по реализации

### Шаг 1: Проверить CLI параметры

```bash
# Проверить доступные параметры
cursor-agent --help

# Попробовать варианты создания нового чата
cursor-agent -p "тест" --new-chat
cursor-agent -p "тест" --session new
cursor-agent -p "тест" --reset-context
cursor-agent -p "тест" --clear-history
```

### Шаг 2: Обновить код для поддержки нового чата

```python
# src/cursor_cli_interface.py
def execute(self, prompt, working_dir=None, timeout=None, new_chat=True):
    """
    new_chat: Если True, пытаться создать новый чат
    """
    cmd = [self.cli_command, "-p", prompt]
    
    if new_chat:
        # Пробуем добавить параметр для нового чата
        # (после проверки, какой параметр работает)
        cmd.append("--new-chat")  # или другой рабочий параметр
    
    # ... остальной код
```

### Шаг 3: Обновить файловый интерфейс

```python
# src/cursor_file_interface.py
def write_instruction(self, instruction, task_id, new_chat=True):
    """
    new_chat: Если True, добавлять маркер для создания нового чата
    """
    if new_chat:
        # Добавлять маркер NEW_CHAT в файл
        content = f"# NEW_CHAT\n{instruction}"
    # ... остальной код
```

## Идеальный поток создания нового чата

### Через CLI (если поддерживается)

```
1. Code Agent формирует инструкцию
2. Вызывает: cursor-agent -p "инструкция" --new-chat
3. Cursor автоматически создает новый чат
4. Выполняет инструкцию в новом чате
5. Возвращает результат
```

### Через файловый интерфейс

```
1. Code Agent формирует инструкцию
2. Создает файл: cursor_commands/instruction_task_001_NEW_CHAT.txt
   Содержимое:
   # NEW_CHAT
   # Task ID: task_001
   Инструкция...
3. Расширение Cursor или пользователь:
   - Читает файл
   - Создает новый чат в Cursor
   - Копирует инструкцию в чат
   - Выполняет задачу
   - Записывает результат в cursor_results/result_task_001.txt
4. Code Agent ожидает файл результата
```

## Текущая реализация vs Идеальная

### ✅ Что работает сейчас

1. **Передача инструкции через CLI:** `cursor-agent -p "инструкция"`
2. **Передача инструкции через файл:** `cursor_commands/instruction_*.txt`
3. **Ожидание результата:** `cursor_results/result_*.txt` или `docs/results/last_result.md`

### ⚠️ Что НЕ гарантировано

1. **Создание нового чата:** CLI может не поддерживать автоматическое создание
2. **Изоляция задач:** Инструкции могут попадать в один и тот же чат
3. **Контекст:** Предыдущие сообщения в чате могут влиять на выполнение

### 🎯 Что нужно добавить

1. **Проверка параметров CLI:** Тестирование `--new-chat`, `--session new`, и т.д.
2. **Маркеры для нового чата:** В файловом интерфейсе явно указывать `NEW_CHAT`
3. **Документация для пользователя:** Инструкции как вручную создать новый чат
4. **Расширение для Cursor** (опционально): Автоматическое чтение файлов и создание чатов

## Следующие шаги

1. **Проверить CLI:** Запустить `cursor-agent --help` и протестировать параметры
2. **Обновить код:** Добавить поддержку создания нового чата (если CLI поддерживает)
3. **Улучшить файловый интерфейс:** Добавить маркеры `NEW_CHAT` в файлы инструкций
4. **Документировать для пользователя:** Как вручную создавать новый чат (fallback)
