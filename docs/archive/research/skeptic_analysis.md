# Критический анализ Code Agent (Режим Скептика)

**Дата анализа:** 2025-01-26  
**Аналитик:** Скептик  
**Цель:** Выявление узких мест проекта и предложение решений

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. Отсутствие реальной интеграции с Cursor IDE

**Проблема:**  
Документация детально описывает взаимодействие с Cursor, но в коде **полностью отсутствует** реализация механизмов взаимодействия.

**Текущее состояние:**
- ❌ Нет модуля для работы с Cursor IDE
- ❌ Нет файлового интерфейса (самый простой способ)
- ❌ Нет интеграции с Cursor CLI
- ❌ Нет интеграции с Cursor Background Agents API
- ❌ Нет скриншотов и OCR (сложный способ)
- ❌ Нет управления мышью/клавиатурой (сложный способ)
- ❌ Нет создания диалогов в Cursor
- ❌ Нет отправки инструкций в Cursor

**Последствия:**
- Проект **не может работать** как задумано
- Документация описывает несуществующую функциональность
- Нет способа автоматизировать взаимодействие с Cursor

**Решение (приоритет реализации):**

> 📖 **Кратчайший путь:** См. [План реализации](docs/planning/roadmap.md)

**Этап 1 (НАЧАТЬ С ЭТОГО): Файловая система как канал связи**
1. Создать модуль `src/cursor_file_interface.py`:
   - Отправка инструкций через файлы `cursor_commands/instruction_{task_id}.txt`
   - Получение результатов через файлы `cursor_results/result_{task_id}.txt`
   - Не требует API ключей и дополнительных библиотек
   - Работает сразу и надежно

**Этап 2: Cursor CLI (если доступен)**
2. Создать модуль `src/cursor_cli_interface.py`:
   - Использование `cursor-agent` для автоматического выполнения
   - Работа в headless режиме
   - Требует установки Cursor CLI

**Этап 3: Background Agents API**
3. Создать модуль `src/integration/cursor_client.py`:
   - Использование официального Cursor API
   - Работа с GitHub репозиториями
   - Требует API ключ

**Этап 4 (в последнюю очередь): UI автоматизация**
4. Создать модуль `src/cursor_ui_interface.py` с реализацией:
   - Скриншоты окна Cursor (через `pyautogui` или `mss`)
   - OCR для распознавания состояния интерфейса (через `pytesseract` или `easyocr`)
   - Управление мышью/клавиатурой (через `pyautogui`)
   - Определение координат элементов интерфейса
   - Создание новых диалогов
   - Отправка текста в диалог
   - ⚠️ Использовать только как fallback, т.к. хрупко

**Рекомендация:** Начать с Этапа 1 (файловая система) - это самый простой и надежный способ.

---

### 2. Отсутствие механизма ожидания файлов-репортов

**Проблема:**  
Документация описывает детальный алгоритм ожидания репортов с проверкой контрольных фраз, но в коде **нет реализации**.

**Текущее состояние:**
- ❌ Нет функции `wait_for_file()`
- ❌ Нет проверки контрольных фраз
- ❌ Нет таймаутов
- ❌ Нет проверки состояния Cursor при таймаутах

**Последствия:**
- Code Agent не может дожидаться выполнения работы Cursor
- Нет синхронизации между агентами
- Процесс не может работать автоматически

**Решение:**
1. Создать модуль `src/report_watcher.py`:
```python
class ReportWatcher:
    def wait_for_report(
        self, 
        report_path: Path, 
        control_phrases: List[str],
        timeout: int = 300,
        check_interval: int = 10
    ) -> bool:
        # Реализация ожидания с проверкой контрольных фраз
```

2. Добавить в `config.yaml`:
```yaml
cursor:
  report_check_interval: 10
  file_creation_timeout: 300
  control_phrase_timeout: 120
  control_phrases:
    - "Отчет завершен!"
    - "Тестирование завершено!"
    - "Отчет готов!"
```

---

### 3. Отсутствие шаблонов инструкций в конфигурации

**Проблема:**  
Документация описывает использование шаблонов инструкций из `config.yaml`, но в конфигурации **нет секции `instructions`**.

**Текущее состояние:**
- ❌ Нет секции `instructions` в `config.yaml`
- ❌ Нет шаблонов для разных типов задач
- ❌ Нет механизма выбора шаблона по типу задачи

**Последствия:**
- Невозможно использовать описанный в документации процесс
- Нет способа генерировать инструкции для Cursor
- Процесс не соответствует документации

**Решение:**
1. Добавить в `config/config.yaml`:
```yaml
instructions:
  default:
    - instruction_id: 1
      name: "Создание плана"
      for_cursor: true
      template: |
        Переходим к выполнению задачи "{task_name}"
        Изучи связанную документацию по проекту.
        Создай план выполнения текущей задачи в файле 
        docs/results/current_plan_{task_id}.md
        Создай краткий отчет в docs/results/last_result.md
        В конце отчета напиши "Отчет завершен!"
      wait_for_file: "docs/results/last_result.md"
      control_phrase: "Отчет завершен!"
    
    - instruction_id: 2
      name: "Выполнение плана"
      # ...
  
  frontend-task:
    # Специфичные шаблоны для фронтенд задач
  
  backend-task:
    # Специфичные шаблоны для бэкенд задач
```

2. Создать модуль `src/instruction_generator.py` для работы с шаблонами

---

### 4. Отсутствие механизма отправки инструкций Cursor

**Проблема:**  
Нет кода для отправки инструкций в Cursor IDE.

**Решение:**
1. Добавить в `src/cursor_interface.py`:
```python
def send_instruction_to_cursor(self, instruction: str) -> bool:
    """Отправка инструкции в новый диалог Cursor"""
    # 1. Создать новый диалог
    # 2. Ввести текст инструкции
    # 3. Отправить (Enter)
    # 4. Вернуть успех/неудачу
```

---

### 5. Разрыв между документацией и реализацией

**Проблема:**  
Документация описывает детальный процесс из 9 инструкций (создание плана, выполнение, тестирование, ревью, коммит), но в коде **нет реализации** этого процесса.

**Текущее состояние:**
- Документация: Детальный workflow с 9 этапами
- Код: Только базовая структура без реализации workflow

**Последствия:**
- Невозможно следовать описанному процессу
- Документация вводит в заблуждение
- Проект не может выполнять задачи как описано

**Решение:**
1. Реализовать `src/workflow_executor.py` с классом `WorkflowExecutor`:
```python
class WorkflowExecutor:
    def execute_task_workflow(self, todo_item: TodoItem):
        # 1. Создание плана
        # 2. Выполнение плана
        # 3. Тестирование
        # 4. Обновление документации
        # 5. Ревью Скептика
        # 6. Исправление по ревью
        # 7. Полное тестирование
        # 8. Коммит
        # 9. Обновление статуса
```

---

### 6. Отсутствие обработки ошибок при ожидании репортов

**Проблема:**  
Нет обработки ситуаций, когда файл-репорт не создан или контрольная фраза отсутствует.

**Решение:**
1. Добавить обработку ошибок в `ReportWatcher`:
   - Проверка состояния Cursor при таймауте
   - Логирование проблем
   - Повторные попытки
   - Эскалация проблем

---

### 7. Отсутствие валидации контрольных фраз

**Проблема:**  
Нет кода для проверки контрольных фраз в репортах.

**Решение:**
1. Добавить в `ReportWatcher`:
```python
def check_control_phrase(self, content: str, phrases: List[str]) -> bool:
    """Проверка наличия контрольной фразы в содержимом"""
```

---

### 8. Отсутствие структуры директорий для репортов

**Проблема:**  
Код не создает директории `docs/results/` и `docs/reviews/`.

**Решение:**
1. Добавить в `server.py` при инициализации:
```python
# Создание директорий для репортов
(self.project_dir / "docs" / "results").mkdir(parents=True, exist_ok=True)
(self.project_dir / "docs" / "reviews").mkdir(parents=True, exist_ok=True)
```

---

### 9. Отсутствие парсинга планов от Cursor

**Проблема:**  
Нет кода для чтения и разбора планов из `current_plan_X.md`.

**Решение:**
1. Создать `src/plan_parser.py`:
```python
class PlanParser:
    def parse_plan(self, plan_file: Path) -> List[PlanItem]:
        """Парсинг плана из markdown файла"""
        # Извлечение пунктов плана
        # Построение иерархии
        # Возврат структурированного плана
```

---

### 10. Отсутствие механизма последовательного выполнения плана

**Проблема:**  
Нет кода для разбивки плана на пункты и последовательного выполнения.

**Решение:**
1. Добавить в `WorkflowExecutor`:
```python
def execute_plan_step_by_step(self, plan: List[PlanItem]):
    """Последовательное выполнение пунктов плана"""
    for item in plan:
        # Отправка инструкции для пункта
        # Ожидание репорта
        # Проверка результата
        # Переход к следующему пункту
```

---

### 11. Отсутствие механизма ревью Скептика

**Проблема:**  
Нет автоматизации процесса ревью Скептика.

**Решение:**
1. Добавить в `WorkflowExecutor`:
```python
def request_skeptic_review(self, task_id: str):
    """Запрос ревью от Скептика"""
    instruction = f"""
    Выступи в роли Скептика.
    Проанализируй текущие доработки и документацию.
    Напиши ревью в docs/reviews/skeptic_{task_id}_{date}.md
    В конце напиши "Отчет готов!"
    """
    # Отправка инструкции
    # Ожидание репорта
```

---

### 12. Отсутствие обработки ошибок тестирования

**Проблема:**  
Нет логики для повторных попыток при ошибках тестирования.

**Решение:**
1. Добавить в `WorkflowExecutor`:
```python
def handle_test_errors(self, test_report: Path, max_retries: int = 3):
    """Обработка ошибок тестирования с повторными попытками"""
    for attempt in range(max_retries):
        if self.has_errors(test_report):
            # Отправка инструкции на исправление
            # Повторное тестирование
        else:
            break
```

---

### 13. Отсутствие механизма коммитов

**Проблема:**  
Нет автоматизации создания коммитов и отправки в репозиторий.

**Решение:**
1. Создать `src/git_manager.py`:
```python
class GitManager:
    def create_commit(self, message: str, files: List[Path]) -> bool:
        """Создание коммита с указанными файлами"""
    
    def push_to_remote(self, branch: str = "main") -> bool:
        """Отправка коммита в удаленный репозиторий"""
```

2. Или делегировать это Cursor через инструкции (как описано в документации)

---

### 14. Отсутствие конфигурации для Cursor

**Проблема:**  
В `config.yaml` нет настроек для взаимодействия с Cursor.

**Решение:**
1. Добавить в `config/config.yaml`:
```yaml
cursor:
  # Настройки взаимодействия с Cursor IDE
  interface_type: "screenshot"  # или "api", "file"
  screenshot_path: "temp/cursor_screenshot.png"
  window_title: "Cursor"
  
  # Настройки ожидания репортов
  report_check_interval: 10
  file_creation_timeout: 300
  control_phrase_timeout: 120
  
  # Контрольные фразы
  control_phrases:
    - "Отчет завершен!"
    - "Тестирование завершено!"
    - "Отчет готов!"
    - "План готов!"
    - "Коммит выполнен!"
```

---

### 15. Отсутствие валидации модели LLM

**Проблема:**  
Нет проверки, что используется правильная (слабая) модель для Code Agent.

**Решение:**
1. Добавить в `config_loader.py`:
```python
ALLOWED_MODELS = [
    "gpt-3.5-turbo",
    "claude-haiku",
    "llama2",
    "mistral"
]

def validate_model(self, model: str):
    if model not in ALLOWED_MODELS:
        logger.warning(f"Модель {model} может быть слишком мощной для Code Agent")
```

---

## 🟡 СЕРЬЕЗНЫЕ ПРОБЛЕМЫ

### 16. Неполная реализация TodoManager

**Проблема:**  
Метод `_save_todos()` не реализован (только `pass`).

**Решение:**
1. Реализовать сохранение обратно в файл todo

---

### 17. Отсутствие тестов

**Проблема:**  
В проекте нет тестов для проверки функциональности.

**Решение:**
1. Создать структуру `tests/`
2. Написать unit-тесты для всех модулей
3. Написать integration-тесты для workflow

---

### 18. Отсутствие обработки иерархических задач

**Проблема:**  
Код не обрабатывает иерархию задач (родитель-дочерние).

**Решение:**
1. Реализовать обработку иерархии в `TodoManager`
2. Добавить логику выполнения дочерних задач

---

### 19. Отсутствие логирования взаимодействия с Cursor

**Проблема:**  
Нет детального логирования операций с Cursor.

**Решение:**
1. Добавить логирование всех операций с Cursor
2. Сохранять скриншоты для отладки

---

### 20. Отсутствие обработки параллельных задач

**Проблема:**  
Нет поддержки параллельного выполнения независимых задач.

**Решение:**
1. Добавить поддержку параллельного выполнения через `asyncio` или `threading`

---

## 🟢 РЕКОМЕНДАЦИИ ПО УЛУЧШЕНИЮ

### 21. Добавить валидацию конфигурации

**Решение:**
1. Создать `src/config_validator.py` для проверки корректности конфигурации

---

### 22. Добавить мониторинг и метрики

**Решение:**
1. Добавить сбор метрик:
   - Время выполнения задач
   - Количество ошибок
   - Успешность выполнения

---

### 23. Улучшить обработку ошибок

**Решение:**
1. Добавить детальную обработку всех типов ошибок
2. Создать систему уведомлений

---

### 24. Добавить документацию по API

**Решение:**
1. Дополнить `docs/api.md` примерами использования всех модулей

---

### 25. Добавить примеры конфигурации

**Решение:**
1. Создать `config/config.example.yaml` с полными примерами

---

## 📋 ПРИОРИТЕТНЫЙ ПЛАН ДЕЙСТВИЙ

### Фаза 1: Критические исправления (необходимо для работы)

1. ✅ Создать `src/cursor_interface.py` - базовая интеграция с Cursor
2. ✅ Создать `src/report_watcher.py` - механизм ожидания репортов
3. ✅ Добавить секцию `instructions` в `config.yaml`
4. ✅ Создать `src/instruction_generator.py` - генерация инструкций
5. ✅ Создать `src/workflow_executor.py` - выполнение workflow
6. ✅ Добавить создание директорий для репортов

### Фаза 2: Важные улучшения

7. ✅ Создать `src/plan_parser.py` - парсинг планов
8. ✅ Реализовать обработку ошибок тестирования
9. ✅ Добавить валидацию модели LLM
10. ✅ Добавить конфигурацию для Cursor

### Фаза 3: Дополнительные функции

11. ✅ Реализовать `_save_todos()` в TodoManager
12. ✅ Добавить тесты
13. ✅ Добавить обработку иерархических задач
14. ✅ Добавить мониторинг и метрики

---

## 🎯 ВЫВОДЫ

### Текущее состояние проекта:

**Документация:** ⭐⭐⭐⭐⭐ (Отлично)  
**Реализация:** ⭐⭐ (Неудовлетворительно)

**Разрыв между документацией и кодом:** Критический

### Основные проблемы:

1. **Отсутствие реальной интеграции с Cursor** - проект не может работать как задумано
2. **Отсутствие механизма ожидания репортов** - нет синхронизации между агентами
3. **Отсутствие workflow executor** - нет реализации описанного процесса
4. **Отсутствие шаблонов инструкций** - нет способа генерировать инструкции

### Рекомендации:

1. **Немедленно:** Реализовать базовую интеграцию с Cursor
2. **Срочно:** Реализовать механизм ожидания репортов
3. **Важно:** Реализовать workflow executor
4. **Необходимо:** Добавить шаблоны инструкций в конфигурацию

### Оценка готовности проекта:

**Текущая готовность:** 20%  
**Готовность после Фазы 1:** 60%  
**Готовность после Фазы 2:** 80%  
**Готовность после Фазы 3:** 95%

---

**Заключение:** Проект имеет отличную документацию, но критически не хватает реализации ключевых компонентов. Необходимо срочно реализовать интеграцию с Cursor и механизм ожидания репортов для того, чтобы проект мог работать как описано в документации.
