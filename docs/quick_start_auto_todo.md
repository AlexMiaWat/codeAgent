# Быстрый старт: Автоматическая генерация TODO

## Что это?

Когда Code Agent выполнит все задачи из TODO листа, он автоматически:
1. 🎯 Проанализирует проект как главный архитектор
2. 📝 Создаст новый TODO лист с задачами для развития
3. 📚 Напишет документацию для каждой задачи
4. ✅ Сделает коммит и продолжит работу

**Ограничение:** Максимум 5 генераций за одну сессию (настраивается).

## Быстрая настройка

### 1. Проверьте конфигурацию

Откройте `config/config.yaml` и убедитесь, что включена автоматическая генерация:

```yaml
server:
  auto_todo_generation:
    enabled: true                          # ✅ Включено
    max_generations_per_session: 5         # Максимум 5 раз за сессию
    output_dir: "todo"                     # Куда сохранять
    session_tracker_file: ".codeagent_sessions.json"
```

### 2. Запустите сервер

```bash
python main.py
```

### 3. Дождитесь выполнения всех задач

Когда все задачи из `todo/CURRENT.md` будут выполнены, сервер автоматически:

```
[INFO] Все задачи выполнены
[INFO] Попытка генерации нового TODO листа...
[INFO] Шаг 1: Архитектурный анализ и создание TODO листа
[INFO] Шаг 2: Создание документации для задач
[INFO] Шаг 3: Финализация TODO листа
[INFO] Шаг 4: Коммит нового TODO листа
[INFO] Генерация TODO листа завершена: todo/GENERATED_20260118_143022.md, задач: 7
[INFO] Загружено 7 новых задач
```

## Что будет создано?

После генерации в вашем проекте появятся:

```
your_project/
├── todo/
│   ├── CURRENT.md                          # ✨ Обновленный TODO лист
│   └── GENERATED_20260118_143022.md        # 📦 Архив генерации
├── docs/
│   ├── planning/
│   │   ├── task_1_20260118_143022.md      # 📄 Документация задачи 1
│   │   ├── task_2_20260118_143022.md      # 📄 Документация задачи 2
│   │   └── task_3_20260118_143022.md      # 📄 Документация задачи 3
│   └── results/
│       ├── todo_generation_20260118_143022.md
│       └── todo_finalized_20260118_143022.md
└── .codeagent_sessions.json                # 📊 Статистика генераций
```

## Пример сгенерированного TODO

```markdown
# Текущие задачи проекта

> **Обновлено:** 2026-01-18
> **Сессия:** 20260118_143022
> **Источник:** Автоматическая генерация Code Agent

## Высокий приоритет
- [ ] Улучшение производительности основного цикла - см. docs/planning/task_1_20260118_143022.md
- [ ] Рефакторинг системы логирования - см. docs/planning/task_2_20260118_143022.md
- [ ] Добавление кэширования результатов - см. docs/planning/task_3_20260118_143022.md

## Средний приоритет
- [ ] Обновление документации API - см. docs/planning/task_4_20260118_143022.md
- [ ] Расширение покрытия тестами - см. docs/planning/task_5_20260118_143022.md
```

## Проверка статистики

Посмотрите, сколько раз генерировался TODO:

```python
from src.session_tracker import SessionTracker
from pathlib import Path

tracker = SessionTracker(Path("path/to/project"))
stats = tracker.get_session_statistics()

print(f"Генераций в этой сессии: {stats['generation_count']}/5")
print(f"Всего генераций: {stats['total_generations_all_time']}")
```

Или просто откройте файл `.codeagent_sessions.json` в корне проекта.

## Отключение автоматической генерации

Если не хотите автоматическую генерацию, отключите в конфигурации:

```yaml
server:
  auto_todo_generation:
    enabled: false  # ❌ Отключено
```

## Изменение лимита генераций

Чтобы разрешить больше генераций за сессию:

```yaml
server:
  auto_todo_generation:
    max_generations_per_session: 10  # Теперь 10 раз
```

## Что делать, если достигнут лимит?

Если увидите в логах:

```
[WARNING] Достигнут лимит генераций TODO для текущей сессии (5)
```

**Варианты:**

1. **Перезапустите сервер** (начнется новая сессия):
   ```bash
   # Ctrl+C для остановки
   python main.py  # Новый запуск = новая сессия
   ```

2. **Увеличьте лимит** в `config/config.yaml`

3. **Сбросьте счетчик вручную** (для тестирования):
   ```python
   from src.session_tracker import SessionTracker
   from pathlib import Path
   
   tracker = SessionTracker(Path("path/to/project"))
   tracker.reset_session_counter()
   ```

## Формат TODO листа

Важно! Сгенерированные TODO листы используют формат с чекбоксами:

```markdown
- [ ] Задача не выполнена
- [x] Задача выполнена
```

**Не используйте нумерованные списки** - они не парсятся Code Agent:

```markdown
❌ 1. Задача один    # Не будет распознана
✅ - [ ] Задача один # Правильно
```

Подробнее: [docs/planning/todo_format_requirements.md](planning/todo_format_requirements.md)

## Как это работает?

1. **Сервер выполняет задачи** из `todo/CURRENT.md`
2. **Обнаруживает пустой TODO** (все задачи выполнены)
3. **Проверяет лимит генераций** (< 5 за сессию?)
4. **Запускает Cursor** с инструкцией:
   - "Выступи как главный архитектор проекта"
   - "Проанализируй код и документацию"
   - "Создай новый TODO лист для развития"
5. **Cursor создает TODO** с 5-10 задачами
6. **Для каждой задачи** Cursor пишет документацию
7. **Финализирует TODO** в правильном формате
8. **Делает коммит** со всеми изменениями
9. **Сервер загружает новые задачи** и продолжает работу

## Полезные команды

### Проверить, сколько осталось генераций

```bash
python -c "from src.session_tracker import SessionTracker; from pathlib import Path; t = SessionTracker(Path('.')); print(f'{t.get_current_session_generation_count()}/5')"
```

### Посмотреть историю генераций

```bash
cat .codeagent_sessions.json
```

### Очистить историю (начать с чистого листа)

```bash
rm .codeagent_sessions.json
```

## Следующие шаги

- 📖 [Полная документация](features/auto_todo_generation.md)
- ⚙️ [Настройка инструкций](../config/config.yaml)
- 🧪 [Тестирование](../test/test_auto_todo_generation.py)

## Часто задаваемые вопросы

### Q: Можно ли генерировать TODO вручную?

**A:** Да, вызовите метод напрямую:

```python
from src.server import CodeAgentServer

server = CodeAgentServer()
success = server._generate_new_todo_list()
```

### Q: Где хранятся все сгенерированные TODO?

**A:** В директории `todo/` с именами `GENERATED_YYYYMMDD_HHMMSS.md`

### Q: Можно ли настроить шаблоны генерации?

**A:** Да, отредактируйте секцию `instructions.empty_todo` в `config/config.yaml`

### Q: Что если Cursor сгенерирует неподходящие задачи?

**A:** Просто отредактируйте `todo/CURRENT.md` вручную или удалите ненужные задачи

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `logs/code_agent.log`
2. Убедитесь, что Cursor CLI доступен
3. Проверьте формат TODO файла
4. Создайте issue на GitHub
