# Быстрый старт: Полный доступ Cursor CLI

## 🚀 Что было сделано

Cursor CLI теперь настроен для **автоматического выполнения команд без запросов разрешений**.

## ✅ Готово к использованию

Все настройки уже применены:

1. ✅ Создан `.cursor/cli-config.json` - конфигурация разрешений
2. ✅ Создан `.cursor/mcp-approvals.json` - одобрение MCP серверов
3. ✅ Обновлен `src/cursor_cli_interface.py` - добавлены флаги `--force --approve-mcps`
4. ✅ Обновлен `config/config.yaml` - настройки автоматизации

## 🎯 Как использовать

### Запуск Code Agent

Просто запустите агент как обычно:

```bash
python -m src.server
```

Все команды Cursor CLI будут выполняться **автоматически без запросов разрешений**.

### Тестовая команда

Проверьте, что все работает:

```bash
# Локальный agent
agent -p "Создай файл test.txt с текстом Hello" --force --approve-mcps

# Docker (если настроен)
docker exec -i cursor-agent bash -c \
  'printf "%s\n" "Создай файл test.txt" | agent -p --force --approve-mcps'
```

## 🔒 Безопасность

### Что разрешено ✅

- Все операции в `src/`, `docs/`, `test/`, `config/`
- Git команды
- Запуск тестов
- Установка зависимостей
- Docker операции

### Что запрещено ❌

- Запись в `*.env`, `credentials*`, `secrets*`
- Опасные команды: `rm -rf /`, `dd`, `mkfs`, `format`
- Системные директории

## 📋 Проверка

### 1. Проверить конфигурацию

```bash
# Windows
dir .cursor
type .cursor\cli-config.json

# Linux/Mac
ls -la .cursor/
cat .cursor/cli-config.json
```

### 2. Проверить логи

```bash
# Проверить логи Code Agent
tail -f logs/code_agent.log
```

### 3. Тестовый запуск

```python
# test_full_access.py
from src.cursor_cli_interface import create_cursor_cli_interface

cli = create_cursor_cli_interface()
result = cli.execute_command("Создай файл test_permissions.txt")
print(f"Success: {result.success}")
print(f"Output: {result.stdout}")
```

## 🛠️ Настройка (опционально)

### Добавить новое разрешение

Отредактируйте `.cursor/cli-config.json`:

```json
{
  "permissions": {
    "allow": [
      "Shell(новая-команда)"
    ]
  }
}
```

### Добавить новый MCP сервер

Отредактируйте `.cursor/mcp-approvals.json`:

```json
{
  "approvals": {
    "новый-сервер": {
      "approved": true,
      "timestamp": "2026-01-18T00:00:00Z",
      "auto_approve": true
    }
  }
}
```

## 📚 Документация

Подробная документация: [full_access_setup.md](full_access_setup.md)

## ❓ Проблемы

### Cursor все еще запрашивает разрешения

1. Проверьте, что файлы существуют:
   ```bash
   ls .cursor/cli-config.json
   ls .cursor/mcp-approvals.json
   ```

2. Проверьте логи:
   ```bash
   tail -f logs/code_agent.log
   ```

3. Проверьте версию Cursor CLI:
   ```bash
   agent --version
   ```

### MCP серверы не работают

1. Проверьте одобрение:
   ```bash
   cat .cursor/mcp-approvals.json
   ```

2. Вручную одобрите:
   ```bash
   cursor-agent mcp login <server-name>
   ```

## 🎉 Готово!

Теперь Code Agent может выполнять команды через Cursor CLI **полностью автоматически** без запросов разрешений.

Просто запустите:

```bash
python -m src.server
```

И агент начнет работать! 🚀
