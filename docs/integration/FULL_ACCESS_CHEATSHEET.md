# 🚀 Шпаргалка: Полный доступ Cursor CLI

## ✅ Что настроено

Cursor CLI теперь работает **БЕЗ ЗАПРОСОВ РАЗРЕШЕНИЙ**:

- ✅ `.cursor/cli-config.json` - конфигурация разрешений
- ✅ `.cursor/mcp-approvals.json` - одобрение MCP серверов
- ✅ `src/cursor_cli_interface.py` - флаги `--force --approve-mcps`
- ✅ `config/config.yaml` - настройки автоматизации

## 🎯 Быстрый старт

```bash
# Просто запустите агент
python -m src.server
```

Всё! Агент работает полностью автоматически.

## 🧪 Тестирование

```bash
# Запустить тесты полного доступа
pytest test/test_full_access_permissions.py -v

# Тестовая команда (локальный agent)
agent -p "Создай файл test.txt" --force --approve-mcps

# Тестовая команда (Docker)
docker exec -i cursor-agent-life bash -c \
  'printf "%s\n" "Создай файл test.txt" | agent -p --force --approve-mcps'
```

## 📋 Проверка

```bash
# Проверить конфигурацию
cat .cursor/cli-config.json
cat .cursor/mcp-approvals.json

# Проверить логи
tail -f logs/code_agent.log

# Проверить версию
agent --version
```

## 🔧 Добавить разрешение

Отредактируйте `.cursor/cli-config.json`:

```json
{
  "permissions": {
    "allow": [
      "Shell(новая-команда)",
      "Read(новый/путь/**)",
      "Write(новый/путь/**)"
    ]
  }
}
```

## 🔒 Безопасность

### ✅ Разрешено
- Операции в `src/`, `docs/`, `test/`, `config/`
- Git, npm, python, pytest, docker
- Чтение/запись проектных файлов

### ❌ Запрещено
- Запись в `*.env`, `credentials*`, `secrets*`
- Опасные команды: `rm -rf /`, `dd`, `mkfs`
- Системные директории

## 📚 Документация

- [Полная документация](full_access_setup.md)
- [Быстрый старт](QUICK_START_FULL_ACCESS.md)

## 🆘 Проблемы

### Cursor запрашивает разрешения

```bash
# Проверить файлы
ls .cursor/cli-config.json
ls .cursor/mcp-approvals.json

# Проверить логи
tail -f logs/code_agent.log

# Проверить версию
agent --version
```

### MCP не работает

```bash
# Проверить одобрение
cat .cursor/mcp-approvals.json

# Вручную одобрить
cursor-agent mcp login <server-name>
```

## 🎉 Готово!

Всё настроено и работает автоматически! 🚀

```bash
python -m src.server
```
