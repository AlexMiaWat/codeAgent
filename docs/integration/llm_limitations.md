# Ограничения LLM агента Code Agent

## Важно: Использование слабых языковых моделей

Code Agent использует **слабые языковые модели** на базе бесплатных моделей от доступных провайдеров. Это накладывает **строгие ограничения** на возможности LLM агента.

## Принципы работы LLM в Code Agent

### ⚠️ Нет Deep Thinking

LLM агента Code Agent **НИКОГДА** не делает:
- ❌ Глубокий анализ архитектуры проекта
- ❌ Сложные рассуждения о реализации
- ❌ Принятие сложных решений без инструкций
- ❌ Генерацию новых планов выполнения
- ❌ Анализ документации проекта
- ❌ Генерацию кода

### ✅ Только простые действия

LLM агента выполняет **только простые осмысленные действия** для понимания контекста:

#### 1. Парсинг текста задачи
```python
# Пример: Простой парсинг текста из todo-листа
task_text = "5. Создание клиентского слоя через web front"
# LLM извлекает: номер задачи (5), текст задачи
```

#### 2. Определение типа задачи (по ключевым словам)
```python
# Пример: Простой pattern matching
if "frontend" in task_text.lower() or "web" in task_text.lower():
    task_type = "frontend-development"
elif "backend" in task_text.lower() or "api" in task_text.lower():
    task_type = "backend-development"
```

#### 3. Выбор шаблона инструкции из конфигурации
```python
# Пример: Выбор предопределенного шаблона
config = load_config("config/config.yaml")
instruction_template = config["instructions"][task_type]
# НЕ создает новый план, только выбирает существующий шаблон
```

#### 4. Подстановка значений в шаблон
```python
# Пример: Простая подстановка в шаблон
instruction = instruction_template.format(
    task_name=task_text,
    task_id="5.1",
    docs_dir="docs"
)
```

#### 5. Простые проверки результатов
```python
# Пример: Простые проверки
- Проверка наличия файла: file_exists("docs/results/last_result.md")
- Проверка контрольной фразы: "Отчет завершен!" in file_content
- Простая валидация формата: check_file_format(file_path)
```

## Рекомендуемые модели для Code Agent

### Бесплатные модели

#### OpenAI
- **GPT-3.5-turbo** - рекомендуемая модель
  - Быстрая
  - Дешевая (или бесплатная в некоторых случаях)
  - Достаточна для простых действий
  - **Не использовать GPT-4** - слишком мощная для простых задач

#### Anthropic
- **Claude Haiku** - легкая модель
  - Быстрая
  - Бесплатная или очень дешевая
  - Подходит для простых задач

#### Локальные модели (Ollama)
- **llama2** - 7B параметров
- **mistral** - легкая модель
- **codellama** - специализированная для кода (но Code Agent не генерирует код!)

### 🆓 Бесплатные API-провайдеры для Code Agent

Для разработки и использования Code Agent доступны полностью бесплатные API-провайдеры, которые идеально подходят для простых задач агента (парсинг, выбор шаблонов).

#### Бесплатные агрегированные провайдеры

**FreeFlow LLM:**
- До ~16 000 бесплатных запросов ежедневно
- Поддержка моделей: GPT-4o, Llama 3.3 70B, Google Gemini 2.5 Flash
- OpenAI-style интерфейс (легкая интеграция с CrewAI)
- Ротация провайдеров при превышении лимитов
- ✅ **Рекомендуется как основной бесплатный вариант**

**OpenRouter:**
- Единый API для 100+ моделей
- Free-tier модели: Llama 3.1 8B Instruct, Mistral 7B Instruct, Gemma 2.5 9B
- Один API-ключ для всех провайдеров
- ⚠️ Ограниченные free-tier лимиты

**Google AI Studio:**
- Официальный доступ к Gemma 2.5/3 (4B/12B/27B)
- Стабильные бесплатные квоты
- Требуется регистрация

#### Сравнение бесплатных моделей для Code Agent

| Модель | Провайдер | Подходит для Code Agent | Примечания |
|--------|-----------|-------------------------|------------|
| **Llama 3.1 8B Instruct** | OpenRouter, FreeFlow | ✅ Да | Баланс качества/скорости |
| **Mistral 7B Instruct** | OpenRouter, FreeFlow | ✅ Да | Оптимальный выбор |
| **Gemma 2.5 / Gemma 3** | Google AI Studio | ✅ Да | Легкие модели для простых задач |
| **GPT-3.5-turbo** | FreeFlow (если доступно) | ✅ Да | Быстрая, достаточна для парсинга |
| **Qwen (free tiers)** | OpenRouter | ⚠️ Зависит от модели | Некоторые варианты подходят |

**Выводы по бесплатным провайдерам:**

1. ✅ **Code Agent идеально работает с бесплатными провайдерами** благодаря простоте задач:
   - Парсинг текста не требует мощных моделей
   - Выбор шаблонов — тривиальная операция
   - Простые проверки выполняют даже легкие модели

2. ✅ **FreeFlow LLM — лучший выбор** для начала разработки:
   - Максимальные бесплатные лимиты
   - Поддержка множества моделей
   - Удобная интеграция с Python

3. ✅ **Не требуется кредитная карта** — можно полностью разрабатывать и тестировать бесплатно

4. ⚠️ **Ограничения бесплатных провайдеров:**
   - Дневные/минутные лимиты могут быть ограничены
   - Для production может потребоваться платный план
   - Некоторые провайдеры имеют лимиты на количество токенов

5. 💡 **Рекомендация:** Используйте бесплатные провайдеры для разработки, а при необходимости масштабирования рассмотрите переход на платные планы или локальные модели (Ollama).

**Важно:** Даже с бесплатными моделями Code Agent будет работать эффективно, так как задачи агента не требуют глубокого мышления или сложных рассуждений.

### Не рекомендуемые модели

- ❌ **GPT-4** - слишком мощная, избыточна для простых задач
- ❌ **Claude Opus** - слишком мощная
- ❌ **GPT-4 Turbo** - слишком мощная

**Причина:** Code Agent использует LLM только для простых действий. Мощные модели предназначены для агентов Cursor, которые выполняют интеллектуальную работу.

## Разделение ответственности

### Code Agent (слабая LLM)
- ✅ Парсинг задач
- ✅ Выбор шаблонов инструкций
- ✅ Подстановка значений
- ✅ Простые проверки результатов

### Cursor Agents (мощные LLM)
- ✅ Глубокий анализ архитектуры
- ✅ Планирование выполнения
- ✅ Генерация кода
- ✅ Анализ документации
- ✅ Сложные рассуждения
- ✅ Рефакторинг
- ✅ Тестирование

## Примеры правильного использования

### ✅ Правильно: Использование шаблона

```yaml
# config/config.yaml
instructions:
  frontend-task:
    template: |
      Переходим к выполнению задачи "{task_name}"
      1. Изучи документацию
      2. Создай план в docs/results/current_plan_{task_id}.md
      3. Отчет в docs/results/last_result.md
```

LLM выбирает шаблон и подставляет значения:
```python
task_type = determine_task_type(task_text)  # "frontend-task"
template = config["instructions"][task_type]
instruction = template.format(task_name="...", task_id="5.1")
```

### ❌ Неправильно: Генерация нового плана

```python
# НЕПРАВИЛЬНО!
instruction = llm.generate(f"Создай план для задачи: {task_text}")
# Это требует deep thinking, не допустимо!
```

### ✅ Правильно: Простая проверка результата

```python
# Простая проверка наличия файла
if file_exists("docs/results/last_result.md"):
    content = read_file("docs/results/last_result.md")
    if "Отчет завершен!" in content:
        proceed_to_next_step()
```

### ❌ Неправильно: Глубокий анализ результата

```python
# НЕПРАВИЛЬНО!
analysis = llm.analyze(code_file, "Найди все проблемы в коде")
# Это требует deep thinking, не допустимо!
# Глубокий анализ делает Cursor Agent, не Code Agent!
```

## Конфигурация модели в .env

```env
# Рекомендуемая настройка для Code Agent
OPENAI_API_KEY=your_key_here
OPENAI_MODEL=gpt-3.5-turbo  # Слабая модель для простых задач

# Или для локальных моделей
OLLAMA_API_URL=http://localhost:11434
OLLAMA_MODEL=llama2  # Легкая модель

# НЕ использовать GPT-4 для Code Agent!
# OPENAI_MODEL=gpt-4  # ❌ Избыточно мощная
```

## Важные ограничения в коде

### Проверка модели

В конфигурации следует проверять, что используется правильная модель:

```python
# Рекомендуемые модели для Code Agent
ALLOWED_MODELS = [
    "gpt-3.5-turbo",
    "claude-haiku",
    "llama2",
    "mistral"
]

# Предупреждение при использовании мощных моделей
if model in ["gpt-4", "gpt-4-turbo", "claude-opus"]:
    logger.warning(f"Модель {model} слишком мощная для Code Agent. "
                   f"Рекомендуется использовать слабые модели для простых задач.")
```

## Почему слабые модели?

### 1. Экономия ресурсов
- Бесплатные или дешевые модели
- Быстрое выполнение простых задач
- Не тратить мощь модели на тривиальные операции

### 2. Четкое разделение ответственности
- Code Agent = простой координатор (слабая LLM)
- Cursor Agents = интеллектуальные исполнители (мощные LLM)

### 3. Предсказуемость
- Слабые модели более предсказуемы для простых задач
- Меньше вероятность ошибок в тривиальных операциях
- Четкое следование инструкциям из конфигурации

### 4. Масштабируемость
- Можно запустить множество Code Agent с дешевыми моделями
- Каждый контролирует свой проект
- Cursor Agents используются только когда нужно

## Заключение

LLM агента Code Agent - это **координатор с минимальными интеллектуальными способностями**. Вся интеллектуальная работа выполняется агентами Cursor, которые используют мощные модели.

**Правило:** Если задача требует deep thinking - она должна выполняться агентом Cursor, а не Code Agent!
