# План реализации: Кратчайший путь к успеху

**Дата:** 2025-01-26  
**Цель:** Определить кратчайший путь для реализации рабочего Code Agent

---

## 🎯 СТРАТЕГИЯ: От простого к сложному

**Принцип:** Начать с самого простого подхода, который работает **прямо сейчас**, без API ключей и сложных зависимостей.

---

## ✅ ЭТАП 1: Файловая система как канал связи (1-2 дня)

### Почему это первое?

✅ **Не требует API ключей**  
✅ **Не требует установки дополнительных библиотек**  
✅ **Работает сразу**  
✅ **Надежно и стабильно**  
✅ **Не зависит от изменений интерфейса Cursor**

### Что нужно сделать:

1. **Создать структуру директорий:**
   ```
   cursor_commands/     # Команды для Cursor
   cursor_results/     # Результаты от Cursor
   ```

2. **Создать модуль `src/cursor_file_interface.py`:**
   ```python
   class CursorFileInterface:
       """Интерфейс взаимодействия с Cursor через файлы."""
       
       def send_instruction(self, instruction: str, task_id: str) -> str:
           """Отправить инструкцию через файл."""
           # Создать файл cursor_commands/instruction_{task_id}.txt
           # Вернуть путь к файлу
       
       def wait_for_result(self, task_id: str, timeout: int = 300) -> str:
           """Дождаться результата от Cursor."""
           # Ожидать файл cursor_results/result_{task_id}.txt
           # Проверить контрольную фразу
           # Вернуть содержимое
   ```

3. **Создать модуль `src/report_watcher.py`:**
   ```python
   class ReportWatcher:
       """Ожидание файлов-репортов с проверкой контрольных фраз."""
       
       def wait_for_report(
           self,
           report_path: Path,
           control_phrases: List[str],
           timeout: int = 300,
           check_interval: int = 10
       ) -> bool:
           # Реализация ожидания
   ```

4. **Обновить `src/server.py`:**
   - Использовать `CursorFileInterface` вместо несуществующего UI интерфейса
   - Интегрировать `ReportWatcher`

### Результат:

✅ Code Agent может отправлять инструкции Cursor через файлы  
✅ Code Agent может получать результаты через файлы  
✅ Работает **прямо сейчас** без дополнительных зависимостей

### Как это работает:

1. Code Agent создает файл `cursor_commands/instruction_001.txt` с инструкцией
2. Пользователь (или будущее расширение Cursor) читает файл и выполняет инструкцию в Cursor
3. Результат записывается в `cursor_results/result_001.txt` с контрольной фразой
4. Code Agent читает результат и продолжает работу

---

## ✅ ЭТАП 2: Cursor CLI (если доступен) (2-3 дня)

### Почему это второе?

✅ **Не требует API ключей**  
✅ **Автоматизирует выполнение**  
✅ **Работает в headless режиме**  
⚠️ **Требует установки Cursor CLI**

### Что нужно сделать:

1. **Проверить доступность Cursor CLI:**
   ```bash
   cursor-agent --version
   ```

2. **Создать модуль `src/cursor_cli_interface.py`:**
   ```python
   class CursorCLIInterface:
       """Интерфейс взаимодействия с Cursor через CLI."""
       
       def execute(self, prompt: str, working_dir: str = None) -> Dict:
           """Выполнить команду через Cursor CLI."""
           # Использовать subprocess для вызова cursor-agent
   ```

3. **Интегрировать в `src/server.py`:**
   - Использовать CLI для автоматического выполнения
   - Fallback на файловый интерфейс, если CLI недоступен

### Результат:

✅ Автоматическое выполнение без участия пользователя  
✅ Работа в headless режиме

---

## ✅ ЭТАП 3: Background Agents API (для репозиториев) (3-5 дней)

### Почему это третье?

✅ **Официальный API**  
✅ **Надежно и стабильно**  
⚠️ **Требует API ключ**  
⚠️ **Работает только с GitHub репозиториями**

### Что нужно сделать:

1. **Получить Cursor API ключ:**
   - Зарегистрироваться на [cursor.com](https://cursor.com)
   - Получить API ключ в настройках

2. **Создать модуль `src/integration/cursor_client.py`:**
   ```python
   class CursorAPIClient:
       """Клиент для работы с Cursor Background Agents API."""
       
       def create_agent(self, repository: str, instruction: str) -> CursorAgent:
           # Создание агента через API
       
       def get_agent_status(self, agent_id: str) -> Dict:
           # Получение статуса агента
   ```

3. **Добавить переменные окружения:**
   ```bash
   CURSOR_API_KEY=your_api_key_here
   CURSOR_API_BASE_URL=https://api.cursor.com/v0
   ```

4. **Интегрировать в workflow:**
   - Использовать для работы с удаленными репозиториями
   - Локальные проекты - через файловый интерфейс или CLI

### Результат:

✅ Автоматическая работа с GitHub репозиториями  
✅ Мониторинг статуса агентов  
✅ Follow-up инструкции

---

## ⚠️ ЭТАП 4: UI автоматизация (в последнюю очередь) (5-7 дней)

### Почему это последнее?

⚠️ **Хрупко и зависит от изменений интерфейса**  
⚠️ **Требует точных координат элементов**  
⚠️ **Может ломаться при обновлениях Cursor**  
⚠️ **Требует установки множества библиотек**

### Что нужно сделать:

1. **Установить библиотеки:**
   ```bash
   pip install pyautogui pytesseract Pillow opencv-python mss pygetwindow
   ```

2. **Установить Tesseract OCR:**
   - Windows: https://github.com/UB-Mannheim/tesseract/wiki

3. **Создать модуль `src/cursor_ui_interface.py`:**
   ```python
   class CursorUIInterface:
       """Интерфейс взаимодействия с Cursor через UI автоматизацию."""
       
       def create_new_chat(self):
           # Найти окно Cursor
           # Открыть новый чат через горячие клавиши
       
       def send_message(self, text: str):
           # Ввести текст в чат
           # Отправить сообщение
   ```

4. **Использовать как fallback:**
   - Только если файловый интерфейс и CLI недоступны
   - С обработкой ошибок и таймаутами

### Результат:

✅ Прямое управление интерфейсом Cursor  
⚠️ Требует постоянной поддержки при обновлениях

---

## 📋 ПРИОРИТЕТНЫЙ ПЛАН ДЕЙСТВИЙ

### Неделя 1: Базовый функционал

**День 1-2: Файловый интерфейс**
- [ ] Создать `src/cursor_file_interface.py`
- [ ] Создать `src/report_watcher.py`
- [ ] Интегрировать в `src/server.py`
- [ ] Протестировать с ручным выполнением инструкций

**День 3-4: Report Watcher**
- [ ] Реализовать ожидание файлов
- [ ] Добавить проверку контрольных фраз
- [ ] Добавить таймауты и обработку ошибок
- [ ] Протестировать с разными сценариями

**День 5: Интеграция и тестирование**
- [ ] Интегрировать все компоненты
- [ ] Протестировать полный цикл работы
- [ ] Исправить найденные ошибки

### Неделя 2: Автоматизация

**День 1-2: Cursor CLI (если доступен)**
- [ ] Проверить доступность CLI
- [ ] Создать `src/cursor_cli_interface.py`
- [ ] Интегрировать в workflow

**День 3-5: Background Agents API**
- [ ] Получить API ключ
- [ ] Создать `src/integration/cursor_client.py`
- [ ] Интегрировать для работы с репозиториями
- [ ] Протестировать создание агентов

### Неделя 3+: UI автоматизация (опционально)

- [ ] Установить необходимые библиотеки
- [ ] Создать `src/cursor_ui_interface.py`
- [ ] Реализовать базовые операции
- [ ] Использовать как fallback

---

## 🎯 КРИТЕРИИ УСПЕХА

### Минимальный рабочий вариант (после Этапа 1):

✅ Code Agent может отправлять инструкции через файлы  
✅ Code Agent может получать результаты через файлы  
✅ Механизм ожидания репортов работает  
✅ Проект может выполнять задачи из todo-листа (с ручным участием)

### Полностью автоматический вариант (после Этапа 2-3):

✅ Автоматическое выполнение через CLI или API  
✅ Работа без участия пользователя  
✅ Мониторинг статуса выполнения

---

## 📚 ДОКУМЕНТАЦИЯ ДЛЯ РЕАЛИЗАЦИИ

### Обязательно прочитать:

1. **`docs/cursor_integration.md`** - Как должно работать взаимодействие
2. **`docs/cursor_communication.md`** - Механизм репортов и контрольных фраз
3. **`docs/workflow_detailed.md`** - Детальный процесс работы
4. **`docs/skeptic_analysis.md`** - Все проблемы и решения

### Справочная документация:

- **`docs/api.md`** - API существующих модулей
- **`docs/cursor_setup_recommendations.md`** - Настройка Cursor и библиотек

---

## ⚡ БЫСТРЫЙ СТАРТ (30 минут)

### Шаг 1: Создать базовую структуру

```bash
# Создать директории
mkdir -p cursor_commands cursor_results

# Создать файл для теста
echo "Тестовая инструкция" > cursor_commands/instruction_001.txt
```

### Шаг 2: Создать минимальный интерфейс

```python
# src/cursor_file_interface.py
from pathlib import Path

class CursorFileInterface:
    def __init__(self, commands_dir: Path, results_dir: Path):
        self.commands_dir = commands_dir
        self.results_dir = results_dir
        self.commands_dir.mkdir(parents=True, exist_ok=True)
        self.results_dir.mkdir(parents=True, exist_ok=True)
    
    def send_instruction(self, instruction: str, task_id: str) -> Path:
        """Отправить инструкцию через файл."""
        file_path = self.commands_dir / f"instruction_{task_id}.txt"
        file_path.write_text(instruction, encoding="utf-8")
        return file_path
    
    def check_result(self, task_id: str) -> tuple[bool, str]:
        """Проверить наличие результата."""
        result_path = self.results_dir / f"result_{task_id}.txt"
        if result_path.exists():
            return True, result_path.read_text(encoding="utf-8")
        return False, ""
```

### Шаг 3: Протестировать

```python
# test_file_interface.py
from src.cursor_file_interface import CursorFileInterface
from pathlib import Path

interface = CursorFileInterface(
    Path("cursor_commands"),
    Path("cursor_results")
)

# Отправить инструкцию
interface.send_instruction("Выполни задачу X", "001")

# Вручную создать результат в cursor_results/result_001.txt
# Затем проверить
exists, content = interface.check_result("001")
print(f"Результат получен: {exists}")
print(f"Содержимое: {content}")
```

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

1. **Немедленно:** Реализовать Этап 1 (файловый интерфейс)
2. **На этой неделе:** Добавить Report Watcher
3. **На следующей неделе:** Интегрировать Cursor CLI или API

---

## 💡 КЛЮЧЕВЫЕ ПРИНЦИПЫ

1. **Начинать с простого** - файловая система работает сразу
2. **Добавлять сложность постепенно** - только когда простое работает
3. **Иметь fallback** - если один способ не работает, использовать другой
4. **Тестировать на каждом этапе** - не переходить дальше, пока текущее не работает

---

**Удачи в реализации! 🚀**
