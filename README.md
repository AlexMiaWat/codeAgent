# Code Agent на базе CrewAI

Автоматизированный агент для управления проектами на базе фреймворка CrewAI. Агент работает в бесконечном цикле, анализирует документацию проекта, читает задачи из todo-листа и выполняет их поэтапно, обновляя статус выполнения.

## Возможности

- 🚀 **Простота интеграции**: Минимальные зависимости, чистый API, запуск за несколько строк кода
- 🔄 **Бесконечный цикл работы**: Агент работает непрерывно над проектами
- 📚 **Работа с документацией**: Проверка наличия документации, анализ выполняется агентами Cursor
- ✅ **Управление задачами**: Чтение и выполнение задач из todo-листа
- 📝 **Статус выполнения**: Автоматическое обновление файла статусов проекта
- 🎯 **Интеграция с Cursor**: Координирует выполнение задач через агентов Cursor, генерирует инструкции и контролирует выполнение через файлы-репорты

## Архитектура

Code Agent состоит из следующих компонентов:

1. **Сервер агента** (`src/server.py`) - Основной сервер для взаимодействия с проектами
2. **Менеджер статусов** (`src/status_manager.py`) - Управление файлом статусов проекта
3. **Менеджер задач** (`src/todo_manager.py`) - Чтение и управление todo-листом
4. **Агенты CrewAI** (`src/agents/`) - Определения агентов для выполнения задач
5. **Конфигурация** (`config/`) - Файлы настроек агента

## Установка

### Требования

- Python 3.8+
- CrewAI framework

### Установка зависимостей

```bash
# Использование pip
pip install crewai crewai-tools

# Или с использованием uv (рекомендуется)
uv pip install crewai crewai-tools
```

## Конфигурация

Создайте файл `.env` в корне проекта:

```env
# Директория проекта для работы агента
PROJECT_DIR=D:\Space\life

# API ключи для LLM (если требуется)
OPENAI_API_KEY=your_api_key_here
# или для других провайдеров
# ANTHROPIC_API_KEY=your_api_key_here
```

Основные настройки находятся в `config/config.yaml`:

```yaml
project:
  base_dir: ${PROJECT_DIR}
  docs_dir: docs
  status_file: codeAgentProjectStatus.md
  
agent:
  role: "Project Executor Agent"
  goal: "Execute todo items for the project"
  backstory: "Automated agent tracking project tasks and statuses."
  allow_code_execution: true
  
server:
  check_interval: 60  # секунды между проверками задач
  max_iterations: null  # null для бесконечного цикла
```

## Использование

### Запуск агента

```bash
# Запуск основного сервера
python -m src.server

# Или через модуль
python src/server.py
```

### Структура проекта для работы агента

Агент ожидает следующую структуру в директории проекта:

```
D:\Space\life\
├── docs/                    # Документация проекта
│   ├── README.md
│   └── ...
├── todo.txt                 # или todo.yaml - список задач
└── codeAgentProjectStatus.md # Статусы выполнения (создается автоматически)
```

### Формат todo-листа

TODO-лист может быть в формате:
- Простой текстовый файл (`todo.txt`)
- YAML (`todo.yaml`)
- Markdown с чекбоксами (`todo.md`)

Пример `todo.txt`:
```
1. Инициализировать проект
2. Настроить окружение разработки
3. Создать базовую структуру
4. Написать документацию
```

## Документация

Подробная документация находится в папке `docs/`:

- [Архитектура проекта](docs/architecture.md)
- [Руководство по настройке](docs/setup.md)
- [API Reference](docs/api.md)
- [Интеграция с Cursor](docs/cursor_integration.md) - Подробное описание взаимодействия с Cursor IDE
- [Взаимодействие через репорты](docs/cursor_communication.md) - ⚠️ Важно: Механизм взаимодействия Code Agent и Cursor через файлы-репорты
- [Детальный рабочий процесс](docs/workflow_detailed.md) - Пошаговое описание выполнения задач
- [Ограничения LLM](docs/llm_limitations.md) - ⚠️ Важно: Ограничения слабых языковых моделей в Code Agent
- [Структура проекта](docs/project_structure.md)

## Формат `codeAgentProjectStatus.md`

Файл `codeAgentProjectStatus.md` хранится **в корне директории проекта** (например, `D:\Space\life\codeAgentProjectStatus.md`). 

Это **последовательный протокол** всех действий Code Agent и ответов агентов Cursor, который содержит:

- **Дату и время** каждого действия
- **Номер задачи** из todo-листа
- **Номер инструкции** (1-9 или кастомная)
- **Действие Code Agent** - что было сделано
- **Инструкцию для Cursor** - текст отправленной инструкции
- **Результат от Cursor** - краткий репорт из файлов-репортов
- **Время завершения** каждого этапа

**Важно:** Файл **не очищается** и ведется как протокол всех действий. Каждая новая задача добавляется как новый раздел в конец файла.

Пример записи:
```markdown
## [2025-01-26 14:30:00] Задача: 5. Создание клиентского слоя

### Инструкция 1: Создание плана
**Время:** 2025-01-26 14:30:15
**Действие:** Отправлена инструкция Cursor для создания плана
**Результат от Cursor:** Создан план в docs/results/current_plan_5.1.md
**Время завершения:** 2025-01-26 14:35:20
```

## Разработка

### Структура проекта

```
codeAgent/
├── config/              # Конфигурационные файлы
│   ├── config.yaml
│   └── agents.yaml
├── src/                 # Исходный код
│   ├── __init__.py
│   ├── server.py        # Основной сервер
│   ├── agents/          # Определения агентов
│   ├── status_manager.py
│   ├── todo_manager.py
│   └── tools/           # Пользовательские инструменты
├── docs/                # Документация проекта
├── tests/               # Тесты
├── .env                 # Переменные окружения (не в git)
├── .gitignore
├── pyproject.toml
└── README.md
```

### Запуск тестов

```bash
pytest tests/
```

## Лицензия

MIT License

## Автор

Разработано на базе фреймворка [CrewAI](https://github.com/crewAIInc/crewAI)
