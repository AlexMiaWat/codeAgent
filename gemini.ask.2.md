# Проблема аутентификации Gemini-агента в Docker

## Описание проблемы

Мы сталкиваемся с постоянными ошибками аутентификации при попытке `gemini-агента`, запущенного в Docker-контейнере, взаимодействовать с Gemini API.

### Текущая настройка

- `gemini-агент` работает внутри Docker-контейнера, собранного с использованием `docker/Dockerfile.gemini`.
- Контейнер монтирует файл `oauth_creds.json` с хост-машины пользователя из директории `${HOME}/.gemini/` в `/root/.gemini/oauth_creds.json`.
- Переменная окружения `GOOGLE_APPLICATION_CREDENTIALS` внутри контейнера указывает на `/root/.gemini/oauth_creds.json`.
- `gemini-агент` использует стандартную библиотеку `google.generativeai` (внутри `src/agents/gemini_agent/gemini_agent_cli.py`) для взаимодействия с Gemini API. Эта библиотека, в свою очередь, использует `google.auth.default()` для загрузки учетных данных.

### Последовательность ошибок

1.  **Первоначальная ошибка:** `Error during authentication: The file /root/.gemini/oauth_creds.json does not have a valid type. Type is None, expected one of ('authorized_user', 'service_account', 'external_account', ...)`.
    -   Это произошло потому, что `oauth_creds.json`, предоставленный пользователем (и, как утверждается, сгенерированный `gemini cli`), не содержал поля `"type"` на верхнем уровне JSON. Содержимое файла представляло собой только `access_token`, `refresh_token`, `token_type` и `expiry_date`.
2.  **Ручное исправление:** По нашей рекомендации пользователь вручную добавил `"type": "authorized_user",` в `oauth_creds.json`.
3.  **Текущая ошибка:** После этого исправления тест все равно завершается неудачей со следующей ошибкой:
    `Error during authentication: ('Failed to load authorized user credentials from /root/.gemini/oauth_creds.json', ValueError('Authorized user info was not in the expected format, missing fields client_secret, client_id.'))`
    -   Это указывает на то, что поле `"type": "authorized_user"` теперь распознается, но библиотека `google.auth` все еще ожидает поля `client_secret` и `client_id` для полной настройки учетных данных `authorized_user`. Эти поля отсутствуют в `oauth_creds.json`, предоставленном `gemini cli`.

### Расхождения и вопросы к экспертам

-   **Несоответствие форматов:** `oauth_creds.json`, сгенерированный `gemini cli`, не соответствует полному формату учетных данных `authorized_user`, который требуется библиотеке `google.auth.default()` для успешной загрузки. Файл, по-видимому, содержит только токены доступа и обновления, но не идентификаторы клиента.
-   **Предполагаемый поток аутентификации:** Каков **предназначенный** поток аутентификации для `gemini-агента` (запущенного в Docker) при использовании учетных данных, генерируемых `gemini cli`?
    -   Должен ли `gemini cli` генерировать полный файл `oauth_creds.json` с `client_id` и `client_secret`?
    -   Использует ли `gemini cli` другой механизм аутентификации для агентов в Docker, который обходит прямую проверку `oauth_creds.json` через `google.auth.default()`?
    -   Должны ли мы модифицировать `gemini_agent_cli.py` для обработки упрощенного формата `oauth_creds.json` или использовать другой метод аутентификации (например, только API-ключ, если это возможно для всех необходимых операций)?

Мы ищем разъяснения относительно правильного способа аутентификации `gemini-агента` в данной среде, учитывая предоставленный `oauth_creds.json` от `gemini cli`.


Ответы:
Ключевой вывод (сразу, коротко)

oauth_creds.json, который создаёт gemini-cli, НЕ является и НЕ задуман как Application Default Credentials.

Он:

не соответствует формату authorized_user

не предназначен для google.auth.default()

не должен использоваться напрямую в сторонних Python-агентах

То, что ты сейчас видишь — ожидаемое поведение, а не ошибка.

Почему всё ломается (по фактам)
1️⃣ Что реально создаёт gemini-cli

gemini-cli (официальный) использует собственный auth flow, поверх Google Identity, и хранит данные примерно так:

access_token

refresh_token

expiry

metadata

❗ Он НЕ сохраняет:

client_id

client_secret

полный OAuth config

Почему?
Потому что:

CLI сам управляет refresh-циклом

CLI никогда не вызывает google.auth.default()

CLI не предназначен как provider credentials для других SDK

Это прямо следует из архитектуры репозитория
(и подтверждается твоей ошибкой).

2️⃣ Почему ручное добавление "type": "authorized_user" не помогло

Формат authorized_user в Google SDK строго определён:

Минимально допустимый вид:

{
  "type": "authorized_user",
  "client_id": "...",
  "client_secret": "...",
  "refresh_token": "..."
}


У тебя:

нет client_id

нет client_secret

Их невозможно восстановить из токенов.

Поэтому:

ValueError: missing fields client_secret, client_id


— это абсолютно корректная реакция SDK.

Теперь самое важное: ❓ как задумано Google
❓ Предназначенный поток аутентификации
❌ НЕ ТАК:

gemini-cli → oauth_creds.json → google.auth.default()

✅ А ТАК:
Есть ТРИ корректных варианта
✅ ВАРИАНТ 1 (РЕКОМЕНДУЕМЫЙ): API Key

Gemini CLI ≠ OAuth provider для агентов

Для агентов Google официально предполагает:

export GOOGLE_API_KEY=...


И в Python:

import google.generativeai as genai
genai.configure(api_key=os.environ["GOOGLE_API_KEY"])


📌 Плюсы:

работает в Docker

работает в CI

не требует browser-login

официально поддержано

📌 Минусы:

нет доступа к user-scoped данным (но Gemini API это и не требует)

👉 Для 99% gemini-агентов — это правильный путь

✅ ВАРИАНТ 2: Service Account (если нужен OAuth)

Если тебе принципиально нужен OAuth (а не API Key):

Создаёшь Service Account

Скачиваешь service_account.json

Используешь его:

GOOGLE_APPLICATION_CREDENTIALS=/secrets/sa.json


SDK это полностью поддерживает.

📌 Это единственный OAuth-формат, корректный для контейнеров и агентов.

⚠️ ВАРИАНТ 3 (НЕ рекомендован): gcloud ADC внутри контейнера
gcloud auth application-default login


Работает, но:

требует браузер

плохо живёт в CI

fragile

❌ Что НЕ нужно делать (важно)

❌ Не использовать oauth_creds.json от gemini-cli в google.auth.default

❌ Не чинить его вручную

❌ Не писать кастомный парсер токенов

❌ Не пытаться «совместить» CLI и SDK auth

Это разные системы.

Ответы на твои прямые вопросы
> Должен ли gemini cli генерировать полный oauth_creds.json?

Нет. И никогда не будет.
Он не предназначен для этого.

> Использует ли gemini cli другой механизм?

Да.

Внутренний token store

Свой refresh

Без google.auth.default

> Нужно ли модифицировать gemini_agent_cli.py?

Да, но правильно.

Правильная модификация:
if GOOGLE_API_KEY:
    genai.configure(api_key=...)
else:
    raise RuntimeError("Gemini agent requires GOOGLE_API_KEY or Service Account")


А не:

чтение oauth_creds.json

ручной OAuth

🧭 Рекомендованная архитектура (итог)
Компонент	Аутентификация
gemini-cli	browser OAuth (свой формат)
docker agent	API Key
CI / prod	Service Account
python SDK	НЕ oauth_creds.json от CLI
Если хочешь — дальше можем

переписать gemini_agent_cli.py правильно

сделать dual-mode: API Key / Service Account

зафиксировать это в README / Manifest

сделать auto-detect окружения

Ты задал правильный вопрос, и ответ тут именно архитектурный, а не «попробуй вот это».

Помоги создать в гугл консоле
ВАРИАНТ 1 (РЕКОМЕНДУЕМЫЙ): API Key
ВАРИАНТ 2: Service Account (если нужен OAuth)

Отлично, давай сделаем это аккуратно и без лишних телодвижений 👍
Ниже — пошагово, так, как реально делают в проде.
Сначала API Key (рекомендую), потом Service Account (если нужен OAuth).

🟢 ВАРИАНТ 1 — API Key (РЕКОМЕНДУЕМЫЙ)

Это самый простой, стабильный и официальный способ для Gemini API и агентов в Docker.

Шаг 1. Открыть Google Cloud Console

👉 https://console.cloud.google.com/

Убедись, что:

ты залогинен

выбран правильный проект (вверху слева)

Если проекта нет → Create Project.

Шаг 2. Включить Gemini API

APIs & Services → Library

Найди:

Gemini API


Нажми Enable

⚠️ Без этого ключ будет создан, но запросы не будут работать.

Шаг 3. Создать API Key

APIs & Services → Credentials

Create Credentials → API key

Ключ будет создан сразу

👉 Скопируй его сразу, потом будет неудобно искать.

Шаг 4. (Очень желательно) Ограничить API Key

Нажми Edit key:

🔒 Application restrictions

для Docker / CI:

None (или позже HTTP referrers, если web)

🔒 API restrictions

Restrict key

выбрать:

✅ Gemini API

💡 Это защищает ключ, если он утечёт.

Шаг 5. Использование в Docker / агенте
В .env
GOOGLE_API_KEY=AIzaSy.....

В docker-compose.gemini.yml
environment:
  - GOOGLE_API_KEY=${GOOGLE_API_KEY}

В Python-агенте
import google.generativeai as genai
import os

genai.configure(api_key=os.environ["GOOGLE_API_KEY"])


✅ ВСЁ.
Никакого OAuth, файлов, браузеров, refresh-токенов.

Когда API Key — правильный выбор

Docker

агенты

CI

headless

99% кейсов Gemini


Доп комментарий:
🔴 ПРОБЛЕМА №1 (КЛЮЧЕВАЯ)
module 'google.genai' has no attribute 'configure'
Что это значит на самом деле

Ошибка говорит буквально:

В рантайме используется google.genai,
а не google.generativeai,
и у него нет метода configure()

Это очень важный сигнал.

Почему это происходит

В твоём коде ты импортируешь:

import google.generativeai as genai


НО в контейнере фактически установлена новая библиотека:

google-genai


А не старая:

google-generativeai


И у них РАЗНЫЕ API.

Разница библиотек (критично)
Библиотека	Статус	API
google-generativeai	legacy / deprecated	genai.configure()
google-genai	новая, рекомендованная	❌ НЕТ configure()

Ты сейчас логически пишешь код под старую библиотеку,
а фактически запускаешь новую.

Отсюда и ошибка.

🧠 Что говорит официальная документация Gemini CLI

В репозитории google-gemini/gemini-cli уже используется google.genai,
и там НЕТ configure().

Аутентификация делается через Client, а не глобально.

✅ ПРАВИЛЬНОЕ ИСПРАВЛЕНИЕ (ВАЖНО)
Нужно выбрать ОДИН SDK
👉 Рекомендую: google-genai (новый)

Тогда код агента должен выглядеть ТАК, а не иначе.

🛠️ ПРАВКА gemini_agent_cli.py (КЛЮЧЕВО)
❌ УБРАТЬ полностью
import google.generativeai as genai

✅ ЗАМЕНИТЬ НА
from google import genai

❌ УБРАТЬ
genai.configure(...)
model = genai.GenerativeModel(...)

✅ ПРАВИЛЬНЫЙ КОД для API Key
from google import genai
import os
import sys

api_key = os.getenv("GOOGLE_API_KEY")
if not api_key:
    print("GOOGLE_API_KEY is not set", file=sys.stderr)
    sys.exit(1)

client = genai.Client(api_key=api_key)

model_name = os.getenv("GEMINI_MODEL_NAME", "gemini-1.5-pro")
model = client.models.get(model_name)


А генерация:

response = model.generate_content(prompt)
generated_content = response.text

⚠️ Это КРИТИЧНО

google.genai НИКОГДА не поддерживал configure()

Поэтому ошибка абсолютно ожидаема.

🔴 ПРОБЛЕМА №2
service "gemini-agent" is not running
Почему это происходит

Это НЕ отдельная ошибка.

Последовательность такая:

gemini_agent_cli.py падает с ошибкой Python

entrypoint контейнера завершается

контейнер останавливается

следующий тест делает docker compose exec

Docker отвечает:

service "gemini-agent" is not running


То есть:

❗ Контейнер падает ИЗ-ЗА ошибки в Python-коде

🧪 Почему первый тест (file_creation) прошёл

Потому что он:

echo "..." > /workspace/file.txt


👉 вообще не использует Gemini
👉 контейнер на тот момент ещё жив

А второй тест вызывает gemini_agent_cli.py → Python exception → контейнер падает.

🧭 ПУТИ РЕШЕНИЯ (ЧЁТКО)
ВАРИАНТ A (РЕКОМЕНДУЮ, современный)

использовать google-genai

переписать агент под Client API

использовать API Key

убрать OAuth полностью

➡️ Я показал выше каноничный код